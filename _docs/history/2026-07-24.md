# 2026-07-24

## v2 렌더용 외부 에셋 확보 (다운로드·분류·목록화, 렌더/생성 없음)

v2 데이터 생성의 배경·조명·distractor 다양성 확대를 위한 CC0/CC-BY 에셋 확보. **렌더·파이프라인 코드 수정 없음.** 게이트: 디스크(E: 1.3TB 여유=PASS), 라이선스(A 불명확→탐색만, B/C 명확→진행).

### A. Isaac Sim 창고 에셋 — 다운로드 보류(라이선스), 대체 CC0 탐색만
- Isaac Sim assets 현재 6.0.1(2026-06), 5-part URL+MD5 확보. **라이선스 불명확**: License FAQ("렌더 출력물/데이터셋 배포 OK") vs "NVIDIA Isaac Sim Additional Software and Materials License"(§2.2 배포 제한/§2.4 파생물 금지/§3 internal use, AI해석이라 불확실) **상충** → 데이터셋 공개가 논문 차별점이라 리스크 회피 위해 **다운로드 보류**(사용자 결정 보류).
- **대체 CC0 탐색 결과**: Poly Haven CC0로 포토리얼 창고 근경 구성 가능(Isaac 불필요). 확보 가능=셔터/창고문 4, 공장 facade 벽, 스틸선반 4, 갠트리크레인, HVAC 덕트·파이프·창고조명 8+, 적재 박스/크레이트/카트. **CC0 gap 3**: ①포토리얼 고층 팔레트 랙 ②구조 기둥/컬럼 ③완결 실내 씬+로딩도크(②③은 자작 단순기하+ambientCG CC0 재질로 대체 가능). Kenney(CC0)는 저폴리라 포토리얼 톤 불일치=비권장. **아직 다운로드 안 함.**

### B. HDRI 확대 9 → 30개 (Poly Haven CC0)
- `data/pallet/hdri/`에 21개 신규 + factory_yard 재다운로드. 전량 2k `.hdr`, md5·디코드 검증. LICENSE.txt(CC0)+SOURCES.txt.
- 3군: **야간 0→9(최우선 충족, time_of_day=night+점광원)** / 실내 4→10 / 야외주간 5→11. 목표 25~30 부합.
- 기존 9개 중 야간 0개 확인(사용자 추정 일치).
- **factory_yard**: 파일은 복구·검증됐으나 과거 실패 원인은 imageio HDR백엔드 부재(에이전트 도구 문제). **파이프라인 gen_dataset_v4.py:474 `_DROP`에 여전히 포함** — Blender magenta 여부는 별개라 미검증. un-drop 하려면 Blender 렌더 테스트+코드변경 필요(안 함). mall_parking_lot(보라)은 미시도.

### C. Distractor 물체 97종 (Poly Haven CC0 65 + Google Scanned Objects CC-BY 4.0 32)
- `data/pallet/distractors/{large,medium,small,road,indoor}/` — **폴더명 ASCII**(한글 mojibake 회피, Win↔Ubuntu 공유), 한글 라벨은 manifest에 보존. 폴더별 LICENSE.txt+SOURCES.txt + `distractors_manifest.csv`(97종 bbox/faces/출처/라이선스).
- 분류: 대 12 / 중 19 / 소 40 / 도로 8 / 실내 18. 578MB, 전량 텍스처 O, trimesh bbox(m) 실측.
- **현재 파이프라인 distractor ≈ 12종**(OCCLUDER_TIERS/CARGO_SOURCES, 배럴+카드보드 편중) → 차/가구/카트/사다리/펜스/실내용품 공백을 이번에 메움.
- 미확보: 라바콘(파이프라인에 TrafficCone 존재), 승합차·트럭·지게차, 주차금지·공사표지판·삼각대, 헬멧(GSO 모자로 부분대체), 정수기 — Sketchfab 제외 방침상 CC0/CC-BY에 없음.
- ⚠️ `steel_frame_shelves_01`=메시 21m 스케일 이상(rescale/교체 필요). 일부 멀티인스턴스 에셋(bbox 큼).

### 미해결 / 다음 TODO (사용자 결정 대기)
- A: CC0 창고 구조 조각 실제 다운로드 여부 + 팔레트 랙 gap 처리(자작 vs Isaac 내부R&D option2, 단 v2 학습 투입 전 재문의 조건).
- C 폴더명 ASCII 유지 vs 한글 / steel_frame_shelves_01 rescale / 미확보 물체 추가 소싱.
- factory_yard un-drop용 Blender magenta 테스트.
- 에셋 바이너리는 data/(gitignore) — manifest·LICENSE 버전관리 여부.
- ★ 이후 실제 v2 렌더(생성)는 별도 태스크(이번엔 금지).

## perm 방위각 교체 + yaw 재해석 + 데이터 공개 준비 (2026-07-24 후반, 렌더/생성 금지 유지)

### perm 방위각 규칙 교체 (승인·구현·검증)
- **검증 먼저**: 기존 라벨로 신·구 규칙 compute-only 대조 → 순수 규칙차(normal-facing vs 방위각) **pooled 0.29%**, POST셋(4pallet) 0.00%, 정확히 **45° 코너**(|az_rel_front| 43.7~46.4°·저앙각)에서만. v4_split OLDvNEW 8.1%는 규칙차 아니라 2026-07-03 이전 baked 오배정(BAKEDvNF 8.2%). 플롯 `scratchpad/A_perm_old_vs_new_scatter_heatmap.png`.
- **구현**: `blender_math.py` compute_perm_v4 = argmin|φ_cam−φ_face|(+_wrap_deg, 검증한계주석 L143). `gen_dataset_v4.py`·`gen_4pallet_mask.py` **FACING_MARGIN_MIN 0.15→6.0°**(옛 ±6° 코너밴드 재현, ⚠️v2 파일럿 재검증 필요) + `front_facing_cos`→`front_visibility_cos`(면선택 미사용). front_cos≥0.40=가시성게이트 유지. smoke 18프레임 0크래시·FRONT==방위각 18/18. **부작용 batch수정**: gen_preview10/gen_topview_test도 6.0°(0.15가 도와 비교돼 무력화되던 것). 학습/eval은 이 필드 미사용.

### yaw 원인 재해석 + PRE 오염표식
- 사용자 자기수정: "레이블ID오염이 yaw 주원인" 규모 안맞음(오배정=90°인데 median 5~6°). → 오염=꼬리·코너뷰 기여, median=별개 precision. 분포 히스토그램 검증은 **meas계열 err_180 하네스 경로 대기(차단)**.
- **mixed_v8(9k,31%)=측정불가(E: 전역 부재)**. 수량: v4_split 4000=train/5000=train+val. **★in-repo tracked 학습은 object-frame `training_data/train`(4000, perm_v4 없는 v11) 소비 — v4/v4_split 미배선**(논문 실학습셋 확인 필요). PRE 오염표식: `training_data_v4_split/README_CONTAMINATION.md`(8.2%)·`training_data_v4/…`(2.5%=2프레임 무의미).

### 배경/에셋 프리뷰
- HDRI 30(Poly Haven CC0) tonemap 몽타주 `data/pallet/hdri/_hdri_preview_montage.png`(순수 numpy RGBE 디코더). glTF 3D 배경 렌더 `data/pallet/background/_bg_preview_montage.png`. distractor gap-fill Sketchfab CC-BY 16종(97→113) 몽타주 `distractors/_sketchfab_gap_montage.png`.

### 데이터셋 공개 라이선스 원장
- `_docs/dataset_license_ledger.md`(전 에셋 저장경로·출처·저작자·라이선스·공개여부 + **URL 36개** + 블로커). `_docs/attribution_cc-by_appendix.md`(CC-BY 부록). 종합판정 **공개불가(N)**.
- 블로커: **B1 NoAI팔레트**(완화→repoint) / **B2 Isaac occluder**(WetFloorSign, gen_palletobj_scenarios/gen_trunc_addon, 기존 "occluder CC0" 정정) / **B3 USD매핑**(해소) / **B4 floor14 미검증** / **B5 CC-BY attribution** / **B6 isaac_assets 제외**. 즉시클린: HDRI30·PH distractor65·wood9·CC0팔레트·pallet_full.obj.

### ★팔레트 NoAI 교체 (완료)
- 카탈로그 렌더 `_pallet_catalog_0123/pallet_0123_row.png` 육안: **P0빨강·P1초록=플라스틱(유지,CC-BY) / P2어두운·P3밝은=목재**. NoAI=목재라 P2/P3 중 하나 → **목재 2종 다 교체**.
- `scene_2.usd`(P2)·`scene_3.usd`(P3) → `data/pallet/_noai_quarantine_usd/` 격리(삭제X, README). ⚠️8+스크립트 참조 → v2 gen 전 repoint 필요.
- 새 목재 2종(`pallets_v2_add/models/`, 정규화·12kp측정·kp오버레이 완료): **①J-Toastie**(Poly Pizza CC-BY3.0, poly.pizza/m/XSKlcrzyi6) + **②EUR-Pallet**(BlenderKit CC0, LensError, .../751202c6, 메트릭 EUR 800×1200×144). 후보#1 Scan Wooden Pallet=막힌면+142k포토스캔+non-manifold로 탈락. 오버레이 `_stringer_kp_overlay.png`·`_jtoastie_kp_overlay.png`·`_eurpallet_kp_overlay.png`. measurements.json 3종.

### 미해결 (사용자 입력/결정 대기)
- 논문 실제 학습셋 확인(object-frame vs v4_split) / yaw 히스토그램용 meas err_180 경로.
- 남은 공개 블로커: B2(Isaac occluder 교체) / B4(floor14) / B5(attribution 동봉) / B6(isaac_assets 제외) / 파이프라인 repoint(scene_2/3.usd→새 목재).
- perm 6.0° 게이트는 v2 파일럿 첫 실행 때 재검증(≤60°만 검증됨, 고앙각 미검증).

### 🚨 일정 영향 (사용자 정정 2026-07-24 — 원장 상단에도 명시)
- **B1(NoAI 팔레트)+B2(Isaac occluder) 겹침 → 기존 렌더 데이터 전부 사용 불가**: v4/v4_split/4pallet_mask=B1(격리 NoAI baked), v3 palletobj/trunc_addon/addon_v1=B2(Isaac occluder baked).
- → **(a) v2 재생성 = 필수(선택 아님). (b) 현 논문 결과(paper_s2·실도메인 평가)는 NoAI 기반 → 발표 사용 불가**, v2 후 재평가.
- **작업 큐 확정**: **B2 최우선**(OCCLUDER_POOL 19 Isaac유래 식별→CC-BY distractor 113종으로 크기분류 대체 매핑+repoint, 출처 원장기록) → B4(floor14) → B5(attribution) → B6(isaac_assets 제외) → 파이프라인 repoint(scene_2/3.usd→J-Toastie/EUR). **v2 렌더는 B2~B6+repoint 후 파일럿 논의**.
- 다양성 note(조치X): P3 막힌데크 목재 빠짐→슬랫목재 2종, 막힌목재 소멸이나 솔리드마스크는 P0 담당→치명적 아님. 논문 한계로 기록.

## v2 세팅 조사 A~D (2026-07-24, 구현·배선·v2렌더 없음)
- **★B2 오탐 정정 [확인]**: 프로덕션 blend 157MB grep Isaac 지문 0 + occluder명=Poly Haven CC0(WetFloorSign_01·cardboard_box_01·metal_toolbox). "WetFloorSign=Isaac"은 이름충돌 오탐. → v3 palletobj/trunc/addon은 Isaac 무관, B1(USD목재)만 실블로커. 원장 상단 일정영향 정정.
- **A1 (B4) floor14**: 9/14 Poly Haven CC0 확정[확인], 5/14(tile_white·tile_brown·wood_laminate·dirt_ground·red_earth) 미검증(PH 404, 개명추정)→CC0 교체후보 확보(floor_tiles_06 등)·미실행(승인대기). `textures_floor/SOURCES.txt·LICENSE.txt` 생성. floor_and_mask.py:34가 파일명만 참조→동일명 교체시 코드 무변경.
- **A2 (B5)**: `_docs/attribution_bundling_plan.md`(위치=릴리스루트 ATTRIBUTION.md + per폴더 SOURCES, 변경사실 명시: USD baking·P0 emissive0·정규화·glb재추출). ✅GSO 32→128 부록 반영 완료(아래 "task5 + 원장/부록 최종 갱신" 섹션).
- **A3 (B6)**: `.gitignore`가 data/ 이미 제외[확인] → `data/pallet/_DISTRIBUTION_EXCLUDE.txt`(isaac_assets/+_noai_quarantine_usd/+작업산출물) 생성.
- **B repoint ⛔STOP**: 팔레트 로더 `randomizers.py:574 bpy.ops.wm.usd_import`=USD전용→.glb 하드에러[확인]. 필요작업=로더 glb분기(gltf임포터 693에 존재) or glb→usd변환 + baked blend 재bake + **canonical 재보정(glb Z-up vs USD Y-up ORIENTATION_OVERRIDES, J-Toastie 6×스케일, efront_kp12 면 재도출)**. =v2 통합작업이라 중단 정당. EUR/J-Toastie 에셋 자체 유효.
- **C1 distractor 분류**: 113(→D후 209). box·container·창고·사무 분포. source PH65 CC0/GSO/SF CC-BY.
- **C2 clutter 수용력 [확인]**: 풀 고정(distractor8+box7)→상한 15 clutter+팔레트1=16, **20 구조적 불가**(복제/메시추가 필요). 타이밍 flat(5→1.6s/10→1.85s, 창고BG가 지배). 40k≈**2.1일**(4.63s/저장프레임, production로그).
- **C3 clutter 분포 ★전제 반전 [확인]**: **outside/night/noapril 3-도메인 평가셋 부재**(noapril 0hit·real_data 1924장 단일 야외주간 세션·night/outside=합성HDRI 분류). 실 real median **4**(range3~6, n=10 육안). 현합성(2~3)과 개수차 작음, ★gap=3D mid-bg(차량·컨테이너·펜스·사람·라바콘) vs 평면2D HDRI.
- **D GSO 확장**: 32→**128**(+96, box39·container31·office14·warehouse10·other2, 전부 CC-BY, 0.08~0.48m). manifest 209, +1.2GB, 정규화 몽타주 PASS. ⚠️GSO OBJ의 MTL map_Kd 경로 불일치(파이프라인 소비시 재bind 필요할 수 있음).
### 쌓인 결정 대기
- A1 floor 5종 CC0 교체 승인? / B repoint=v2통합(로더+blend+canonical) 진행 or 파일럿까지 보류? / C clutter=3D-bg 배치강화(20+ 필요시 복제로직) / paper_s2 실학습셋(v4·4pm=B1 vs palletobj) 확인.

## task5 + 원장/부록 최종 갱신 (2026-07-24, 문서만 편집 — 렌더/생성/재bake 없음)

### ★ task5 [확인, zstd 해제 grep] — B1: 격리 ≠ 해소 (blend 재-bake 필수)
- 파이프라인이 여는 `data/pallet/blender_scene/synth_data_scene.blend`(zstd 압축, 해제 308MB) 안에
  **Pallet_0~3 형상이 baked** + custom prop `pallet_source_file` 4개(=scene.usd/scene_1/2/3.usd)로 출처 표식.
- 즉 **격리한 `scene_2/3.usd`(P2/P3 NoAI 목재)가 blend에 mesh로 잔존** → 이 blend를 렌더하면 NoAI 팔레트가
  계속 출력됨. (blend에 Old_Wooden/Luka 문자열 0 — blend는 USD 파일명만 저장, 메시는 baked.)
- **결론**: USD 격리만으로 B1 미해소. **완전 해소 3단 = ① USD 격리(완료) + ② blend 재-bake(Pallet_2/3 →
  새 목재 J-Toastie/EUR) + ③ 로더 repoint(`usd_import`→.glb 분기 or glb→usd; 8+스크립트 scene_2/3.usd 참조)**.
  ②③ = B repoint STOP 사유(=v2 통합작업). → 원장 B1 상태 "미해소(blend 재-bake)"로 갱신, 상단 일정영향에
  "B1 해소엔 blend 재-bake 필수" 한 줄 추가.

### 문서 갱신 (3개 파일)
- **`_docs/dataset_license_ledger.md`**:
  - B1: "완화→repoint" → **"미해소(blend 재-bake + repoint)"**. blend baked 잔존 상세(task5) 반영.
  - **B2: "미해결 HIGH" → "종료(오탐)"** — decompressed blend(157MB) grep Isaac 지문 0, occluder=Poly Haven
    CC0(WetFloorSign_01·cardboard_box_01·metal_toolbox 등). 이전 "WetFloorSign=Isaac S_WetFloorSign.usd"
    단정은 파일명 이름-충돌 오탐(verify 누락). 그룹 7·8·종합판정·상속표에서 B2 제거, occluder=CC0/CC-BY 반영.
  - GSO 32→128, distractor 총 ≈209 반영(그룹 5, B5 목록). occluder CC-BY 8종(추정)을 B5 대상에 추가.
  - **머신 역할 범위** 상단 명시(verbatim): "이 워크스테이션 = 합성 데이터 생성 전용. 평가셋(outside/night/
    noapril, GT)·학습 코드·meas 하네스는 별도 머신." + real_data 1924장=self-training unlabeled 풀 추정.
  - **죽은 항목 종료**: paper_s2 실학습셋 확인 = 불필요(v2 재생성으로 대체) / C3 실 평가셋 clutter 분포 = 이
    머신 범위 밖 / B2 = 오탐. **보류**: 20+ clutter 복제로직(에셋 통합 후 판단, 지금 결정 안 함).
- **`_docs/attribution_cc-by_appendix.md`**: GSO **32→128** 반영(확장분 box39·container31·office14·
  warehouse10·other2, 전부 CC-BY, Google Research via Gazebo Fuel). **우리가 가한 변경 고지**(CC-BY §3(b)):
  접지(min-z→0)·XY중심 정규화·(일부)glb/obj 재익스포트·MTL map_Kd 재bind. occluder CC-BY 8종(추정)·floor
  미검증을 §3에 caveat로 명기(silent 미표기 방지). → 위 "⚠️GSO 32→128 부록 반영 필요"(A2) **완료**.
- **`_docs/history/2026-07-24.md`**: 본 섹션.

### 갱신 후 블로커 상태
- **종료**: B2(Isaac occluder=오탐) · B3(USD 매핑=재질 확정) · **B4(floor 14/14 Poly Haven CC0 확정)**.
  **잔여 실질**: B1(blend 재-bake+repoint) · B5(attribution 동봉) · B6(isaac_assets 제외, `_DISTRIBUTION_EXCLUDE.txt`).
- 종합판정 여전히 **공개불가(N)**, 단 팔레트측은 B3 해소·B2 오탐·B4 해소, **B1(blend 재-bake)만 실질 남음** + B5/B6.

### B4(floor) 해소 반영 (floor 교체 완료 — 문서 갱신)
- floor 14/14 **CONFIRMED Poly Haven CC0**. 미검증 5종을 동등 Poly Haven CC0로 **내용 교체**(파일명 유지 =
  `floor_and_mask.py:34` 참조만이라 코드 무변경, 원본 백업 `textures_floor/_replaced_originals/`):
  tile_white→floor_tiles_02 / tile_brown→brown_floor_tiles / wood_laminate→laminate_floor_02 /
  dirt_ground→brown_mud_dry / red_earth→cracked_red_ground (URL=polyhaven.com/a/<slug>). 유지 9종은 기존
  slug 실증. `textures_floor/{SOURCES.txt,LICENSE.txt}` = 14/14 CC0, 0 UNVERIFIED. 실내 룩 확인(페일 스톤타일·
  오크 라미네이트=실내 적합, mud/red_ground=실외). → 원장 B4 해소·floor CC0 클린 이동, 부록 §3 CC0 확정.


## v2 통합 재-bake 완료 (2026-07-24 심야)
- **★B1 blend 관문 해소** [확인, zstd 해제 grep 재검증]: `synth_data_scene.blend` 재-bake로 NoAI 목재(scene_2/3.usd 유래) 완전 제거 — 새 blend에서 scene_2.usd·scene_3.usd·LP_merge_lambert16·Material_018·**Legacy_Pallet_2/3=0**(백업엔 각 존재). ★과거 로더가 불일치 팔레트를 삭제 않고 `Legacy_*`로 rename만 한 NoAI **중복본**까지 함께 제거(이게 없었으면 미해소). 새 목재 **Pallet_2=J-Toastie(CC-BY3.0)·Pallet_3=EUR(CC0)** 투입.
- **3부 조건 완료**: ①USD격리 ②blend재bake ③로더 repoint = `randomizers.py:_import_pallet_asset`에 `.glb` 분기(최소변경) + `config/synthetic/blender.yaml` P2/P3→새 glb + `models_usd/`에 새 glb 복사.
- **V검증**: V1(NoAI 식별자 0)·V2(P0/P1 9kp 로컬좌표 회귀 diff 0)·V3(4종 오버레이 P2=J-Toastie·P3=EUR)·V4(`efront_kp12.py` P2/P3 비율행 신값 갱신, P0/P1 불변, import 확인; bottom_open 일부 반전=신 기하)·V6(gen_dataset_v4 1프레임 스모크 PASS, 새 EUR 로드) 통과. **V5 distractor 209 = 1GB 게이트+GSO magenta로 STOP**(별개 미완).
- blend 234.6MB·object 1517→286. 백업 `synth_data_scene.PREBAKE_BACKUP_20260724_181050.blend`(md5 무결)+WIP본. 원장 B1→해소, **종합판정 "파이프라인 라이선스 클린"**.
- **잔여**: B5(attribution 동봉)·B6(isaac 배포제외) 절차 + distractor 209 통합(미완, 방향결정 대기) + **v2 재생성**(기존 데이터셋 클린화).
- **다음 단계(미착수)**: distractor 209 방향(①외부참조/②GSO fix+서브셋 pack) / **규약 구현**(Visibility Gate·Placement f_target·Intrinsics DR·Illumination DR)+레이블 스키마 / v2 재생성.


## legacy-purge + distractor 209 append (2026-07-24 심야, 재-bake 후속)
- **★occluder/cargo 생존 확인** [확인, zstd 대조]: DISTRACTOR_NAMES 8(Barrel_01·Barrel_1·concrete_road_barrier·Sketchfab_model×3·TrafficCone×2) + BOX_NAMES 7(PalletBox_0~6) = **13/13 생존**. 1517→286 감소는 팔레트+Legacy 제거뿐, occluder 무손상 → 가림 렌더 가능.
- **legacy-purge** [확인]: `randomizers.py` 로더를 rename→**삭제on성공**으로 수정(fallback 유지: 새 import 확인 후에만 구 팔레트 삭제, 실패 시 복원). `Legacy_Pallet_0/1`(플라스틱)=orphan 중복본 판정 → purge(8 obj + orphan). **P0/P1 9kp 회귀 diff 0**(save 게이트). → Legacy 누적·NoAI 재-baked 원인 제거.
- **distractor 209 unpacked append** [확인]: GSO 128 MTL `map_Kd` 일괄 repoint(`../materials/textures/`) → **magenta 0/128**. 209종(PH65 gltf + GSO128 obj + SF16 glb) append, `Dist_<name>` Empty로 `Distractors_v2` 컬렉션(hidden). unpacked 외부참조(**pack 금지 준수**, per-image 타겟 unpack). 메타: `distractor_size_class`(대/중/소/도로/실내)·`bbox_m`·source·license·`is_distractor_v2` = 209/209. tier small117/indoor40/medium21/large18/road13. blend 245→**342MB(<1GB)**. object 278→1122.
- ★부작용 자가교정: 첫 append가 global `unpack_all`로 472 packed 이미지 전부 외부화(395MB 중복, gso/ph 소스 이탈) → **POSTBAKE_CLEAN 백업서 복구**·per-image unpack로 재시도(백업이 값을 함).
- **내 검증(zstd)**: Legacy_0/1=0 · is_distractor_v2=209 · size_class=209 · scene_2/3.usd=0(NoAI 여전) · Barrel_01=12 · Pallet_2=1 · 새목재 present. random20 몽타주 육안 정규화 PASS.
- 변경파일: `randomizers.py`(삭제on성공) · `synth_data_scene.blend` · gso MTL×128 · `blender_scene/textures/`(62MB SF sidecar). 백업 `POSTBAKE_CLEAN_20260724_191902.blend`.
- ⚠️ **다음 phase(미착수)**: DISTRACTOR_NAMES 선택배선을 209로(현재 옛 8=Sketchfab_model×3 unknown-license가 occlusion에 쓰임) = Placement 소관 / Visibility Gate·Intrinsics·Illumination DR·레이블 스키마 / v2 재생성.


## 해상도·종횡비 DR 스펙 추가 (2026-07-24, 규약 구현 phase 대상 — 미구현)
- 사용자 지시: 규약 구현에 **해상도·종횡비 DR** 포함. 배경=렌더 해상도/종횡비 하드코딩(`randomizers.py:523-524`·lens `:545`), 16:9가 4:3 대비 가로 25% 더 눌림(비등방 0.563 vs 0.750), 다중카메라 목표인데 미랜덤.
- 처방: 4:3 640×480 50%(D435i)·16:9 960×540 25%·3:2 720×480 15%·1:1 560×560 10%·(선택)1280×960 ≤5%. 픽셀수 억제(렌더시간∝픽셀·40k). 다중스케일 입력(400/320/512)=안 함(baseline 흐려짐).
- ★3 동반수정(없이 흔들면 층화 붕괴): ①projected-size bin px단위(gen_palletobj_scenarios.py:770·gen_trunc_addon.py:996-1001)→이미지폭 비율 or 640정규화(감사도) ②camera_effects(camera_effects.py, blur 0.5~1.8px)를 해상도 비례 스케일 ③surf_dist 공식(fx·1.3/px, gen_palletobj_scenarios.py:771·gen_trunc_addon.py:1001) 단일화(Intrinsics DR lens_mm과 무충돌).
- 감사 추가: 종횡비별 프레임수(처방=실측)·projected-size 분포가 정규화 후 4종횡비 동일한지.
- **스펙 문서**: `_docs/method/v2_domain_randomization.md` 생성(코드 위치 grounding). 구현·렌더 안 함.

- **해상도DR 정교화 (사용자 승인+보강 2026-07-24)**: `_docs/method/v2_domain_randomization.md`를 ★3→★5 동반수정으로 갱신. ①projected-size=**이미지폭 비율 확정**(640정규화 아님; 네트워크가 정사각 리사이즈본만 봄). ③surf_dist=**해상도 소거형 Z=focal·W_pallet/(sensor·f)** 재작성→Intrinsics DR 무충돌. ②camera_effects **효과별**(blur 스케일/noise 안함/vignette 자동/JPEG 렌더해상도). **신규 ④** 평가 belief→px SCALE(×12.8/9.6 640하드코딩)=**이 repo엔 없음**[확인: evaluate_on_val.py:188-190 per-frame·run_dope_live:271 proc_scale·detector:558 ×8]→**meas 하네스(별도머신) 소관**(machine-role). **신규 ⑤** preprocess squash=정사각resize(evaluate_on_val:170·self_train:101·DOPE utils:340, 임의종횡비 수용) 단 **run_dope_live:271=종횡비 보존→train↔live 불일치**. 기록만: 1:1 synth edge case·레터박스 상쇄→추론프로토콜(undistort→4:3정규화→squash). 감사+anisotropy 분포. 구현·렌더 없음.

- **해상도DR 문서 2정정 (사용자 2026-07-25)**: ①**근거 정정** — "정사각resize라 640/320·960/480 동일"은 폭에만 참·높이엔 거짓(정사각resize는 폭·높이를 다른배율로 눌러 4:3 높이×0.75 vs 16:9 ×0.563). 폭비율 층화는 유지하되 근거를 "폭만 불변량·높이 비등방이 종횡비따라 달라짐=종횡비DR 필요이유"로 교체. ⑤**BUG 재분류** — live(run_dope_live:271 proc_scale=400/h 종횡비보존)가 정사각 학습과 불일치=미학습 형태+폭 어긋남, 프로토콜로 덮지말고 정사각 squash로 고칠것(규약phase). **★live 오염확인[확인]**: run_dope_live:450=오버레이 이미지만 저장·정량 미저장 → 논문 정량표(ADD/PCK/Reproj)는 live 아님(evaluate_on_val 448²=학습동일 비오염). 미확인=별도머신(정성 데모그림 live 사용여부·meas squash 방식). ④열린항목: meas 640하드코딩 SCALE 미확인=그 머신 첫확인. 감사에 anisotropy×f_target 교차확인 추가. 구현·렌더 없음.

- **별도머신(meas) 첫확인 우선순위 문서화 + 세팅 phase 종료 (2026-07-25)**: `v2_domain_randomization.md` 상단에 우선순위 섹션 — [높음] meas 640하드코딩 SCALE 실재여부 · [높음] meas 전처리=정사각 squash인지(live처럼 종횡비보존이면 실도메인 평가 틀어짐) · [낮음] 논문·슬라이드 정성그림 live 사용여부. ★[높음]2개 걸리면 슬라이드 px·ADD·yaw 전부 영향 → 그 머신 작업 시 최우선(grep 몇번). 이 repo 확인분=비오염(evaluate_on_val per-frame·448²·run_dope_live 정량미저장). **세팅 phase(라이선스·에셋·blend통합·DR스펙) 종료 — 다음 지시 대기. 구현·파일럿·v2렌더 전부 금지 유지.**


## v2 규약 구현 착수 — Phase A 완료 (2026-07-25, 파일럿·본렌더 없음)
- 순차 11단계를 4 phase로: A(1-3 데이터/블로커) → B(4-9 아키텍처/레이어) → C(10 dry-run) → D(11 캘리브). 핵심설계: 순수함수분리→dry-run 렌더0 설계검증 / 실측배정(target≠actual도 폐기말고 actual bin)으로 통과율28%(v_gate_reject 14405) 해소 / 안전게이트만 하드.
- **Phase A [확인, 내 grep 검증]**: ①스키마 additive — `objects[0].v2_labels`(pallet_type·material_variant[버려지던 randomize_pallet_appearance 반환 캡처]·occlusion_fraction[9]·f_cargo/occ/total·position_mode·luma_actual·edge_map) + `camera_data`(resolution·aspect_ratio·scene_preset). front_cos/margin=기존 front_visibility_cos/facing_margin 재사용. per-corner binary occlusion은 원래 없음(aggregate raycast_visibility만). ②occlusion 재배선 — config distractor_names 8→5(**Sketchfab_model×3 제거, 3경로 grep 실증**), 신규 `distractor_pool_v2.py`(209풀·domain_plausible[indoor196/outdoor70]·size가중선택), randomizers additive 분기(distractor_names=None=레거시). ③fill_ratio — `compute_distractor_fill_ratio.py`(trimesh 8방위), manifest 209/209(projected147/hybrid60/raster2), 극단<0.15 = plastic_broom0.061·hand_truck_01 0.117. cargo box7=측정불가(blend baked). py_compile 5/5, manifest 원본14열 0-diff.
- ⚠️ **리스크(파일럿 전 필수)**: `Distractors_v2` 컬렉션 view-layer exclude면 "조용히 0 distractor" 렌더→fallback 안걸림→Phase D 첫렌더 전 realize에서 컬렉션 render-enable 확인. Dist_<name> resolve(GSO 긴이름). render_blender_data.py(3rd gen)는 209 미배선(범위밖).
- commit 안 함. **Phase B1(4 아키텍처+5 sample_frame) 진행중.**

- **Phase B1+B2 [확인, 내 실행검증]**: `scripts/data_prep/blender/v2_pipeline.py`(신규, bpy 실import 0). B1=6함수 분리 + `sample_frame` 완성(쿼터 greedy, elev×V IPF joint, seed 결정성, 1000샘플 축분포=처방±0.002). B2=`solve_placement` 완성(순수 해석기하): Z=fx·W/(proj_size_ratio·W_img) 해상도소거·V-probe(count_in_frame_only 재구현)·가림순서(cargo→f_need→occluder 에셋먼저·d_occ=fx·√(bbox_cross·fill_ratio/A_target))·C1/C2(ray-AABB). **★용어확정: 투영크기=proj_size_ratio, 가림=f_target(별개 축)**. accept 82.2%(v_below14.6·d_occ_fail34.8·C1 38.2·C2 12.4·penetration0), RESAMPLE_MAX=30. 에이전트 자가교정: position_mode를 하드필터로 오취급→선호로 수정(resample_exhausted 68%→0). W_pallet=1.1m(canonical_bbox). 레거시 무손상·commit 안 함.
- **⏳ 사용자 입력 대기 3건(Phase C dry-run 전 반영)**: ①elevation A안(gen_trunc_addon 경계·frac, +60-80° 10% ×0.90) 승인? ②projected-size per-bin 비율(처방에 경계만·비율없음→uniform 0.20 가정) ③luma 4bin 비율(uniform 0.25 가정). 추정 안 하고 대기.

- **v2 규약 3 결정 확정 (사용자 2026-07-25)**: ①**elevation A안 수정채택**(trunc_addon 3-15°에 60%는 addon 세트용 base아님; addon_v1은 0-60 균등) — 7bin [0.5,3]0.08/[3,8]0.18/[8,15]0.20/[15,25]0.20/[25,40]0.16/[40,60]0.10/[60,80]0.08. **★잠정 — 평가셋 앙각(GT pose, 별도머신) 측정 후 재조정**(코드·문서 명시). ②**projected-size uniform 0.20 유지**(bin 경계 기하급수→bin균등=거리 log-uniform=스케일불변 학습에 맞음; CLI 제안 30/25/20/15/10 기각). ③**luma는 Layer1 쿼터 축에서 제거**(정정: luma=HDRI×노출×알베도×씬 결과라 직접샘플 불가; scene_preset night25%와 충돌) → Layer1은 scene_preset+**노출 EV U(-3.0,+0.2)** 샘플, Layer3서 luma_actual 측정→bin(<35/35-80/80-140/>140) **감사만(≥10% 커버 통과, 쿼터강제 없음)**. G5 luma 하한 게이트 유지.
- **B2 3 지시**: (1)C1 reject 38.2% → dry-run (b)에서 **원인 메시별 분해**(배경glTF/distractor/cargo/파렛트). (2)fill_ratio 극단(plastic_broom0.061·hand_truck0.117) d_occ 불안정 → **f_target≥0.20 bin은 fill_ratio≥0.25 에셋만 후보**(안떨어지면 임계 재제안). (3)cargo box fill_ratio=**1.0 해석적**(볼록직육면체, 회전0.85~1.0), 측정불가 플래그 해제.

- **Phase C dry-run 40k [확인, 내 재실행 84.9% 재현]**: 3결정+B2지시 반영 후 `v2_pipeline.py`. **예상 통과율 33947/40000=84.9%(≥70% OK), 어떤 cargo×f_target 셀도 50%↑ 없음 → 처방 조정 불필요.** 44.6s·bpy=NONE·render=NONE·seed결정성 True. 하네스 `scripts/data_prep/blender/audit_v2_dryrun.py`, 산출 `data/pallet/v2_dryrun_audit/`((a)~(f) png+report). 폐기 15.1%=C1 5.5(★distractor 57%>pallet27>cargo16, background 순수층 미모델 0)·d_occ_fail 4.4(★large 0.96/road0.84 vs indoor0.23/small0.19=대형밀집 지배)·v_below2.7·C2 2.2·penetration0.3. 셀 최대 elev[0-3]×[0.10-0.20]=0.38. fill≥0.25 효과 d_occ_fail -5.4%·accept+1.1pp(작음). **관찰(게이트 아님, 사용자 판단)**: near-pallet 강등 45.2%(밴드 0.70-0.90이 하드천장0.90에 근접) / d_occ_fail 근본개선=고-f_target서 large 가중↓ or bbox_cross 상한. 반영 변경 v2_pipeline.py만(elevation 7bin·exposure_ev·luma제거·fill≥0.25·cargo fill=1.0·C1메시추적), 레거시 무손상·commit 안 함. v2_domain_randomization.md에 elevation잠정/projsize/luma 섹션 추가.

- **Phase C iter-B 튜닝+재-dry-run [확인, report.md+플롯]**: 사용자 진단(측면오프셋 누락→large 멀어져야 함) 반영. **①accept-time quota**(sample_frame pure-read+Picks, 호출부가 accept시만 advance) → **accept 분포=처방 정확일치(전 축)**, attempted만 하드셀 왜곡=자기교정. **②측면오프셋(side{left34·right33·bottom30·center3}) 복원**(A_target만 겹침, depth+offset 동시풀이) → **★large 사용 0.3%→29.9%**(다양성 회복, size_class 균형: large29.9·medium26.9·road22.7·small14.6·indoor6.0). **③클리어런스 배치제약**(d_occ≥0.2+half-extent) → distractor-C1 2927→18. near-pallet 밴드 0.70→0.55·강등 45%→0%. 통과율 77.5%→81.6%(≥70, 인위상승 아님). ★CLI 제안(large 가중↓)은 안 함. 관찰: C2 1349→3382(lateral occluder 월드AABB>실루엣 보수적 과폐기=안전, 통과율 무해, 필요시 2D 중심픽셀 검사로 정합-후속). 산출 `data/pallet/v2_dryrun_audit/`(usage_size_class·a_accept_dist·reject_compare·side_dist png+report). 변경 v2_pipeline.py·audit_v2_dryrun.py·v2_domain_randomization.md, 레거시 무손상·bpy0·commit 안 함.
