# 2026-07-31

## data/pallet Stage 2-D1.2 — D11B / D11C 실제 이동 (D11 범위 종료)

작업은 2026-07-30 밤에 시작해 07-31 새벽에 끝났다. 직전 단계는
[2026-07-30.md](2026-07-30.md) 의 Stage 2-D1.1 이다.

산출물 전체: `reports/data_pallet_cleanup/stage2d12/`
최종 보고: `reports/data_pallet_cleanup/stage2d12/final_report.md` (45항목 순서 + 마감 수치)

### 왜 했나

Stage 2-D1.1 은 hash read 예산(전역 20 GiB) 때문에 **이동을 못 하고** 조사·근거만 남겼다.
D1.2 는 사용자가 예산을 D12B 36 GiB / D12C 34 GiB / 합 70 GiB 로 승인해 그 2 cohort 를
실제로 옮긴 단계다.

### 결과

```
cohort                  row  files     bytes            pre-hash    post-hash   failures
────────────────────────────────────────────────────────────────────────────────────────
D12B_REFERENCE_MOVE      4   92,429   17,334,010,020   16.14 GiB   16.14 GiB      0
D12C_PROVEN_NOAI_MOVE    4   39,620   15,588,789,193   14.52 GiB   14.52 GiB      0
────────────────────────────────────────────────────────────────────────────────────────
                         8  132,049   32,922,799,213   30.66 GiB   30.66 GiB      0
                                       (30.66 GiB)     total hash read 61.32 / 70 GiB
```

`hash-mode=all` · `workers=1` · same-volume rename · 삭제 0 · overwrite 0 · unhashed 0 ·
SHA256 mismatch 0 · `data/pallet` 파일수 **363,090 불변**.

이동 내역:

```
D1-003  assets/scenes/production/blender_scene/_sandbox_parking_lot_check.blend
        -> archive/legacy_scenes/snapshots/
D1-033  archive/train_palletobj_v3          -> archive/legacy_datasets/redistributable/
D1-038  archive/training_data               -> archive/legacy_datasets/noai_baked/
D1-053  archive/train_palletobj_v3_post_v1  -> archive/legacy_datasets/redistributable/
D1-041  archive/training_data_v4_split_GREYBUG -> archive/legacy_datasets/noai_baked/
D1-042  archive/training_data_v4_split_bg1bak  -> archive/legacy_datasets/noai_baked/
D1-043  archive/training_data_v4_emptywood     -> archive/legacy_datasets/noai_baked/
D1-049  archive/training_data_v4_pilotA        -> archive/legacy_datasets/noai_baked/
```

NoAI baked 8종이 `archive/legacy_datasets/noai_baked/` 한곳에 모였다.

### D12B — registry 전환 덕분에 실행 표면 무수정

D1.1 이 registry 키 4개를 신설하고 config·스크립트를 `registry:` 참조로 바꿔 뒀다.
그래서 D1.2 는 **`config/synthetic/pallet_paths.yaml` 의 값 4줄만** 바꿨다.

```
legacy_sandbox_parking_lot_scene        -> archive/legacy_scenes/snapshots/_sandbox_parking_lot_check.blend
legacy_train_palletobj_v3_root          -> archive/legacy_datasets/redistributable/train_palletobj_v3
legacy_training_data_root               -> archive/legacy_datasets/noai_baked/training_data
legacy_train_palletobj_v3_post_v1_root  -> archive/legacy_datasets/redistributable/train_palletobj_v3_post_v1
```

`--audit` ok=28 missing=0.

★ **live reference 를 다시 쟀다.** 처음에 D1.1 CSV 의 `current_runtime_test_refs` 를
그대로 복사했더니 4건 전부 LIVE_REF 문제로 잡혔다 — 그 값은 **registry 전환 이전**에 잰
것이라 전부 오탐이다. 지금 실행 표면을 다시 재서(registry 정본 yaml 과 resolver docstring 은
경로 소유자라 제외) 3건은 0 이 됐고, 1건(`generate_all.sh:42` 낡은 주석)은 **진짜라서**
갱신했다. 그대로 믿었으면 이동 자체를 못 했거나 진짜 stale 1건을 놓쳤다.

### D12C — PROVEN_NOAI, ledger B8 해소

D1.1 의 판정을 다시 믿지 않고 file_count/bytes 로 동일성 재확인(`identity_unchanged`
True 4/4). 라벨 JSON 13,122개(= 프레임 13,120) 전수 스캔 결과:

```
dataset                        프레임   NoAI 프레임      %     mtime
──────────────────────────────────────────────────────────────────────
training_data_v4_split_GREYBUG  5,000      3,286      65.7%   2026-06-17
training_data_v4_split_bg1bak   5,000      3,272      65.4%   2026-06-16
training_data_v4_emptywood      3,000      3,000     100.0%   2026-06-18
training_data_v4_pilotA           120         76      63.3%   2026-06-16
──────────────────────────────────────────────────────────────────────
합계                            13,120      9,634      73.4%
```

`_docs/dataset_license_ledger.md` 의 **B8 을 "미해결 MEDIUM" → "해소"** 로 바꾸고
근거 3단(적극적 사용 증거 / 자산 동일성 / 재-bake 이전 시점) · 분모 · 최종 경로 ·
`release_allowed=NO` · exclusion 이 gitignored 라는 주의를 본문에 기록했다.

도구가 `PROVEN_NOAI` 는 `noai_baked/` 외 목적지로 못 가게 막는다
(`/redistributable/` `/packages/` `/unidentified/` `/release/` `/partial/` 거부).

### ★ chain 은 자기 mapping 만 책임진다 — 이번에 배운 것

C2C 원장(`c2c_distractor_scene.jsonl`)은 `blender_scene/` 폴더를 옮긴 원장이고, 그 안의
파일을 D1.1 이 10개, D1.2 가 1개 다시 빼냈다. 재검증하면 11개가 MISSING 이다.

```
D11A chain 만 주면  -> failures 2   (D1.2 가 옮긴 1개 MISSING)
D12  chain 만 주면  -> failures 11  (D1.1 이 옮긴 10개 MISSING)
둘 다 주면          -> successor chain: 11 file(s) from 2 chain(s) / failures 0
```

그래서 `--successor-ledger-chain` 을 **반복 지정 가능**(`action="append"`)하게 고치고,
여러 chain 이 같은 prior key 를 중복 주장하면 exit 2 로 거부하게 했다. D1.1 이 만든
`stage2d11/c2c_successor_chain.json` 은 수정하지 않고 새 chain 파일을 추가했다.

prior ledger 소속은 "chain 이 안 만들어졌으니 없을 것"으로 넘기지 않고 8건 전부 직접
조회했다 — D1-003 만 소속(S2C2002), 나머지 7건 없음.

### 새 policy `stage2d12-final-moves`

기존 `stage2d11-residual-finalization` 은 D1.1 CSV 스키마(`scope` 열)를 기대해서
`KeyError: 'scope'` 가 났다. **기존 policy 를 고치지 않고** 새 policy 를 추가했다 —
기존 policy 를 고치면 이미 검증 끝난 원장의 재검증 의미가 바뀐다.

`_stage2d12_candidates()` 가 강제하는 것: scope SHA 결속 · cohort 분할 거부 ·
live ref 0 · PROVEN_NOAI 목적지 제한 · registry key 가 아직 source 를 가리킴 ·
`registry_final_value == destination` · prior ledger flag.

`scripts/data_prep/manage_pallet_data_layout.py` +241줄.

### exclusion — 검증기가 stale 4건을 잡았다

D12C 직후 첫 검사에서 `problems 4 (STALE_ENTRY)`. v4 파생 4종의 옛 경로가
`_DISTRIBUTION_EXCLUDE.txt:52-55` 에 남아 있었다. 새 경로로 갱신하고 재검사해
`entries 16 / problems 0 / leaks 0 / stale 0`.

주석도 "UNKNOWN_LICENSE 라 보수적 제외" → "PROVEN_NOAI 확정" 으로 갱신했다.
(처음 쓸 때 "라벨 13,122 프레임" 이라고 적었는데 13,122 는 **라벨 JSON 개수**이고
프레임은 13,120 이다 — 정정함.)

### ★ 참조 감사 범위를 넓혀 발견한 것

canonical 감사(이전 단계와 동일 범위 = `.gitignore` 준수)는 **fix_required 0 / 530 행**
으로 D1·D1.1 과 동일하다. 이번에 `--no-ignore` 로 **`data/pallet` 내부까지** 스캔해 봤다
— 이전 단계들은 이 범위를 한 번도 본 적이 없다(내부 파일 행 0 으로 교차 확인).

발견 11건 중 5건이 진짜였다: `data/pallet/assets/README.md` 의 오른쪽 열 제목이
`CURRENT 위치(2026-07-28 기준)` 인데 값이 `data/pallet/models_usd/` `distractors/`
`textures_wood/` `blender_scene/` 등 **지금 하나도 없는 경로**였다. 리터럴을 다시 적으면
같은 일이 반복되므로 **열 자체를 registry key 로 교체**했다.

남은 6건은 고치면 안 되는 것 — `_v2_pilot_2k/diagnosis/code/` 는 진단 시점 코드 스냅샷
(현역 정본은 `scripts/data_prep/blender/v2_pipeline.py`, 스냅샷을 import 하는 코드 0건),
`archive/` 아래 일회성 스크립트, NoAI USD provenance README. 분류기가 이들을
`CURRENT_RUNTIME` 으로 오분류하는 이유는 `_` 접두 판정을 basename 으로만 하고
`data/pallet/archive/` 규칙이 없어서다 — canonical 범위에서는 스캔되지 않아 안 드러났다.

### 구조 감사 depth 를 4로 넓혔다

D1.1 은 depth 1~3(233 entry)까지만 봤다. D1.2 의 목적지는 semantic 컨테이너의 **손자**
(`archive/legacy_datasets/noai_baked/training_data`)라 depth 3 까지면 이번에 옮긴 게
하나도 안 보인다 → depth 4 추가, **277 entry**.

파생 과정에서 실수 2개를 냈다가 잡았다:
- 분류 우선순위를 `moved_dest` 먼저로 바꾸려다 `pass`/`elif` 를 잘못 끼워 `plan` 분기가
  죽었고 `RESIDUAL_DATASET_DIR` 가 107 로 부풀었다 → 원래 순서로 복구.
- depth 4 의 배경 원본 ZIP 3개가 `UNEXPECTED_TOP_LEVEL_ZIP` 으로 잡혔다 — 이름 그대로
  depth 1 규칙인데 depth 4 에 적용된 것이고, 그 ZIP 은 Stage 2-C2 가 옮긴 제자리다
  → 원장 목록을 `MPL.PRIOR_LEDGERS` 전체로 넓히고 ZIP 규칙을 depth 1 한정으로 고쳤다.
  최종 UNKNOWN 0.

### 세 판정 — 서로 다른 질문이다

```
D12B_COMPLETE                             ✅
D12C_COMPLETE                             ✅
D11_SCOPE_COMPLETE                        ✅  D1.1 residual_scope 18행 전부 해소
                                              (D1D 10 = D1.1 D11A / BLOCKED_REFERENCE 4 = D12B
                                               / BLOCKED_UNKNOWN 4 = D12C)
FULL_DATA_PALLET_LAYOUT_POLICY_COMPLETE   ❌  권장 구조 밖 200개(5.47 GiB) 잔존
                                              depth1 65(4.27GiB) + archive depth2 135(1.20GiB)
                                              전부 Stage 2-A archive.csv 계획 있으나 executed=no
FULL_PHYSICAL_MINIMAL_TREE                ❌  빈 폴더 7 유지(삭제 금지) + 위 200개
```

성격 미상은 0 이다(UNKNOWN 0 · 미분류 0 · broken ref 0 · leak 0 · BLOCKED 0).
남은 것은 "뭔지 모르는 것"이 아니라 "아직 제자리로 안 옮긴 것"이다.

### 회귀 (전부 실제 실행)

```
unit 745 / integration 31 / golden 51        skip 0 fail 0
registry                                     ok=28 missing=0
exclusion                                    entries 16 / problems 0 / leaks 0 / stale 0
기존 원장 9종                                  failures 0
C2C (exact + expected-additions + chain x2)   failures 0 / 인정된 이관 11
신규 원장 D12B                                 sha256 92,429 / failures 0
신규 원장 D12C                                 sha256 39,620 / failures 0
5k FrameSpec                                 4,313 / 938f387d   (D1.1 과 동일)
5k proposals                                 4,439 / 3cd365ee / 12-12
Blender no-render                            abs 0 / missing 0 / node 0 / Dist_ 209
파일 수                                        363,090 -> 363,090
원장 멱등성                                    재검증 후 원장 dirty 0
보호 영역 SHA256 (blend/weight/package/dataset) 문제 0 (read 9.48 GiB)
```

`bytes +11,790` 의 출처를 전부 특정했다 — 자산·데이터셋은 1바이트도 안 바뀌었다.

```
assets/README.md            +621    구 경로 열 -> registry key 열
manifests/*.csv          +10,754    archive/path_map/assets 갱신
_DISTRIBUTION_EXCLUDE.txt   +415    D12C 경로 갱신 + 근거
```

### 변경 파일

```
_docs/dataset_license_ledger.md                    B8 해소 (+61)
config/synthetic/pallet_paths.yaml                 키 4개 값 전환
scripts/data_prep/manage_pallet_data_layout.py     stage2d12 policy + chain 반복 (+241)
scripts/data_prep/isaac_sim/generate_all.sh        낡은 주석 갱신
reports/data_pallet_cleanup/grouped_inventory.csv  재생성 (276 -> 277)
reports/data_pallet_cleanup/stage2d12/             신규 산출물 일체

(gitignored — diff 에 안 나옴)
data/pallet/_DISTRIBUTION_EXCLUDE.txt
data/pallet/assets/README.md
data/pallet/manifests/{archive,path_map,assets}.csv
```

### 하지 않은 것

삭제 0 · ZIP 삭제/수정/해제 0 · package 병합 0 · weight 이동 0 · isaac_assets 이동 0 ·
NoAI USD quarantine 이동 0 · active/rollback scene 이동 0 · D11A 재이동 0 ·
기존 원장 rewrite 0 · Blender 렌더 0 · 데이터 생성 0 · 모델 학습 0 · **commit 0 · push 0**.

### 다음 TODO

1. 사용자 승인 후 commit + push (branch `chore/data-pallet-stage2d12-final-moves`).
2. `FULL_DATA_PALLET_LAYOUT_POLICY_COMPLETE` 를 닫으려면 잔여 200개(5.47 GiB)를
   권장 구조로 옮기는 단계가 필요하다. 용량 이득은 없고 목적은 구조 정리다.
   Stage 2-A `archive.csv` 에 이미 계획이 있다(`executed=no` 202행).
3. 빈 폴더 7개 처리는 삭제 승인이 필요하다 — 지금은 다음 단계 분류의 목적지로 남겨 뒀다.
4. 참조 감사 분류기의 빈틈(`data/pallet/archive/` 를 아카이브로 인식 못 함,
   `_` 접두를 basename 으로만 판정)은 canonical 범위를 넓힐 때 함께 고쳐야 한다.
