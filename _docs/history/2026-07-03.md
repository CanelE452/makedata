# 2026-07-03

## train_palletobj_addon_v1 재생성 런처 정비 (재현성 확보)

사용자 요청: "이전에 만든 addon 데이터셋을 다시 그런 식으로 만들 수 있게 준비" — 처음부터 재설계 말고 기존 자산 기반 재구현.

### 조사 결과 (재현 경로 확정)
실제 생성 로그 `data/pallet/train_palletobj_addon_v1_gen.log` 상단에서 정확한 실행 근거 확인:
- blend: `data/pallet/blender_scene/_sandbox_palletobj_production.blend` (157MB)
- 드라이버: `scripts/data_prep/blender/run_mass_10k.py` → `gen_palletobj_scenarios.py`
- 파라미터: `--num_frames 6000 --out .../train_palletobj_addon_v1 --seed 7777` (start_idx 0, purge_every 50, chunk_size 1000 기본값)
- blender: `C:/Program Files/Blender Foundation/Blender 5.1/blender.exe`, EEVEE 16spp 640×480
- 결과: `[Mass10k] DONE: 6000/6000 attempts=6498 elapsed=3859s` (~64분)
- 의존물 전부 존재: HDRI 9종, pallet_full.obj, textures_floor 42, textures_wood 27, OCCLUDER_POOL 19 objs(blend 내장)

### 결정성 분석
- `run_mass_10k.py` L91 `random.seed(7777)` 한 번 → `sample_random_scenario` → `generate_frame` 순차 소비. 단일 `random` 스트림이라 같은 seed면 rejection 지점까지 동일 시퀀스. numpy는 수학용만(난수 아님).
- `camera_effects.py` L15 `apply(img_path, seed)`: `np.random.default_rng(int(seed)*2654435761 % 2**32)` — frame_idx 기반 프레임별 결정적. mask/keypoint 무관(RGB post only).
- 유일한 비결정: EEVEE/GPU 픽셀 미세차. 라벨·시나리오는 bit 재현.

### 추가한 것
- **`scripts/data_prep/blender/run_addon_v1.sh`** (신규): run_mass_10k.py 얇은 래퍼. 기본값 = addon_v1 정확 재현(seed 7777, 6000장). 기능:
  - preflight: blender/blend/script/obj/hdri/textures 존재 확인 후 실행(누락 시 abort).
  - 덮어쓰기 가드: out에 png 있고 start_idx 0 & --force 아니면 REFUSE(기존 6000장 보호). --start_idx로 resume, --out으로 variant(addon_v2), --force로 덮어쓰기.
  - 로그: `data/pallet/<name>_gen.log` (기존 네이밍 일치). 완료 후 png/mask/json 카운트 출력.
- 검증: 인자 없이 실행 → preflight 통과 + 가드 REFUSE(exit 1) 확인. `bash -n` syntax OK. (실제 64분 렌더는 미실행 — 비파괴 smoke test만.)

### ⚠️ 미해결 / 다음 (사용자 결정 필요)
- **재현 스크립트 5개가 git 미추적(`??`)**: gen_palletobj_scenarios.py, camera_effects.py, run_mass_10k.py, blender_math.py, floor_and_mask.py + 신규 run_addon_v1.sh. working tree에만 존재 → 유실 시 재현 불가. 사용자 OK 시 커밋 권장(CLAUDE.md: 명시 요청 전 커밋 금지).
- 재현 명령(수동): `bash scripts/data_prep/blender/run_addon_v1.sh --force` (또는 새 out으로 variant).

## trunc_addon_v1 파일럿 300장 — PASS (독립 검증)

저앙각+truncation 합성셋. gen_palletobj_scenarios.py 복사→`gen_trunc_addon.py`(원본 무수정=addon_v1 repro 보존) + `run_trunc_addon.py` 드라이버 + `audit_trunc_addon.py`. 위임: palletobj-blender-pipeline-techniques agent(파일럿까지). 출력 `data/pallet/trunc_addon_v1/`.

### 파일럿 감사 (내가 300장 독립 재집계로 확인, agent PASS 검증)
- elevation 실현: <3 9.7 / 3-8 29.7 / 8-15 29.3 / 15-25 16.3 / 25+ 15.0 % (처방 ±1pp, 3-15°=59%≈60%).
- V_geom: V4 15.0/V5 25.0/V6 30.0/V7 20.0/V8 10.0 % (목표 정확 — stratified 결정 배정 45/75/90/60/30).
- per-corner 레이블: keypoint_in_front_of_camera[9]/keypoint_inside_image[9](raw)/keypoint_visible[9]/V_geom/missing_corner_bitmask. 내가 bitmask=not(inside&infront) 재계산 → 300장 불일치 0, V_geom 불일치 0, camera-behind 0.
- unclamped 270/300(90%) 화면밖 raw 좌표 유지(clamp 없음). 예: 000005 id4 u=915.5>640, bitmask=153=코너{0,3,4,7} missing.
- rear(4-7) 저앙각<15°(n=206): in_front 100%, inside 79.4%, all-4-infront 100% = rear depth 관측(병목 정조준). rear inside(81-84%)>front(55-71%): lateral 클립이 front를 더 자르고 rear 보존(설계의도).
- 오버레이 6장 눈검증 통과(000103 정렬정확, 000005 rear5,6 생존, 000198 occlusion 4/8 게이트, worst-combo 000085/000025 depth near-coincident).
- reproj p99=0.05px는 agent값 채택(Blender 투영 독립재유도 안함, 내부정합 지표).

### 미해결/다음 (사용자 결정 대기)
- **6000 스케일업 승인 대기** (~1.8h). 명령: `blender -b _sandbox_palletobj_production.blend --python run_trunc_addon.py -- --num_frames 6000 --out .../trunc_addon_v1 --seed 20260703 --progress_every 50`. ⚠️ 같은 out이라 파일럿 300 덮어씀(새 dir 원하면 --out 변경).
- worst-combo 원거리 tail 최대 18.7m(~40px) 유지 vs 캡(~110px) 결정. 캡 시 gen_trunc_addon.py sample_trunc_scenario worst size_bin `[(40,64),(64,110)]`만 조정.
- agent 삽질 3건 해결: odd V(5,7) lateral 도달불가→vertical/diag+adaptive alpha / elevation-V marginal 충돌→elevation-primary planner / Blender --out 상대경로시 render(C:) vs PIL(E:) 불일치→절대경로 필수.

## ★ compute_perm_v4 camera-facing 0123 버그 확정 + 수정 (3d-expert)

사용자가 trunc_addon_v1 오버레이(000002/4/8) 눈검증 중 "camera 기준 0123이 oblique/저앙각서 안 맞는다" 지적. 6000 재생성 중단하고 3d-expert 위임 진단.

### 판정: 버그 (설계 오해 아님)
- 원인: `compute_perm_v4`(blender_math.py) step3 "top4 평행-edge split"이 직사각 팔레트에선 ±X쌍/±Y쌍 둘 다 |cos|=1(동률)이라 애매 → `parallel[0]`이 corner 입력순서+부동소수로 결정(카메라 무관). FRONT 후보가 한 축쌍 2면으로 줄고 수직 2옆면은 배제. docstring("normal이 카메라 가장 마주보는 side 면") 위반.
- 근거: (1) 000002 cam≈+X, +X면 facing 0.971 vs 배정된 -Y면 0.059 (2) 300프레임 전부 FRONT=±Y 고정(카메라 무관) (3) azimuth sweep에서 FRONT축 고정.
- 상위(나) quaternion→object 검증이 ±Z로 튄 건 내 스크립트 오류(3d-expert가 world프레임 재확인, 함수 front_cos와 소수점 일치로 파이프라인 검증).

### 수정 (blender_math.py compute_perm_v4, 라인 197-239 + docstring)
- 평행 split 제거 → **4개 옆면 전부 열거**(top_cyc 각도정렬→top_edges 4개) → normal-facing argmax=FRONT, +2=REAR. face_quad/pair_bot/step5(좌우)/step6/step7 유지. per-corner 레이블·clamp금지 무손.
- `facing_margin` 정의: 맞은편 차이 → **최상-차상(인접) 면 차이**(코너뷰 애매성 측정). 
- 검증: azimuth sweep 수정후 24/24 FRONT 4면 순환. 000002/4/8 FRONT -Y→+X. 오버레이 `data/pallet/_perm_v4_fix_check/000002_compare.png`(4/8도). connector-cross 0/288 유지.

### 파급 (공유 convention — 모든 생성기 오염)
- compute_perm_v4 공유: trunc_addon/addon_v1(게이트 없음, 수직az ~절반 오배정), v4/topview/preview(게이트 front_cos≥0.40∧margin≥0.60 있으나 ~45°코너 통과 → 2860중 548=19.2% 오배정, 수정후 0%).
- 기존 baked JSON 라벨은 함수 수정으로 안 바뀜 → **재생성해야 교정**. 하위호환 분기 불필요.
- 부수효과: blender_math.py 변경으로 run_addon_v1.sh 재현이 교정된 라벨을 냄(bit-동일 아님).
- `FACING_MARGIN_MIN` 재튜닝 필요(정의 변경, 스케일↓): gen_dataset_v4.py:95, gen_topview_test.py:76, gen_preview10.py:78 → ~0.15-0.30 권장(재생성시 sweep 확정).

### 미해결/다음 (사용자 결정)
- 재생성 범위: trunc_addon_v1(6000, 명백히 필요) / addon_v1·v3·v4(오염됐으나 범위 큼) 선택.
- 재생성 전 FACING_MARGIN_MIN 재튜닝.
- ⚠️ 6000 재생성 시 파일럿 300 보존 위해 깨끗한 dir 사용(직전 덮어쓰기 실수 반복 금지).
- scratchpad 재사용 스크립트: diag_perm_v4.py / overlay_perm_v4_fix.py / old_vs_new_gate.py.

### convention 수정 후 남은 항목 점검 (사용자 "또 고칠 것 없나" 질의)
- **(A) FACING_MARGIN_MIN 재보정 [확인]**: grep로 소비처 정확히 3개 확정 — gen_dataset_v4.py:95 / gen_topview_test.py:76 / gen_preview10.py:78 (전부 0.60). margin 정의 맞은편→인접으로 스케일↓, 0.60 과도 → v4류 재생성 전 ~0.15-0.30 재튜닝 필수. front_cos 게이트(0.40)는 정의 불변이라 유지 OK. addon_v1(gen_palletobj_scenarios.py:433)·trunc_addon(gen_trunc_addon.py:513)은 return_margin 미사용=게이트 없음.
- **(B) 감사 맹점 [확인]**: audit_trunc_addon.py는 reproj+per-corner in_front/inside만 검증, **permutation(0123 배정) 검증 없음** = 이 버그가 PASS 통과한 근본 원인. camera-facing 정합 체크(FRONT=4옆면 facing argmax) 추가 필요. 안 고치면 다음 파일럿도 permutation 오류 못 잡음.
- **(C) git 미추적 [확인]**: blender_math.py(수정본)+gen_trunc_addon/run_trunc_addon/audit_trunc_addon 전부 `??`. 수정·신규 파이프라인 버전관리 안 됨 → 유실 위험. 커밋 필요(사용자 OK 후).
- **(D) trunc_addon 게이트 없음 [추정]**: true edge-on(front_cos≈0)서 FRONT 좌우(0/1) 배정 불안정 가능. 저앙각 worst-case 의도라 일부 grazing OK나 극단 edge-on은 mild front_cos floor 고려(정량 미측정).
- **(E) trunc_addon_v1 dir 혼합 [확인]**: 000000-104 새seed+105-299 원본, 재생성 시 자동 해결.

### B) 감사 강화 (audit_trunc_addon.py) + C) 커밋 — 완료
- **B [확인]**: audit_trunc_addon.py에 "CONVENTION PERMUTATION" 체크 추가 — compute_perm_v4와 독립적으로 4옆면(front{0,1,2,3}/rear{4,5,6,7}/left{0,3,4,7}/right{1,2,5,6}, 000002 좌표로 adjacency 검증) facing 재계산해 FRONT(0-3)==argmax인지 검사(eps=0.05). VERDICT에 추가.
  - 검증: 현재 버그 라벨 데이터에 실행 → reproj p99<1px=True(옛 감사 통과)인데 **0123==camera max-facing 154/300(51.3%) PASS=False**로 버그 포착. mislabeled 000002 front=0.06 vs right=0.97(손계산 일치), 틀린 프레임의 진짜 정면 {right:69,left:77}=배제된 수직면. 재생성(수정convention) 후 목표 ~300/300.
- **C [확인]**: 브랜치 `fix/perm-v4-camera-facing-0123` 생성(main이 default라 정책상 분기), 4파일 개별 스테이징 커밋 `ca78435`(blender_math.py 수정본 + gen_trunc_addon/run_trunc_addon/audit_trunc_addon, 1933 insertions). push 안 함(요청 시).
  - ⚠️ gen_trunc_addon.py가 import하는 floor_and_mask.py, camera_effects.py(둘다 untracked)는 이 커밋에 미포함 → 완전 실행엔 추가 커밋 필요(+ .blend/HDRI 등 데이터 자산은 별도).

### A) FACING_MARGIN 재튜닝 + D) trunc 게이트 측정 — 완료 (3d-expert 이어서)
- **A [확인]**: FACING_MARGIN_MIN 0.60→**0.15**. 근거=az/elev 77,760뷰 sweep, FRONT-unstable(±3° 뷰변화에 id0 physical corner flip) 뷰의 margin 전부 ≤0.080 → 0.15는 leak 0 + ~2배 buffer, 45°코너당 ±5.8° 밴드만 reject(passRate 83.5%, 옛 0.60은 32.9%로 2/3 버림). FRONT_FACING_COS_MIN(0.40)은 정의 불변이라 유지. 수정: gen_dataset_v4.py:102 / gen_topview_test.py:80 / gen_preview10.py:83 + 주석. 커밋 ab999bb.
- **D [확인]**: front_cos 게이트 **불필요, 코드 무변경**. 측정(trunc_addon_v1 300장, FIXED함수 front_cos 재계산): front_cos min=0.345(FIXED가 항상 best-facing 선택이라 안 작아짐), <0.40은 4장뿐 그중 불안정 0. 실제 불안정(id0 flip 12장=4%)은 전부 front_cos≥0.40=45°코너 애매성(front_cos 무관, facing_margin 담당). floor 추가시 0장 걸러 효과 0 → over-engineering. 교훈: grazing(front_cos↓)≠corner-ambiguity(facing_margin↓), 게이트 임계는 섭동실험으로 측정.
- **C 마무리 [확인]**: gen_trunc_addon 의존 floor_and_mask.py+camera_effects.py 커밋 04ae521. 남은 미추적은 _접두 일회성 진단뿐(불필요).

### 이번 세션 코드 fix 총정리 (브랜치 fix/perm-v4-camera-facing-0123)
- ca78435: compute_perm_v4 버그수정(4옆면 열거) + trunc_addon 파이프라인 + audit permutation 체크
- ab999bb: FACING_MARGIN_MIN 0.60→0.15
- 04ae521: 공유 모듈(floor_and_mask, camera_effects)
- push 안 함. **유일한 미결 = 재생성 범위 결정**(trunc_addon_v1만 vs +addon_v1/v3/v4). 재생성 시 수정 convention+0.15 margin 자동 반영.

### 파일럿 재생성 (수정 convention) — PASS
`data/pallet/trunc_addon_v1_pilot/` 300장, run_trunc_addon.py fixed blender_math, seed 20260703, 308s(1065 attempts). 기존 버그 dir(trunc_addon_v1) 안 건드리고 별도 dir(덮어쓰기 실수 방지).
- **★CONVENTION PERMUTATION 300/300 (100%)** = 0123==camera max-facing (버그때 154/300 → 수정 후 100%, 강화 audit로 확인).
- reproj p99<1px True / elevation <3:10 3-8:29 8-15:31 15-25:15 25+:15% (3-15°=60%,<15°=70%) / V 15/25/30/20/10 정확 / per-corner bitmask·V_geom 불일치 0, camera-behind 0 / unclamp 90% / rear<15° in_front 100%(210f). 내 독립 재집계도 audit와 완전 일치.
- 큐레이트 6장(audit_eye) 사용자 전달(저앙각/최악조합서 0123 카메라정면 배정 확인).
- **다음: 6000 스케일업 승인 대기**(통과했으니). 출력=trunc_addon_v1(기존 버그 data clear 후) 예정, seed 20260703, ~1.8h.

### 6000 최종 생성 (수정 convention) — 완료·검증
`data/pallet/trunc_addon_v1/` 6000장(기존 버그 dir clear 후 재생성). run_trunc_addon.py fixed blender_math, seed 20260703, 6199s(~1.72h, 21350 attempts, 에러 0). png/json/mask 각 6000.
- **0123==camera max-facing 6000/6000 (100%)** (강화 audit). reproj p99<1px True.
- elevation <3:9.7 3-8:29.9 8-15:29.9 15-25:15.4 25+:15.1% (3-15°=59.9%≈60%, <15°=69.6%). V 15.0/24.9/30.0/20.0/10.1%. per-corner bitmask·V_geom 불일치 0, camera-behind 0, unclamp 90%, rear<15° in_front 100%(4174f). **내 독립 전수 재집계도 audit와 완전 일치.**
- 보존: trunc_addon_v1_pilot(파일럿), _perm_v4_fix_check(before/after), 브랜치 3커밋.
- **trunc_addon_v1 태스크 완료.** 남은 선택: addon_v1/v3/v4 재생성(오염됨, 미결), 브랜치 push(미실행), v4류 FACING_MARGIN 0.15는 재생성 시 sweep 재확인 권장.
