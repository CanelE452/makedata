# 작업 기록

## 개요

팔레트 6D 포즈 추정 프로젝트의 주요 작업 이력. 최신순 정렬.

- 2026-07-27: Phase 9D usable 50-frame continuous EDA의 17개 figure(PNG 전수 확인)를 [결과 해석 문서](../experiments/v2_smoke50_continuous_eda_results.md)로 정리했다. 표본 50·usable 선택 편향을 전제로 못 박고, figure 10/11/12가 tautology(base rate 1.000, LOO Brier ~1e-32, 신뢰 격자점 0~2/200)로 무정보인 이유를 별도 섹션으로 설명했다. 부수 확인: camera distance q50 3.44 vs 3.180은 분위수 규약(higher vs linear) 차이, fig 14의 0.32 계단은 anchor fx 605.9065 16장 점질량, fig 08 잔차 전부 양수(계통 결함 B5), V_vis=4가 17/50(G1 경계). 상세 [2026-07-27.md](2026-07-27.md).
- 2026-07-27: Blender v2 scene logic 500-record EDA의 22개 chart, 500-frame manual audit, contact sheet, 표 결과를 [결과 해석 문서](../experiments/v2_scene_logic_500_eda_results.md)로 정리했다. proposal/rendered 분모, controlled error 이중 분모, automated audit 493 vs manual clean 286을 구분하고, chart 16-18 grouping 결함을 발견해 교정표와 재생성 조건을 기록했다. 상세 [2026-07-27.md](2026-07-27.md).
- 2026-07-26: Blender v2 constrained scene assembly를 additive opt-in으로 구현하고 exact 20-frame smoke 및 500-record/435-render 진단을 검증했다. fatal defect 0, rendered 조건 all-pass 83.68%; controlled delivery 58%와 tiny target 때문에 production default는 legacy로 유지한다. 최종 요약은 [2026-07-26.md](2026-07-26.md), 133개 보존 실행의 전체 시행착오는 [2026-07-26-v2-attempt-log.md](2026-07-26-v2-attempt-log.md)에 기록했다.
- 2026-07-25: v2 규약 구현(B3 realize/measure/G1~5/규약 realize측 + D 캘리브200 + G5 dark폐기버그 visible-luma 수정) 후 파일럿 2k 렌더·전축감사(perm/night/12kp PASS, 야간 G5 22→10%)·전수오버레이로 ★마젠타 배경오염 45장 all_pass 발견·전 DR축 분포EDA(처방완벽 max|Δ|≤0.05pp·실측 f_actual+27.6pp·occluder center 2%·all_pass 40%)·진단패키지(diagnosis/pilot_frames.csv 2480행+solve_reject480 재현+code+온보딩md, ★f_cargo가 배경 벽/선반 가림 오집계 45.4%·center occluder d_occ 1차+C2 62% 드롭·통과율40% 주범=G1 코너가림)·온보딩md(blender_mcp_onboarding.md 40실패). launcher 무한루프 OOM 사고(복구·데이터무결). 40k 미승인·commit 안 함. 상세 [2026-07-25.md](2026-07-25.md).
- 2026-07-23: E-단면 12kp(efront_kp12) 스킴 측정→결정→구현. 4팔레트×앞면2 개구부 실측(P2 long=닫힌개구부0=4-way아님, P1 short=bottom_open, ±면대칭→축쌍2). efront_kp12.py 신규(intrinsic 비율×per-frame dims, kp12_valid=물리축 기준). gen 2개 compute_annotation_v4에 additive 배선(스모크 EXIT0·기존키 불변·outer4 0.0px·1만장 재생성X). 부수: scene_noemit.usd(emissive 세척본), 4팔레트 카탈로그 렌더.

- 2026-06-16: gen_dataset_v4.py + run_dataset_v4.sh 신규(camera-facing 0123 대규모 생성, CLI/resume/chunk재시작). 파일럿 120장 라벨 전수통과(conn0/front_near/simple-quad 120/120, 마젠타0), elev15~58·scene1/3·ratio42%·azim360 분산. raycast 86% reject=병목, 15.4s/frame → 5000장 ~21.6h. 저장 data/pallet/training_data_v4/.

---

- 2026-06-15: preview10 grazing 배제 게이트 추가 — FRONT 면 정면도 front_cos>=0.40(edge-on 차단) + facing_margin 0.30→0.60. compute_perm_v4가 front_cos 반환. 재생성 10/10 PASS(0123 직사각). → `_docs/history/2026-06-15.md`
- 2026-06-15: compute_perm_v4 (카메라 동적 0123 keypoint ID) + 비등방 비율 랜덤(40%/±12%) 구현, scene_1/2/3 10샘플 프리뷰 생성·검증 (전수 PASS). → `_docs/history/2026-06-15.md`

## 2026-04

### 실내 환경 합성 데이터 테스트 (04-07)
- 실제 추론 환경(연구실)과 합성 데이터 간 domain gap 분석, Blender에 실내 방 환경 구축 + 10프레임 테스트 렌더링
- 상세: [2026-04-07.md](2026-04-07.md)

### Blender 합성 데이터 대량 생성 v65~v70 (04-06)
- 탑다운 뷰/엣지크롭/어두운 색상/빈 팔레트 추가, 총 ~8100프레임 생성
- 상세: [2026-04-06.md](2026-04-06.md)

---

## 2026-03

### Blender 렌더링 파이프라인 구축 + Cuboid 정합 (03-27)
- 상세: [2026-03-27.md](2026-03-27.md)

### Blender 합성 데이터 씬 구성 (03-25)
- 상세: [2026-03-25.md](2026-03-25.md)

### config 통합 및 TensorBoard 설정
- `config/default.yaml` 생성 — 모든 하이퍼파라미터 단일 소스로 통합
- `train_dope.sh`를 `default.yaml`에서 값을 읽도록 리팩토링
- `scripts/launch_tensorboard.py` 추가 — 실험별 loss 비교, 요약 출력
- TensorBoard 설치 (`pallet-pose` conda env)
- scripts/ 하위 디렉토리에 CLAUDE.md 추가 (data_prep, dope, self_training)

### docs 구조 재편
- `_docs/` 하위를 preprocessing, method, experiments, survey, history로 분리
- 키포인트 정의 문서 복원 (`preprocessing/keypoint_definition.md`)
- 합성 데이터 파이프라인 문서 추가 (`preprocessing/data_pipeline.md`)

### 상태줄 최적화
- `statusline.sh` Windows 최적화: jq 8회 → 1회 호출, 12초 → 0.15초
- ccusage 직접 호출 제거 (Claude Code 자식 프로세스 대기 문제) → JSON 내장 비용 데이터 사용

---

## 2026-02 ~ 2026-03 (합성 데이터 v11)

### 렌더링 파이프라인 안정화
- Isaac Sim 4.5.0 DLL 충돌 해결: `CUDA_MODULE_LOADING=LAZY`
- 200프레임/배치 재시작 전략 (메모리 누수 대응)
- DLSS 완전 비활성화: `anti_aliasing=0` + `/rtx/post/dlss/enabled=False`

### Domain Randomization 개선 (iter1 → iter13)
- **iter3-5**: 팔레트 색상 override — `diffuse_texture` disconnect + opacity/metallic 고정
- **iter4**: Distractor 색상 — per-distractor material 생성 (`stage.Traverse` 방식 폐기)
- **iter13**: 바닥/벽 텍스처 — USD API `Sdf.AssetPath` 직접 변경 (Replicator API 불가 확인)
- **v11**: 조명 상한 하향 (DomeLight 5000→3500, Main 400K→300K), brightness skip 240으로 완화

### 키포인트 Convention 확립 (Y=UP, v2)
- 메시 노멀 분석 방식 폐기 (팔레트에서 불안정)
- Canonical bbox 방식 도입: `R_canonical = R_yz_swap @ euler(base_rot)`
- ORIENTATION_OVERRIDES 4개 모델 전수 검증 완료
- 검증 스크립트: `verify_keypoints.py`, `DIAGNOSE_MODELS=1` 환경변수 진단 모드

### Nucleus Props 대체
- Isaac Sim 4.5에서 `Simple_Warehouse/Props/` 에셋 없음 확인
- Enhanced primitive fallback: cube 60% + cylinder 20% + cone 10% + sphere 10%

---

## 2026-01 ~ 2026-02 (DOPE 학습)

### Pretrain (pallet_category)
- 합성 데이터 ~2,000장으로 60 epoch 학습
- sigma=4.0 설정 (sigma<1은 gradient vanishing)
- 최종 loss: belief=0.043, affinity=0.004

### Fine-tune (pallet_v11)
- pretrain weight에서 91 epoch 추가 학습 (lr=5e-5)
- 최종 loss: total=0.044 (epoch 117)

### 평가 체계 구축
- `evaluate_on_val.py`: PCK@3/5/10px + PnP reproj error + ADD + 5cm-5°
- `visualize_inference.py`: belief heatmap + keypoint + cuboid wireframe
- `visualize_pretrain.py`: 종합 시각화 (belief + cuboid + PnP yaw/pitch/roll)

---

## 2025-12 ~ 2026-01 (프로젝트 초기)

### 아키텍처 결정
- DOPE (keypoint-based) 선택 — 팔레트는 비대칭 직육면체로 keypoint 방식에 적합
- Depth-free (RGB only) 접근 — 산업 현장 카메라 호환성
- 3단계 파이프라인 설계: 합성데이터 → Geometric Filter → Self-Training

### 환경 구축
- conda env `pallet-pose` (PyTorch 2.10 + CUDA 12.6)
- Isaac Sim 4.5.0 standalone 실행 환경
- Docker 실시간 추론 환경 (RealSense D435i)
- Deep_Object_Pose 서브모듈 통합 + Windows 호환 패치

### Self-Training 모듈 개발
- `scripts/self_training/` 5개 모듈 구현
- Geometric Filter 3단계 설계 (Augmentation Consistency + Edge Consistency + Pallet Ratio)
- PnP solver: EPnP + RANSAC wrapper
- FixMatch augmentation: photometric-only (좌표 불변)
- 2026-06-15 preview10 수정: FRONT(0123)=카메라 near 면(평면 팔레트 2D면적 far오판→3D거리), 배경 magenta 제거(parking_lot/broken HDRI 제외+Cycles), visibility gate(area>=4.5%+base/측면 raycast). 10/10 검증.
- 2026-06-15 compute_perm_v4 FRONT 판정 normal-facing 교체: 면 중심 3D 거리 → dot(outward_normal, cam-center) max. top-down 평면 팔레트 near 흔들림 해결(거리차 0.04~0.11m→facing margin 0.46~0.87). preview10 10/10 PASS, connector_cross=0, magenta 0.
- 2026-06-16: gen_dataset_v4 배경 다양화(Polyhaven HDRI 7종, magenta 0 검증) + raycast 가림완화(0.55→0.30, partial-occ 84%, x3 속도, GT reproj 0.000px). 재파일럿 80/80 통과. 5000장 ~6.9h.
- 2026-06-16: 팔레트 머티리얼 단색→Polyhaven 실사 나무 결 텍스처(albedo+normal+rough) 전환, 밝기DR=albedo×multiply tint. 10장+클로즈업 검증(grain 0.11~0.19, conn0/front_near/magenta0 전수). config만 교체, USD 무수정.
- 2026-06-16: scene_3(Pallet_3) deck "청록/회색" 진단 — 머티리얼/UV/노드 버그 아님(308메시 전부 UV+텍스처 정상, 실제 픽셀 cool_frac=0.00 warm). 원인=faded_gray 변종(brown_planks_03 중립회색)이 cool HDRI에서 청록으로 읽힘+녹색 오버레이 착시. 수정=config 1줄 faded_gray→aged_brown(웜). _mat_test10d 10장 scene_3 R-B+38~+58 teal0 conn0 검증. USD/코드 무수정 라벨무영향.
- 2026-06-16: gen_dataset_v4 출력 구조를 test_palletobj_r3(flat)로 전환 — png+json 폴더 루트 colocated, overlay/{6d}.png 전수(every1), stats→out_root/logs, _crop/_stats subdir 제거. resume=폴더 루트 png 카운트. 후처리 _restructure_to_r3.py(재렌더 없이 train_batch_000/001 변환, 002 삭제). 소량 검증(새 구조+SKIP+부분 이어감 png==json==overlay) 통과.
- 2026-06-16: 목재 팔레트(scene_2/3) 나무 스킨 다양화 — Polyhaven 나무 판자 6종 추가(textures_wood 9 base/27 png), config wood 그룹 6→11 variant(밝은pine~tan~cabin~dark_knot~grey~reddish, uv_scale 0.85~1.15, weight합1.0). 코드무변경 config-only(plastic/floor 불변). 카탈로그 시트 11장+랜덤적용 8장 검증(살짝씩다른 결/색/톤·자연접지·마젠타0·conn0/front_near 전수) → data/pallet/_wood_skin_compare/. split 미변경.
- 2026-06-16: 바닥 텍스처 항상 회색 버그 수정 — floor_texture 라벨 무관 회색. 원인=randomize_background가 매프레임 BG_industrial 재노출→자체 `Floor*_Asphalt_0` 20장(z≈0)이 FloorRandPlane(z=-6mm) 가림(raycast plane 0.7%↔asphalt 93%, 사용자 가설 CONFIRMED). 수정=blender_config FLOOR_BG_GROUND_HIDE + randomizers `_hide_bg_ground()`(매프레임, randomize_floor서 호출). 검증 plane 93.7%, 6종 RGB stddev R25/G24/B36(red_brick/tile_white/wood 라벨일치) → data/pallet/_floor_fix_test/_sheet.png. 접지/raycast/라벨 무영향, USD/씬 무수정.
- 2026-06-17: emptywood 보조셋(목재 scene_2/3 + 빈 데크 + distractor 유지) 설정 검증 — gen_dataset_v4.py EMPTYWOOD_WOOD_ONLY/NO_CARGO env 플래그(기존 구현) 동작 확인, 15장 smoke pass 15/15(scene_1=0,connX=0,front_near=True,데크 cargo 없음,distractor 유지), 풀 3000 커맨드 확정.
- 2026-07-03: train_palletobj_addon_v1(D435i 정합 6000장) 재생성 런처 `scripts/data_prep/blender/run_addon_v1.sh` 신규 — gen 로그에서 실제 명령 복원(run_mass_10k.py, seed 7777, 6000장, _sandbox_palletobj_production.blend, Blender 5.1 EEVEE). preflight 의존물 확인 + 기존셋 덮어쓰기 가드(--force/--start_idx/--out variant). 결정성 확인(random 단일 시드→시나리오 bit재현, camera_effects frame_idx 결정적). ⚠️ 재현 스크립트 5개 git 미추적 상태.
- 2026-07-26: makedata 저장소 분리 push — FoundationPose fork(NVlabs origin, 46커밋/120.8MB, assets mp4·jpg가 과거 blob에 상주)에서 orphan `main`으로 새 히스토리 시작(커밋 4819fb6, 194 entries/2.49MB). 기존 main→`legacy/foundationpose-main` rename으로 로컬 보존, remote는 origin=CanelE452/makedata + upstream=NVlabs/FoundationPose. `git rm --cached`가 orphan에서 거부→`git read-tree --empty` + `git add --pathspec-from-file`로 실존 192개만 정밀 스테이징. .gitignore에 이미지/영상/모델/3D 확장자 차단 추가(추가 전후 후보 대조 → 누락 0). Deep_Object_Pose는 서브모듈(035f8e7=NVlabs master tip), 로컬 수정 62줄은 `scripts/dope/deep_object_pose_local.patch`로 보존. 원격 검증: ls-tree 194, 이미지·영상·모델 매칭 0건.
- 2026-07-26: makedata 에서 Deep_Object_Pose 서브모듈 제거 — 저장소 참조만 해제하고 로컬 폴더(로컬 수정 62줄 포함)는 유지. `.git` 이 디렉토리(미흡수, .git/modules 없음)임을 확인한 뒤 `git rm --cached` + `.gitmodules` 제거 + `git config --remove-section submodule.*`. `deinit -f` 는 서브모듈 워킹트리를 삭제하므로 미사용. `.gitignore` 에 `Deep_Object_Pose/` 추가(clone·patch 적용법 주석 병기). `scripts/dope/deep_object_pose_local.patch` 는 복원용으로 유지.
- 2026-07-27: v2 개편 Phase 4 — PnP eligibility 감사기 `scripts/data_prep/blender/audit_pnp_eligibility.py` 신규(bpy-free). belief map stride 8px(원본 기준, DOPE 로더 crop이 픽셀 등가라 리스케일 없음) → threshold 후보 16/24/32px, visible-kp bbox min side 기준. 평가 코드와 동일한 solvePnPRansac+EPNP(8.0px/100iter)로 exact-GT PnP + sigma=1/2/3px 가우시안 keypoint 섭동 200회 Monte-Carlo(435프레임 실행). 3D 점은 canonical object frame이 아니라 라벨 자신의 world cuboid를 centroid로 옮겨 사용(camera_dynamic_0123_v4 perm이 프레임마다 달라 canonical은 최대 426px 어긋남, 실측). 결과: 1~8 cell 스윕에서 knee 없음 + 최엄격 후보도 sigma=2px에서 29.3% 5cm-5도 실패 → **최종 threshold 확정 불가(근거 부족)**로 보고, manifest에 physical_valid/gate_valid/pnp_eligible_candidate_2·3·4cell/tiny_warning/pnp_stress만 추가(프레임 삭제 0). 산출물 reports/v2_revision/pnp_threshold_study.{csv,md} + pnp_stability_continuous.pdf + pnp_eligibility_manifest.{csv,json}. 테스트 211 passed(신규 41).
- 2026-07-27: v2 개편 Phase 5 — mask 무결성 감사를 면적 단조성에서 픽셀 단위로 승격(`audit_v2_scene_logic.py` 확장, `mask_area()`는 Phase 4 호환 위해 보존). strict decode(verify+load, LOAD_TRUNCATED_IMAGES=False 고정) + 바이트 sha256 + content sha256(전경 packbits) + `M4⊆M3⊆M2⊆M1⊆M0` boolean 비교 + frame내/frame간 duplicate 분리 + projected-cuboid convex hull 정렬(monotone chain + Sutherland-Hodgman 클립, numpy/PIL만) + RGB↔mask 해상도 일치. 실측이 설계를 바꾼 3건: ①all-black mask는 sub-threshold noise 때문에 sha256이 전부 달라 content 기준 그룹핑 없이는 empty-target duplicate가 0건 검출(→`__all_black__` 키) ②같은 실루엣도 바이트가 달라 content 해시 필요(435프레임 within-frame 그룹 617개 중 byte-exact 3개) ③"occluder 놨는데 mask 동일"은 결함이 아님(f_context==0이면 정상) → 4상태 판정으로 false-positive 4건 제거. 435+20프레임 실행: inclusion 위반 0, stale duplicate 0, empty M0 4(48/321/453/478)가 단일 empty_target_defect 그룹, hull IoU 중앙값 0.970. 산출물 reports/v2_revision/mask_hashes.csv + mask_duplicate_groups.json + mask_pixel_inclusion_failures.csv + mask_integrity_source_masks.png. 테스트 251 passed(신규 40).
- 2026-07-27: v2 개편 Phase 6 — 상세 overlay 복원 `scripts/data_prep/blender/overlay_v2_detailed.py` 신규(bpy-free, PIL+numpy만). 현재 audit overlay(cuboid+centroid+헤더 1줄)에 gen_trunc_addon의 pose axis/카메라 거리·높이/elevation/lens·HFOV/가시율/pitch·yaw·roll/quaternion/info panel/범례를 복원하고, Phase 1~5 신규 지표(dist>limit violation, ground continuity, noise tier·sigma·final luma, pnp eligibility 2/3/4cell·tiny·stress, mask pixel inclusion·hull IoU, M0/M4 컨투어)를 추가. 핵심 설계는 **absent/null/present(0) 3상태 분리** — 구 렌더에 없는 필드를 조용히 0으로 채우지 않고 `N/A (field absent)`로 명시(falsy-0 버그 방지). pose axis는 pose_transform이 object→CAMERA임을 실측 확인(centroid 투영 정확 일치, cuboid 재현 1e-14px) 후 K@(R·0.5m+t)로 투영. 500프레임 + smoke20 전수 overlay + contact sheet 44장을 `<ds>/eda_phase6/`에 생성(기존 eda/eda_phase5 overlay는 guard로 보호). 육안 검증 11장 + 시트 2장: 컨투어·cuboid·축 정합 OK, 텍스트 겹침 0. 발견: gate PASS인데 94m/1015m 거리·M0 13px인 프레임이 빨간 VIOLATION으로 즉시 식별됨. 테스트 297 passed(신규 46).
- 2026-07-27: v2 개편 Phase 7 — `run_v2_scene_logic.py`에 `--completion-mode {records,usable}` 추가 (기본 records = 기존 20/500 proposal 동작 그대로). usable 모드는 usable N장을 정확히 채울 때까지 proposal을 계속 뽑고(`iter_proposals` 제너레이터, generate_accepted와 동일 draw·accept-time quota 규칙), 배달 id는 0..N-1 연속, 각 record에 proposal_index/attempt_seed 기록. usable 조건 19개를 **각각 독립 계산 후 AND**(`usable_conditions`, None=미측정은 절대 통과 아님): physical(Phase1 거리<=10m·Phase2 ground continuity·collision·support·clearance·corrupt·M0 non-empty) + magenta + Phase5 픽셀 포함관계·cross-frame stale mask + G1~G5(Phase3 final luma). 모든 reject를 `records_rejected.jsonl`에 보존(신설; stage=solve/mode_filter/render, 사유 `solve_reject:C1`·`gate_fail:G5`·`usable_reject:*`), reject된 시도의 이미지는 삭제. PnP threshold는 Phase 4에서 미확정이므로 조건에 넣지 않고 manifest에 SIZE 축만 `pnp_size_eligible_2/3/4cell`로 기록(cv2가 Blender에 없어 solve 축은 audit_pnp_eligibility.py 몫), README/manifest에 **"final training-ready 아님"** 명시. 실측 usable 10장: proposal 21·render 14(71% 통과)·324s, reject 사유는 G5 1건뿐이고 실제 병목은 controlled-occlusion의 explicit occluder realize 실패(6/8). 배달 10장 중 1장이 9.77m·11px(tiny, 2cell 미달)로 Phase 4 threshold 필요성 재확인. records 모드 회귀: 5프레임 비교에서 record 146필드 diff 0·RGB 바이트 동일·mask 픽셀 동일(label은 stage_runtime_s만 상이). 테스트 323 passed(신규 26).
- 2026-07-27: v2 개편 Phase 8 — 논문용 continuous EDA `scripts/data_prep/blender/analyze_v2_continuous.py` 신규(bpy-free, matplotlib+numpy만) + 기존 `analyze_v2_scene_logic.py`의 falsy-0 버그 15곳 회귀 수정. 수정은 `group_label()`(None/공백만 missing)과 `bin_or_numeric_fallback()`(bin 존재 판정을 `is not None`으로) 두 헬퍼로 통일 — 실측 효과: Fig18 all_pass by cargo_on이 `(missing) 285장 0.6982 / True 215장 0.7674`에서 `(missing) 65 / False 220장 0.9045 / True 215장 0.7674`로 바뀌어 **cargo=off가 나쁘다는 결론이 뒤집혔고**(0.698<0.767 → 0.905>0.767), Fig17은 bin0 88장이 다른 binning 체계의 가짜 bin 2개(45+43)로 분열돼 있던 것이 단일 bin(0.9091)으로 복원, Fig16 bin0 38장·azimuth_bin 0 36장·V_vis 0 1장도 missing에서 분리(이들 변수의 실제 missing은 0건). 신규 모듈은 ECDF 주 + bandwidth 명시 KDE 보조(bounded는 reflection 보정, domain 밖 밀도 0), azimuth는 von Mises circular KDE(kappa는 LOO 우도 CV; 실측 |f(0)-f(360)|=2.78e-17, 이음매 step/인근 median step=0.9978), zero-inflated는 P(X=0) 점질량과 X>0 조건부 분리(f_total P0=0.434 등), pass-probability는 순수 numpy Nadaraya-Watson + LOO Brier bandwidth + seed 1000·1000 bootstrap pointwise 95% CI + Kish n_eff<20 점선, 이산변수 42개는 `DISCRETE_FIELDS`에 등록해 KDE 호출 시 `DiscreteVariableError` raise(코드로 강제, 테스트로 검증). 필수 13 figure + 보조 4를 PNG 300dpi·PDF 양쪽 생성, 캡션마다 denominator·missing 수 기재. 435프레임 legacy셋(Phase 1/3 필드 없음 → 01/05/10을 "N/A: field not present" 패널로 명시, raw luma 무단 대체 금지)과 ph7 usable10셋(01/05 정상 출력) 양쪽 실행 17/17. 발견: `projected_size_actual`이 435장 중 87장(20%) >1.0·최대 39.09로 과대읽기 → Pearson 0.371 vs Spearman 0.985로 갈림(fit은 [0,1]로 제한하고 제외 건수·최대값 기록, ECDF는 전량 유지); 감사 manifest는 frame_id가 데이터셋 간 중복이라 형제 JSON의 `dataset`으로 출처를 확인해 불일치 시 join 거부(가드 없을 때 ph7 10장 분석에 500셋 435행이 섞여 rows=446 재현). 테스트 358 passed(신규 35).
- 2026-07-27: v2 개편 Phase 9A/9B — 전체 unit test 358 passed(failed 0, 78.4s; `test_camera_distance_cap.py`의 100k spec bulk draw 포함) 최종 확인 후, bpy-free dry-run 하네스 `scripts/data_prep/blender/dryrun_v2_proposals.py` 신규(렌더 0, bpy import 0). 기존 `audit_v2_dryrun.py`는 accepted 목표 + LEGACY/LATERAL A/B 구조라 예산 단위가 달라 별도 파일로 만들되 스트림은 Phase 7의 production `iter_proposals`, 통계는 Phase 8의 `ecdf/pearson/spearman`을 재사용. 결정성은 proposal canonical JSON의 SHA-256 스트리밍 해시로 비교. **B1 5,000 proposal 12/12 PASS**(거리>10m 0건 max 9.999785m, NaN/inf 0, 빈 feasible interval 0(draw당 최소 4 bin), out_of_range reject 0, digest 3cd365eec96d1009 일치, acceptance 4439/5000=88.78%, exhaustion 0(1.126≤20), 무관 marginal 최대 0.00063). B1 통과 후 **B2 40,000 proposal 12/12 PASS**(max 9.999910m, out_of_range 0, NaN 0, digest 066daafe45e60357 일치, acceptance 35792/40000=89.48% [Wilson .8918-.8978], starvation 0, 무관 marginal 최대 0.00008, 92s). 핵심 실측: bin0(<10%)이 draw의 68.98%에서만 feasible한데 accepted marginal은 정확히 0.2000 — masking-only sampler라면 0.138로 주저앉을 값을 accept-time quota deficit이 복원(Phase 1 설계 주장 40k 실증); 거리는 projected size에만 반응(log d vs log ratio Pearson -0.9381 / log fx +0.0107 / log W -0.0029)이라 상한이 fx·aspect 커플링을 만들지 않음; 항등식 잔차 max 0.000e+00; reject는 bin4(>60%, 0.38-2.22m)에 집중(accept 0.6554, C1 1355/d_occ_fail 1170/v_below_min 1231)이고 나머지 bin은 0.956-0.995. B3(100k accepted)은 지시대로 미수행. 산출물 reports/v2_revision/dryrun_{5k,40k}_summary.md + checks.json + axis_marginals.csv + proposals.csv + joint_eda.png/pdf(distance x fx x aspect x projected-size 6패널). ⚠️ RGB/mask/조명 미검증 — training-ready 선언 불가.
- 2026-07-27: v2 개편 Phase 9C — 20-frame exact determinism smoke를 fresh headless Blender 프로세스로 2회 실행(seed 7000, `--completion-mode records --n 20 --render-profile diagnostic-exact`, out=`data/pallet/_v2_smoke20_9c_run1|run2`, 각 619s). **결정성 축은 완전 통과**: record 20/20·label 18/18 semantic mismatch 0, RGB 18/18 sha256 바이트 동일, mask 90/90 디코드 픽셀 동일(PNG 바이트는 90/90 상이 — Blender 인코더 비결정성, 기지), realize 실패 위치·사유·수치까지 재현. 감사(`audit_v2_scene_logic.py`) 두 실행 모두 status=PASS(fatal 0, strict decode fail 0, magenta 0, empty M0 0, pixel inclusion 위반 0, ground continuity 18/18, hull IoU median 0.963), 거리 위반 0(actual/target 최대 9.3277m ≤ 10m). 9B 40k dry-run(seed 7000) 첫 20 accepted proposal의 target 거리가 이번 20 record와 20/20 일치 → pure층↔Blender층 plan 스트림 동일 [확인]. **다만 "20 rendered / realize fail 0" 조건은 FAIL(18/20)**: 두 실행 모두 idx 15·18(controlled-occlusion, occluder_side=bottom, f_target 0.446/0.223)에서 `bounded_local_search_exhausted`. 이는 요구 가림률을 못 만든 프레임을 잘못 렌더하지 않고 버리는 설계 경로(`v2_realize.py:2428` + `explicit_requirement_failure`)이고, 손대지 않은 기존 500셋도 435/500 rendered·실패 62건 중 63/65가 controlled-occlusion이라 **Phase 1~8 회귀가 아님**. controlled 슬롯 realize 실패율 42%(side=bottom은 27%로 최약) → records 모드에서 20/20 rendered가 나올 확률 ≈6.6%로, 조건 자체가 `--completion-mode records` 정의와 양립하지 않음(정확히 N장 렌더는 Phase 7 usable 모드 역할). 지시대로 수정하지 않고 blocker 보고, 9D로 넘어가지 않음. 산출물 `reports/v2_revision/smoke20_9c_determinism.json`.
- 2026-07-27: v2 개편 Phase 9D/9E — usable 50-frame quality smoke(`--completion-mode usable --n 50 --render-profile dataset-quality`, seed 7000, out=`data/pallet/_v2_smoke50_9d`) **50/50 배달, 32.1분**(Phase 7 추정 35~70분보다 빠름; proposal 107·render 시도 75·수율 66.7%·배달당 median 13.6s). 지시된 17개 조건 전부 PASS: rgb/label/mask 50/50/250, records_rejected.jsonl 57행(mode_filter 24+solve 8+render 25) 전량 보존, 거리>10m 0(max 9.737m), empty M0 0, strict decode fail 0/300, 픽셀 포함관계 위반 0px, exact collision 0, support/clearance/ground continuity 50/50(probe fail 0/550, min floor edge margin 15.484m), G1~G5 50/50, audit status=PASS(failures 0), overlay 50+sheet 5, source-mask sheet 3페이지, continuous EDA required 13/13 status=ok(구 500셋에서 N/A였던 figure 01·05·10이 실제 곡선 → Phase 1/3 필드 배선 회귀 확인). G5가 final RGB 기준임은 코드 경로+라벨로 [확인](raw≠final 48/50, max Δ5.94)이나 이번 50장은 최소 luma 16.35라 임계 12.0을 가로지르는 프레임이 0이어서 배달셋만으로는 raw/final 구분 불가. **신규 발견 2건**: ①solvePnP exact success는 50/50이지만 그중 f0038·f0049 2건이 미러/뒤집힌 해(reproj 34.9/30.6px, rot err 146°/160°) — 둘 다 visible kp 5 + 저앙각(8.5°/4.6°)이고 노이즈 0인 exact GT에서 발산하므로 EPnP의 평면 퇴화 구성 문제(평가 코드와 일치시키기로 한 solver는 미변경) ②usable 셋은 정의상 전원 all-pass라 continuous EDA figure 10/11/12의 pass-probability 곡선이 퇴화(base rate 1.000, 신뢰 격자점 0/200) → 게이트 튜닝은 records-mode 셋 필요. 9C 예측 재현: controlled-occlusion side 배달 구성이 처방(SIDE_WEIGHTS .30/.30/.25/.15, **균등 아님**) 대비 left 7·right 6·bottom 2·center 0으로, bottom 성공률 17%(2/12)·center는 solve 단계 resample이 side를 매번 재추첨하는 탓에 렌더 시도조차 0.026(1/38). noise tier 실측 clean 26/low 19/medium 5/high 0(chi2 5.58, df=3 → 처방과 불일치 근거 없음; high는 n=50에서 기대 1.5장). 보고서 `reports/v2_revision/quality_smoke50/summary.md`(534행) 작성 — Section 10 6개 질문 답변 + blocker 11건(B1 controlled realize 실패 61%, B2 side 편향, B3 PnP threshold 확정 불가, B4 exact-GT PnP 발산, B5 projected_size_actual 과대추정 12%, B6 tiny 배달 8%, B7 high tier 미검증, B8 GPU 비재현성, B9 usable셋 판별력 부재, B10 f_static 카운터 부재, B11 40k 감사 비용). 500-frame pilot·40k 본렌더·commit/push 없음.
- 2026-07-28: v2 mask output profile — `--mask-profile {full-audit,public}` 신규(기본 full-audit = 기존 500-record 진단 동작 완전 보존). 신규 bpy-free 모듈 `scripts/data_prep/blender/mask_profiles.py`가 stage 목록·경로 레이아웃·홀드아웃 패스·분해를 단일 정의하고, `v2_realize.measure_geometry_and_masks`는 하드코딩 5회 렌더 대신 `MP.holdout_passes(profile, hide_groups)`를 돈다. public은 **M1~M3를 렌더 자체를 안 함**(렌더 후 삭제 아님 — 2프레임 Blender 스모크 로그의 `Saved:` 줄이 프레임당 3개[rgb+2]로 [확인])이고 `mask_amodal/fNNNN.png` + `mask_visible/fNNNN.png` 2장만 남긴다(`mask/` 디렉토리 미생성, m0/m4 중복 저장 없음). label: `mask_profile`·`occlusion_decomposition_available`·`mask_area_amodal` 추가, public에서 f_static/f_cargo/f_context/f_explicit는 **`None`(0.0 아님)**, `f_total`은 `1-visible/amodal` exact — 실측으로 가림 0 프레임의 f_total이 `0.0`(≠None)임을 확인(0/False→None 금지 규칙). 같은 seed 2프레임을 두 프로파일로 렌더해 mask 면적 동일(14304/80813) → 프로파일은 저장 정책만 바꾼다. 감사기는 `--mask-profile auto`(디렉토리로 감지)로 public 셋을 2-mask 기준(`visible ⊆ amodal`)으로 검사해 status=PASS, 위반 시에는 여전히 fatal. compatibility lookup `MP.resolve_frame_mask_path`로 audit_pnp_eligibility·overlay_v2_detailed를 1행씩 배선(레거시 셋 동작 불변). record 스키마(146키 frozen)는 미변경, 기존 데이터셋 삭제·이동 없음. 테스트 387 passed(358→+29). 미배선: analyze_v2_scene_logic·compare_v2_determinism의 `root/"mask"` 하드코딩, usable 모드에서의 public 파일 정리 경로.
- 2026-07-28: canonical overlay 복원 — `gen_trunc_addon.py`의 `# === Detailed Overlay ===` 블록(L577-712)을 신규 bpy-free 모듈 `scripts/data_prep/blender/overlay_archive_trunc_style.py`로 **verbatim 추출**해 archive `trunc_addon_v1_pilot/overlay` 스타일을 정본으로 복원. 엣지 per-world-axis 색 (255,80,80)/(80,220,80)/(80,130,255)·keypoint 9색·off-screen (130,130,130)·r=6/centroid r=7·패널 (6,6,175,240) 줄간격 11·레전드 90x60 at (W-96,H-66)까지 상수 그대로이고, **출력 캔버스 = 입력 RGB 크기**(상단 header·외부 우측 패널·M0/M4 contour·FRONT/REAR 색 전부 제거). `overlay_v2_detailed.py`에 `--style {archive,frontrear-debug}` 추가(**기본 archive -> `<out>/overlay/`**, 어제 만든 FRONT/REAR 넓은 패널은 `overlay_frontrear_debug/`로 격하). v2 어댑터 `archive_metadata()`는 20개 패널 필드를 label/record에서 채우고 없는 값만 `N/A`/`?`로, **0·False는 값으로 그대로 표시**(Trunc는 in-image 코너<8, Occ는 f_total>0, Size는 프레임별 `dimensions_m` — 하드코딩 없음). pose 축은 `pose_transform` 회전+K 투영, z>0일 때만 그림. Pillow 10.1 이후 `load_default()`가 TrueType으로 바뀐 탓에 패널 글꼴이 달라지는 문제를 `load_default_imagefont()`로 고정. 부수적으로 `MASK_NAMES` 5개 하드코딩을 `resolve_mask_names()`(mask profile 감지)로 교체. smoke50 3프레임(f0000/f0003/f0007)을 스크래치패드에 생성해 육안 검증(패널·색·레전드가 archive 원본과 동일, N/A 0건). 테스트 441 passed(387->+54, 기존 테스트 삭제·완화 없음). 데이터셋 재생성/Blender 렌더/commit 없음.
- 2026-07-28: archive-style overlay 재생성 + 정본 대조 검증 — Blender/RGB 재렌더 없이 `_v2_smoke50_9d` 50장 전량을 canonical overlay로 재생성(`overlay_archive_style/` 50 + `contact_sheets_archive_style/` 5, 기존 `eda/`·`eda_phase6/` 미변경). `overlay_v2_detailed.py`에 leaf 폴더명 override `--overlay-dirname`/`--sheet-dirname` 추가(기본값 = 기존 `overlay/`·`contact_sheets` 동작 그대로). `_v2_cleanbase_smoke20_seed7100`은 존재하지 않아 건너뜀. **버그 1건 수정**: Section 2~4의 `_archive_bitmap_font()`가 PIL 고전 6x11 비트맵을 강제했으나 정본은 Pillow>=10.1의 AA TrueType(`load_default()`)으로 그려진 것이 픽셀로 반증됨 — 정본 panel 순백 픽셀 0개(글자 코어 240), 고정 내용인 legend 박스(91x61) 골든 diff가 `load_default()`는 **0px**, 비트맵은 **482px**, 패널 1행만도 442px 차이. `_archive_font()`로 교체 후 전량 재생성하고 골든 테스트 `test_legend_is_pixel_identical_to_the_archive`로 회귀 고정(테스트 3개는 잘못된 가정 교체 — 순백 매칭 -> AA ink 임계 120, 20번째 줄 descender 1px 허용). 검증은 신규 `_verify_archive_style_pixels.py`로 12항목을 픽셀 판정: 완성 오버레이를 같은 keypoint의 참조 레이어(edge 전용/edge+dot)와 비교하고 나중 요소의 덮음·AA convex blend·1px 래스터 편차(정본 JSON 소수 2자리 반올림)만 허용. **판정 기준 자체를 정본 12장이 12/12 통과하는지로 먼저 검증**(초기 기준은 정본도 FAIL시킴 — 13.7m 프레임은 엣지가 dot에 완전히 가려지고, 축 글자 AA가 순수 축색 픽셀을 하나도 안 남김). 최종: archive 12장·신규 12장 **각각 12항목 전부 PASS**(Read로 24장 육안 확인 병행), panel (6,6)/(181,246)=(0,0,0)·legend swatch (255,60,60)/(60,220,60)/(80,130,255)·canvas==RGB 24/24 일치. 산출물 `reports/v2_overlay_fix/archive_reference_vs_new.png`(6쌍, 양쪽 원본 크기)·`visual_verification.md`. 테스트 443 passed(441->+2). commit/push 없음.
- 2026-07-28: 8-frame public-mask + archive-overlay end-to-end 스모크(실제 Blender) — `--completion-mode usable --n 8 --mask-profile public --render-profile dataset-quality --noise-tier clean`, seed 7000, out=`data/pallet/_v2_publicmask_overlay_smoke8`, **299 s / exit 0 / 8장 배달**(proposal 12·render 시도 9·reject 1). 6.3 필수 결과 **15/15 PASS**: rgb/label/mask_amodal/mask_visible 각 8, M1/M2/M3 파일 0, 영구 mask 16, `f_total` 8/8 non-null(0.0~0.4663, 가림 0은 None 아닌 0.0 유지)·`f_static/f_cargo/f_context/f_explicit` 8/8 null·`occlusion_decomposition_available=false` 8/8·`mask_profile="public"` 8/8, visible⊆amodal 8/8(위반 0 px), archive overlay 8장 canvas==RGB 8/8·외부 패널 0·full-width header 0. `mask/` 디렉토리 미생성, 프레임당 `Saved:` 3줄(M1~M3 렌더 자체 없음). 감사기 auto 감지 PASS(frames 8, failures 0, `mask_names=['m0','m4']`). **Section 1이 남긴 미검증 항목(usable reject cleanup on public paths)은 스모크만으로는 검증 불가**였다 — 유일한 render reject가 realize 실패라 record에 `mask_paths`가 없어 cleanup 경로를 타지 않음. 그래서 `--magenta-max-fraction=-1.0`으로 **렌더 성공 후 gate reject를 강제**한 1-frame probe를 스크래치패드에 별도 실행해, `removed_files` 3건(rgb + `mask_amodal/` + `mask_visible/`)·잔재 0·PNG 0으로 public 경로 정리 동작을 [확인]. overlay 8장 Read로 전량 열람 + 픽셀 판정 12항목 8/8, mask 시트로 amodal(가림 포함 전체 실루엣, 포크홀 구멍까지) vs visible(가림 반영) 육안 확인. 판정 스크립트 `_verify_archive_style_pixels.py`의 centroid on-screen 조건이 `<= h-1`이라 y=539.82(H=540)인 f0004를 오판 → archive 원본과 동일한 `<= H`로 수정(정본 12 + 신규 12 재실행해 회귀 없음 확인), `--new-root`/`--new-overlay-dirname`/`--skip-archive` 추가(기본값 = 기존 동작). camera-postprocess를 완전히 끄는 CLI 옵션은 없어 최약 티어 `--noise-tier clean`(blur/noise/jpeg 확률 0)을 사용. 프로덕션 코드 무변경, 테스트 443 passed 유지.
- 2026-07-28: 테스트 커버리지 검증 + 최종 보고서 — 지시가 요구한 필수 unit test 12항목이 어느 테스트 함수에 있는지 전수 대응표를 만들어 확인(전문은 `reports/v2_overlay_fix/final_report.md` 10절). 12항목 모두 커버되고 있었으나 4가지가 약해서 **테스트 12개를 신규 추가**(기존 삭제 0·완화 0). ① 신규 클래스 `TestMatchesTheArchiveSource`(9) — edge/keypoint/panel/legend/축/Area%/캔버스 상수를 테스트 파일 리터럴이 아니라 **`gen_trunc_addon.py` 소스를 정규식+`ast.literal_eval`로 파싱해 꺼낸 값**과 비교하므로 "archive와 exact match"가 진짜 diff가 된다(복사와 리터럴이 함께 틀리는 경우를 잡는다). 여기에 archive의 **inclusive on-screen 조건**(`0 <= x <= W`, Section 6의 f0004 centroid 오판 원인)과 "블록 안에 Image.new/paste/concatenate/hstack/vstack이 하나도 없다(= 캔버스 불변)"도 고정. ② `test_only_the_profile_mask_directories_are_created` — `MP.mask_dirnames`가 어떤 테스트에도 안 걸려 있었고 "public 셋에 빈 `mask/`를 안 남긴다"가 스모크로만 확인돼 있었다. ③ `test_corner_ids_follow_the_label_projected_cuboid_order` — keypoint convention 가드가 v2_realize 소스 텍스트뿐이라 **그리기 쪽이 비어 있었다**: label `projected_cuboid` 인덱스 i가 archive 색 i로 칠해지는지 픽셀로 8/8+centroid 확인. ④ `test_overlay_run_never_rewrites_the_dataset` — overlay는 view여야 하므로 CLI 실행 전후 labels/records/mask/rgb SHA-256 동일 검증. **전체 455 passed(443->+12, failed 0, skip 0, 142.6s)**. 최종 보고서 `reports/v2_overlay_fix/final_report.md` 작성(지시 12항목 순서대로: overlay가 archive가 아니었던 이유=정본이 코드로 존재하지 않았음 / 재사용 범위 L90·L603-611·L616-710 매핑표 / 변경파일 신규7·수정8 / canonical vs debug 11축 비교 / M0~M4 의미와 f_* 정의 / public이 M0·M4만 남기는 이유 / 50장 재생성 12항목 12/12 / 8-frame smoke 15/15 + reject cleanup probe / 비교 이미지 경로 / 테스트 대응표 / 남은 문제 11건 / git diff). 완료 조건 5개 전부 충족(archive형 canonical, public 2장, full-audit만 5장, 500·40k 미실행, commit·push 미실행). 남은 문제: analyze/compare의 `root/"mask"` 하드코딩 미배선, 정본 경로 문서화, **M1이 배경을 안 숨겨 f_static이 배경 가림을 흡수하는 구조**, n=8 수율 일반화 금지(9D 50장 66.7%), 어제 blocker(controlled realize 실패·PnP threshold·EPnP 발산 등) 유효. 프로덕션 코드 변경 0, Blender 미실행, commit/push 없음.
- 2026-07-28: data/pallet 구조 조사·정리 계획 Stage 1(비파괴) — 디렉토리 2,489/파일 363,015/191.02GB 전수 인벤토리 + 경로 참조 그래프(124파일 552참조) + 중복 조사. **이동 0·삭제 0·폴더생성 0**. 핵심: ① `archive/textures_{wood,floor}` 는 이름과 달리 현역 — `v2_realize.py:768/804` 가 다줄 `os.path.join` 으로 하드코딩(단순 grep 미검출, ripgrep multiline 으로 확인) ② 참조 252개 중 168개가 존재하지 않는 경로이고 74건이 실행 코드/설정 — 과거 archive 이동 때 코드를 안 고쳐 이미 끊겨 있었다(`floor_and_mask.py:22`, `run_addon_v1.sh:66-67` preflight 는 지금 실행하면 중단) ③ `archive/trunc_addon_v1_pilot` 은 `skipUnless` 기반 golden fixture라 옮기면 FAIL 이 아니라 **조용히 SKIP**(현재 51 passed/skip 0 실측) ④ 최상위 zip 15개(85.29GB)가 archive 추출본과 엔트리수·비압축바이트 일치(central directory 만 읽어 대조), `train_palletobj_v1.zip` 은 BadZipFile 손상, `pallet.zip`=v1+v2 번들 ⑤ `_DISTRIBUTION_EXCLUDE.txt` 경로 5/5 stale → 이동 시 갱신 안 하면 릴리스 라이선스 게이트 무력화. 분류: SAFE 330(172.74GB)/BLOCKED 35/KEEP 11/USER_DECISION 36/UNIDENTIFIED 2(isaac_assets 는 근거 충돌로 이동계획 제외). 산출물 `reports/data_pallet_cleanup/`(README·proposed_tree·rollback_plan + CSV 7종). Stage 2 는 경로 registry(`config/paths.yaml`) 도입이 이동보다 먼저 — 사용자 승인 대기. commit/push 없음.
- 2026-07-28: data/pallet Stage 2-A(폴더틀+경로 registry+저위험 run 이동) — 뼈대 34폴더/README 7 생성, `config/synthetic/pallet_paths.yaml`(21키)+bpy-free resolver `pallet_data_paths.py`(테스트 22 신규) 도입 후 blender_config·v2_realize·v2_pipeline·distractor_pool_v2 배선. **저위험 run 146건/6,921파일/1.197GB 를 runs/{smoke,diagnostics,failed} 로 트랜잭션 이동**(같은 볼륨 rename, 삭제 0, overwrite 0, SHA256 6,921건 전부 일치). ★핵심: Stage 1 정규식 스캐너가 `os.path.join(root,"data","pallet",X)` 를 구체 경로로 환원하지 못해 `audit_v2_dryrun.py:39 DEFAULT_OUT=v2_dryrun_audit` 이 "문서 참조뿐"으로 보였다 — join-형 재스캔(34건)으로 잡아 이동 대상에서 제외(옮겼으면 다음 실행이 옛 경로를 재생성해 이동이 조용히 무효). 같은 재스캔으로 `blender_config.py:184/196` 이 존재하지 않는 `data/pallet/textures_{wood,floor}` 를 조립 중이고 `v2_realize.py` 의 archive 하드코딩 폴백이 그걸 가려주던 것도 발견 -> registry 로 교체. stale 189건 분류(ACTIVE_RUNTIME 3 전부 수정, ACTIVE_TEST 0 -> 이동 게이트 PASS), ledger 11곳+CLAUDE/AGENTS 경로 정정, history 문서 22건은 소급 수정 없이 path_map.csv 로 대응. 검증: 파일 363,015->363,026(+11=신규 README/manifest만, 기존 소실 0), pytest 477 passed(455->+22, skip 0), 5k dry-run FrameSpec sha256·accepted 4313/86.3% 전후 동일. production 자산·zip·legacy 이동 0, Blender 렌더 0, commit/push 0.
- 2026-07-28: data/pallet Stage 2-A.1 안정화(데이터 이동 0) — ① registry 테스트를 clone-safe unit 41 + local integration 20 으로 분리(기존 22, assertion 약화 0). Stage 2-A 테스트가 새 clone 에서 3건 FAIL + 1건은 **틀린 이유로 통과**(detect_project_root 가 data/pallet 부재 시 fallback 으로 떨어지는데 그 값이 우연히 repo) → fixture 로 walk-up 자체를 검증하도록 재작성. `data/` 없는 임시 트리에서 41 passed/skip 0 실증. 작업 중 새 CLI 테스트가 fixture 아닌 실제 저장소 경로로 통과하던 것도 발견해 --data-root 고정 ② runtime 정본 단일화: pallet_paths.yaml = source of truth / manifests/*.csv = local snapshot(문서 4곳 정리) ③ 트랜잭션 --hash-mode selective|all 추가(manifest 에 hash_mode·hashed/unhashed count·타임스탬프, all 인데 unhashed 남으면 중단), 기존 원장은 "selective-legacy" 로 읽고 rewrite 안 함, 경로 경계를 startswith → is_within(realpath+normcase+commonpath) 로 교체(data/pallet_backup 오판 해소) + unit 39 ④ pallet_data_paths CLI `if args.audit or True` 제거(--key/--audit/무인자 분기, 잘못된 key 는 목록+exit 1) ⑤ onboarding 을 M0~M4 mask 규약(public 2-stage / full-audit 5-stage, None=미측정 vs 0.0=가림없음) + canonical overlay(--style archive) + registry 절로 갱신, 과거 실패기록 경로는 미수정 ⑥ analyze/compare 를 mask_profiles 기반 public·full-audit 양쪽 대응(auto 감지, mismatch 는 기본 error, --allow-mask-profile-mismatch 는 partial 로만 표기). analyze 에서 public CSV 에 빈 m1~m3 컬럼이 남던 버그도 발견·수정. 검증: unit 566 + integration 20 = 586 passed(skip 0), Stage2-A verify 146/6,921/failures 0 + 원장 sha256 불변, 5k dry-run FrameSpec sha256·accepted 4,313 동일, data/pallet 파일수 delta 0. commit/push 없음.
- 2026-07-29: data/pallet Stage 2-B(현역 자산·golden reference 이동) — 10 source 중 **7개 이동(3,288파일/1.711GB, hash-mode all, SHA256 mismatch 0, 삭제 0, overwrite 0)**, 3개 BLOCKED. archive/textures_wood·textures_floor -> assets/materials/{pallet,floor}/, archive/trunc_addon_v1_pilot -> reference/golden_overlay/, real_data -> reference/real_images/, hdri -> assets/lighting/hdri/library, models_usd -> assets/pallets/models/, pallets_v2_add -> assets/pallets/source/. Stage 1 이 찾은 "이름은 archive 인데 현역" 3종이 전부 정상 위치로. ★BLOCKED 사유: production .blend 가 distractors/ 를 **절대경로로 356건 참조**(Blender 읽기전용 감사 603 datablock: BLOCKED_ABSOLUTE 356 / SAFE_RELATIVE 246 / MISSING_CURRENT 1) → distractors·blender_scene 이동 금지, background 는 원본 ZIP 3개(157MB) 때문에 규칙상 금지. .blend 를 고쳐 억지 통과시키지 않음. 부수 발견: 그 절대경로들 탓에 synth_data_scene.blend 는 이미 이 머신에 못박혀 있고(다른 워크스페이스 흔적이 MISSING_CURRENT 로 남음) 이는 Stage 2-C 최우선 과제. scene_noemit.usd 의 절대경로는 pxr Sdf.Layer 로 확인한 결과 documentation 주석이고 composition arc 0 → models_usd 이동 안전. ★Stage 2-A 잠복 회귀 발견·수정: Blender 내장 Python 에 PyYAML 이 없어 json fallback 으로 가는데 registry 파일 머리의 # 주석 때문에 **Blender 안에서 blender_config import 가 실패**하고 있었다(Stage 2-A 이후 Blender 미실행이라 미발견) → _strip_hash_comments + 회귀 테스트 2개. _DISTRIBUTION_EXCLUDE.txt stale 5건 정정 + NoAI baked legacy 4종 신규 추가 + 검증기 verify_distribution_exclusions.py(entries 10/problems 0/leaks 0). golden overlay 테스트는 리터럴 대신 registry 조회로 전환. 검증: unit 568(566->+2)·integration 23(20->+3)·golden 51 전부 skip 0, Stage2-A 원장 sha256 불변, 5k dry-run FrameSpec 938f387d·accepted 4,313 동일, Blender no-render 감사 신규 파손 0, 2-frame smoke 필수 66항목 실패 0(overlay 육안 확인). commit/push 0, 500/40k 렌더 0.
- 2026-07-29: data/pallet Stage 2-C1(production .blend portable 화, 원본 무손상) — 원본 `synth_data_scene.blend`(sha256 46f436dc…, 358,917,479B, mtime 2026-07-24 19:39)를 **한 바이트도 건드리지 않고** 같은 폴더에 `synth_data_scene_portable.blend`(5cad94e5…)를 새로 만들어 registry `production_scene` 을 그쪽으로 승격. 절대 외부경로 **229→0**, missing **1→0**, 구조 diff **0**, 데이터 폴더 이동 0. ★Stage 2-B 의 "BLOCKED_ABSOLUTE=356" 은 과다계상이었다 — Stage 2-B 가 커밋한 `blend_external_paths.csv` 의 356행을 raw 로 세면 **228 은 드라이브 절대경로, 128 은 이미 `//..\distractors` 상대경로**다(그쪽 판정 로직이 resolve 결과로 분류). 파일은 변하지 않았고(SHA256 동일) 실제 재작성 대상은 228건. 두 보고서에 등장하던 중간값 514 가 그 흔적(158+356 = 286+228). ★factory_yard_2k.hdr 누락 datablock은 users=1(활성 world 의 Environment Texture)이라 제거 불가 → 전 저장소 검색 결과 후보 정확히 1건이고 그 파일의 sha256(71d5b32c…)이 Stage 1 인벤토리가 기록한 **이동 전 원본과 동일**, SOURCES.txt/CC0 출처 일치, Blender decode OK → **REPOINT_EXACT**. v2 `CONSTRAINED_HDRI_EXCLUDE`·v4 `_DROP` 이 그대로라 렌더 pool 은 28 로 불변(un-drop 아님). 신규 재사용 도구 3종: bpy-free 순수 helper `blend_path_utils.py`(단위 테스트 43 — Py3.13 ntpath.isabs 변경으로 POSIX 절대경로가 미검출되던 버그를 테스트가 잡아 수정), `manage_blend_external_paths.py`(--audit/--plan/--apply-candidate/--verify, source==candidate 거부·candidate overwrite 거부·source SHA 전후 대조·계획 외 변경 거부·strict 게이트, pack/make_paths_* 전체 호출 0), `audit_blend_assets.py`(Stage 2-B 가 임시 스크립트로 돌리던 no-render 감사를 정식화). 검증: mapping SHA256 228/228 mismatch 0, 계획 외 변경 0, 재개방 후 절대경로 0·missing 0·//textures 158·//../distractors 356, no-render 감사(HDRI 30/30·floor 42/42·wood 27/27·node image 누락 0), 2-frame smoke seed 7210 usable 2/2 **필수 87항목 실패 0**(마스크 전경은 파이프라인 정본 strict_decode_mask(>127) 를 import 해 사용 — 처음 >0 으로 세어 틀렸던 것 정정), RGB·overlay 4장 육안 확인, unit 614(568→+46)·integration 26(23→+3)·golden 51 skip 0, Stage2-A 원장 fe1adc26… 불변, 5k dry-run FrameSpec 938f387d…/accepted 4,313 및 proposals digest 3cd365ee…/12-12 PASS 전후 동일. 런타임 씬 경로는 리터럴 대신 resolver CLI 조회로 교체(run_dataset_v4/run_4pallet_mask/run_pilot_2k + docstring 예시). Stage 2-C2 선행조건: distractors/blender_scene 이동 시 이제 **상대경로 356+158건 rebase** 가 필요(사유가 절대참조에서 바뀐 것). commit/push 0, 500/40k 렌더 0, 데이터 삭제 0.
- 2026-07-29: data/pallet Stage 2-C2(최종 레이아웃 이동 + blend rebase) — 루트에 남아 있던 distractors/blender_scene/background 를 **최종 assets 구조로 이동(1,411 파일 / 6.09GB, hash-mode all, SHA256 mismatch 0, 삭제 0, overwrite 0, 같은 볼륨 rename만)** 하고, 옮긴 뒤 blend 상대경로 357건을 rebase 한 새 씬 synth_data_scene_portable_stage2c2.blend(8cb4109a…) 를 승격. data/pallet 루트에 남은 자산군 없음. C2A background/*.zip 3개(157MB, 상대경로 보존)→archive/packages/background_sources → C2B background 74→assets/scenes/backgrounds/background → C2C distractors 1,161 + blender_scene 173 을 **하나의 transaction_group 으로 atomic 이동**. ★트랜잭션 도구 확장: Stage 2-A/2-B 는 directory 만 옮길 수 있어 file entry(entry_kind=file)와 transaction_group(그룹 실패 시 역순 자동 rollback), ZIP cohort 제한을 새로 넣었다 — 이때 group rollback 이 transaction_group 없는 Stage 2-A row 를 cohort 로 묶어버려 "실패 시 이미 옮긴 것은 둔다" 계약을 깨뜨렸고 **기존 테스트가 그걸 잡았다**(명시적 group 이 있는 row 에만 적용하도록 수정). 신규 정책 테스트 30개(tmpdir fixture 전용). ★rebase 는 문자열이 아니라 **이동 전에 각 datablock 의 resolved 절대경로+SHA256 을 target manifest 로 고정**한 뒤 그 정체로 대조했다(--emit-target-manifest 신규). action 판정을 처음엔 "root 가 움직였는가"로 해서 REBASE 514/KEEP 1 이 나왔는데(기대와 정반대), **이동 후 디렉토리 기준으로 상대경로를 실제 계산해 기존 문자열과 비교**하도록 고쳐 KEEP_RELATIVE 158(//textures, 동반 이동이라 그대로 유효) / REBASE 357(distractor 356 → //../../../distractors/library/, HDRI 1 → //../../../lighting/hdri/library/) 이 나왔다. ★최종 감사에서 //../distractors=0 이 떴는데 파일 파손이 아니라 **감사기 카운터가 옛 상대경로 문자열을 하드코딩**한 탓 → resolve 절대경로가 registry root 안인지로 세도록 위치 비의존 수정(안 고쳤으면 "참조 0" 이라는 틀린 안심을 보고할 뻔). registry ok=22→24(신규 키 production_scene_stage2c1_rollback, background_package_archive), blender.yaml 2종의 background filepath 리터럴은 새 경로를 다시 박지 않고 **background_root 기준 relpath 키 + blender_config 조인**으로 전환. _DISTRIBUTION_EXCLUDE 에 archive/packages/background_sources 추가(entries 11/problems 0/leaks 0). 검증: unit 646(614→+32)·integration 31(26→+5)·golden 51 skip 0, Stage2-A 원장 fe1adc26…·2-B B1 43461e47…/B2 0d0c06a8… **전부 불변**, C2A/C2B/C2C failures 0 unhashed 0, 5k FrameSpec 938f387d…·proposals 3cd365ee… 전후 동일, blend 재개방 absolute 0/missing 0/textures 158/distractor 356/HDRI 1/구조 diff 0/packed 86 불변/계획 외 변경 0, no-render 감사 전 항목 통과, 2-frame smoke seed 7220 필수 93항목 실패 0 + **두 프레임 모두 외부 distractor 실제 렌더**(렌더 시점 경로 해석 직접 증거). 원본 blend 46f436dc… 와 Stage 2-C1 portable 5cad94e5… 는 수정 없이 rollback source 로 보존. legacy dataset·isaac_assets·NoAI quarantine·weight 이동 0, 500/40k 렌더 0, commit/push 0.
- 2026-07-30: data/pallet Stage 2-D1.1(잔여 정리) — **D11A COMPLETE(10건 2.24GiB) / D11B·D11C PARTIAL(준비 완료·이동 미실행)**. ★**원장 연쇄(chained-ledger) 도입**: Stage 2-D1 이 rollback 해야 했던 D1D 10건(C2C 원장 구성원)을 `--successor-ledger-chain` 으로 되살렸다 — 없어진 파일을 통과시키는 유일한 근거는 **파일 단위 SHA256 identity 3중 일치**(prior 원장 기록 == successor pre-hash == successor destination 실측)이고 broad removal allow / expected-removal 목록은 쓰지 않는다. 16개 조건 강제(prior/successor manifest SHA 결속 · relpath 실재 · size/sha · successor source==prior dest · VERIFIED · destination 존재/size/sha · prior 1:1 · successor 가 복수 prior 대표 금지 · path escape · ledger cycle · unmapped missing 0 · **prior destination 에 아직 있는 파일은 chain 불가**). 결과: C2C 가 chain 없이 failures 11 -> exact additions+chain 으로 **failures 0**, mapping 10/10, unmapped 0. 기존 manifest 무수정. ★실행 중 발견: **verify 가 멱등이어야 한다** — 재검증이 verified_at 을 갱신해 successor 원장 SHA 가 바뀌자 chain 결속이 깨졌다(spec e2c1a19f vs actual 500da414). 첫 검증만 기록하도록 고치고 테스트 2개로 고정. 신규 테스트 31개(§4 요구 22항목 전부, tmpdir 30 + 실제 원장 읽기 1), unit 714->745 skip 0. **D11B registry 전환 완료**: 신규 key 4개(legacy_training_data_root 등, ok=24->28) + ★**config YAML 값도 `registry:<key>/<sub>` 참조로** 바꿨다 — resolve_config_value()+CLI --resolve 신설(리터럴은 그대로 통과=하위호환), 소비자 train_dope.sh resolve_path()·self_train.py 도 전환. 이렇게 해야 이동 후 config 재수정 없이 pallet_paths.yaml 키 1줄만 바꾸면 된다. 전환 16곳, 옛 경로 직접 참조 0, canonical broken ref 0. postprocess_v3.py 는 sys import 도 없어 함께 추가(없으면 NameError). **D11C: v4 파생 4종 = PROVEN_NOAI 확정 → ledger B8 해소**. 라벨 13,122 프레임 **전수** 스캔(표본 아님, 읽기실패 0)에서 objects[].name 에 Pallet_2/Pallet_3 가 63.3~100%(emptywood 는 100%, Pallet_1 아예 없음). Pallet_2/3=scene_2/3.usd="Old Wooden Pallet"(NoAI, ledger B1) + 해당 USD 가 _noai_quarantine_usd/ 에 실존 + mtime 2026-06-16~18 = 재-bake 이전. 반대증거도 봤다: 부모와 바이트 동일 프레임 0/200 = 복사본 아닌 독립 렌더인데도 NoAI 를 썼다(더 직접적 증거). README_CONTAMINATION.md 는 부모에만 있다 — **표식 부재는 무죄 근거가 아니다**. PROVEN_REDISTRIBUTABLE 0 / UNRESOLVED_LICENSE 0, 미확정을 redistributable 로 추정하지 않았다. ★**중단 사유 = hash read 예산**: §6 전역 상한 20GiB vs D11B 32.29+D11C 29.04=61.33GiB(3.1배). §17 이 초과를 중단 기준으로 명시하고 selective 강등 금지라 예산을 지켰다. cohort=transaction_group 원자성 때문에 쪼개지도 않았다(남은 15.53GiB 에 3건은 들어갔지만). 예산과 무관한 작업은 전부 완료 — 승인만 있으면 이동은 바로 가능. 검증: unit 745 · integration 31 · golden 51 · registry ok=28 missing=0 · exclusion 16/0/0/0 · 기존 원장 8종 failures 0 + SHA256 전부 불변(D1D ROLLED_BACK 증거 무수정) · 5k 938f387d/3cd365ee · active scene 8cb4109a 불변 · Dist_ 209 · data/pallet files 363,090 **불변**(bytes +16,749 = manifests 갱신분, exclude 파일 변경 0). 판정: D11_SCOPE_COMPLETE ✗ · FULL_DATA_PALLET_LAYOUT_POLICY_COMPLETE ✗ · FULL_PHYSICAL_MINIMAL_TREE ✗ (UNKNOWN 0 · 미분류 0 · broken ref 0 · leak 0 은 충족했으나 BLOCKED 8건 이동 미완). 잔여 top-level 65(4.27GiB) · archive depth-1 136 · KEEP 10 · quarantine 3 · 빈 폴더 7(삭제 안 함). 삭제 0 · 압축해제 0 · weight 이동 0 · Blender 렌더 0 · 학습 0 · commit 0 · push 0. 상세 [2026-07-30.md](2026-07-30.md).
- 2026-07-30: data/pallet Stage 2-D1(archive 정리 실행) — **판정 D1_PARTIAL: READY 40건 중 30건 이동 VERIFIED(139,733,678,407 B = 130.14 GiB / 191,518 파일), 10건 rollback.** 루트 ZIP 15개 -> 0개, archive depth-1 166 -> 151. 전 파일 SHA256 을 이동 전·후 2회 대조(read 260.27 GiB) — mismatch 0 · unhashed 0 · relpath/파일수/bytes 전부 pre==post · source 잔존 0 · 삭제 0 · `data/pallet` 파일수 363,090 불변(bytes +624 = exclude 파일 단독, dirs +7 = 새 semantic 폴더). cohort: D1B corrupt 1(4.22GiB, BadZipFile 상태 그대로 보존 — 복구·삭제 0) · D1A packages 14(75.21GiB, structural match 도 전부 보존, duplicates/ 미사용) · D1C datasets 15(50.70GiB/191,503파일, NoAI 3 -> noai_baked/ · PARTIAL 1 -> partial/ · 재분류 안 함). ★**D1D_BLEND_BACKUPS 10건은 cohort verify 까지 통과했으나 rollback**: 그 blend 들이 Stage 2-C2 C2C 이동(S2C2002)의 구성원이라 C2C destination 밖으로 빼내자 C2C exact verify 가 MISSING 11건으로 실패했다 — 데이터는 안전(D1D 원장이 새 위치·해시 기록)했지만 **검증 사슬**이 끊긴다. "예상된 제거" 허용 옵션을 만들면 통과하지만 그건 완화라 §18 따라 중단·rollback(blend 17개 원위치, C2C failures 0 복구, 보호 blend 6개 SHA256 불변). 대신 **prior-ledger guard 신설**(PRIOR_LEDGERS/prior_ledger_members/find_prior_ledger_conflict — 앞선 원장 구성원 이동 시 plan exit 2, 테스트 4개 중 1개는 실제 저장소 원장으로 검증). 40건 전수 재검사 결과 충돌은 D1D 10건에만 있었다. 실행 중 판정 2건도 고쳤다(완화 아님): (a) cohort 는 원래 READY+KEEP 이 섞여 있다 -> 금지 status 는 건너뛰고 --move-ids/--only-source 로 명시 요청할 때만 거부, (b) dataset 안의 ZIP(training_data_v4_split.zip)은 딸려 가되 같은 계획에 별도 row 로도 있으면 거부. 신규 policy `stage2d1-archive-finalization` — allowlist 가 코드 상수가 아니라 **동결 CSV**(SHA256 c343b807… 결속, 바뀌면 exit 2) + `--max-hash-read-gib` HashBudget(해시 시작 전 stat 검사 + 읽는 중 검사, selective 자동강등 금지, 생략 시 무제한=기존 동작) + `stat_only_snapshot()`(기존 verify 가 소파일을 두 번 읽어 D1C post read 가 2배 되는 것 방지) + cohort=transaction_group(실패 시 cohort 전체 역순 rollback). 예산 실사용 257.78/286 GiB, worker=1 순차. exclusion: D1C 전에 검증기가 stale 3건을 실제로 잡아 NoAI 3건을 새 경로로 정정(entries 16/problems 0/leaks 0/stale 0), `archive/training_data` 는 runtime 참조 살아있어 미이동이라 옛 경로 유지, redistributable 11건은 과잉 제외 안 함. manifest: archive.csv 30행 executed=yes+D1열 16개(나머지 202행 여전히 no) · path_map 175->205 · assets stage2d1_status 현역 17개 변경 0 · grouped_inventory 416->266(그룹 단위 유지, depth 3~4 추가, 사라진 206 목록은 diff md). 검증: unit 664->714(skip 0) · integration 31 · golden 51 · registry ok=24 · 앞선 원장 7종 failures 0 + SHA256 4종 불변 · 5k 938f387d(4,313/687)/3cd365ee(4,439,12/12) · active scene 8cb4109a 불변 · Dist_ 209 · 보호영역 전수(blend17/weight29/package20/dataset120) 불일치 0. **FULL_DATA_PALLET_LAYOUT_COMPLETE 아님** — BLOCKED 8(REFERENCE 4: registry 키 선등록 필요, UNKNOWN 4: ledger B8) · KEEP 12(weight 4 는 삭제후보 아님) · D1D 10 · top-level 잔여 65(4.27GiB) · archive 잔여 136. Blender 렌더 0 · 학습 0 · commit 0 · push 0. 상세 [2026-07-30.md](2026-07-30.md).
- 2026-07-30: data/pallet Stage 2-D0.1(안정화) — **데이터 이동·삭제 0**, `data/pallet` delta dirs+0/files+0/bytes+1,084(= `_DISTRIBUTION_EXCLUDE.txt` 단독). 판정 **READY_FOR_STAGE_2_D1**(게이트 15항목 PASS/FAIL 0, 범위=READY 40건 132.37GiB). ★canonical broken CURRENT reference **44 → 0**: 과거 보고 2/8/10 중 고르지 않고 같은 검출기를 기준커밋 트리(`git grep 60e0860`)와 현재 트리에 각각 돌려 셌다. **내 1차 감사가 19로 과소보고했고 원인은 스캔 패턴 2건**: (a) literal 패턴이 `data/pallet/` 앞에 따옴표를 요구해 YAML plain scalar(`train_dir: data/pallet/training_data/train`)를 전부 놓쳤다 → `config/default.yaml`·`stage3_selftrain.yaml` 의 깨진 학습 입력 5건 누락, (b) rg 덤프를 경로형 2패턴만으로 만들어 join-form(`os.path.join(root,"data","pallet",…)`)이 덤프에 아예 안 들어왔다 → `compute_distractor_fill_ratio.py:49`·`debug_pallet_orientation.py:10`·`merge_and_validate.py` 4건 누락. 7패턴(literal/literal_bs/os_path_join/pathlib/fstring/shell_var/yaml_value/bare)으로 덤프·파서 모두 교정. 고친 파일 25개/경로 변경 줄 67, `current_reference_fixes.csv` 는 손목록 대신 **git diff 에서 재구성**(1차 12파일이 손목록에 빠져 있었다). ★io_role 을 나눠야 정확하지만 output 을 전부 넘기면 안 된다 — `gen_palletobj_v1.py:569` 의 `save_as_mainfile(filepath="data/pallet/blender_scene/…")` 는 Stage 2-C2 로 없어진 **옛 폴더를 다시 만들어내는** 저장이었고 §11-G old-active-path 게이트에서 잡았다. 부재 자산 4종(`pallet_scene`·`real_unlabeled`·`test_render_v2`·`ndds3_pallet.pth`)은 이동 원장 0+Stage1 인벤토리 0 = Stage 2 회귀 아님이라 고치지 않고 주석만 달았다(`real_unlabeled`→`real_data_root` 동일시 근거 없음). exclusion entries 11→16, release leak 5→0 — **D0 의 "누락 5건"과 개수는 같고 멤버가 다르다**: `pallet.zip` 은 central directory 실측상 v1+v2 뿐이고 둘 다 redistributable 이라 leak 아님, D0 가 놓친 `training_data_v4_pilotA` 가 leak. 추가분 = `train_4pallet_mask_v1.zip`(9.01GiB, 추출본만 제외돼 ZIP 이 누출구, ledger **B7 해소**) + v4 파생 4종(NoAI 상속을 라벨 metadata 로 확인하지 않아 UNKNOWN_LICENSE 보수적 제외, ledger **B8 신규**). C2C verify 를 broad allow → `--expected-destination-additions <json>`(manifest_sha256 결속 + path/size/sha256 exact)로 고정, broad 단독 사용은 argparse 오류로 차단하고 `--allow-any-destination-additions`(deprecated)로 분리 — 음성검증 5종 전부 PASS(부분 allowlist→UNEXPECTED_ADDITION, manifest sha 위조→exit2, addition sha 위조→exit1, `../escape.txt`→exit2), strict 3 / exact 0. `inventory.csv` → `grouped_inventory.csv`(git mv, 416행 그룹집계 vs 전수라면 363,090행, 코드참조 0). ★Stage 2-D1 계획 재계산 **READY 40건 132.37GiB / BLOCKED 8 / KEEP 12** — D0 의 "48건 163.03GB" 를 쓰지 않았다. 계산 중 자체 버그 3개를 잡았다: (a) `under_registry()` 양방향 prefix 검사로 `archive_root` 하위 전부 차단 → READY 0 (container root 는 자식을 차단하지 않는다), (b) `current_runtime_refs` 를 기본값 0 으로 둬서 READY 판정 근거가 없었다 → 실측 채움, (c) leaf 매칭 후행경계 누락으로 `trunc_addon_v1`이 `_pilot`을, `training_data`가 `_v4`를 먹고 bare stem `pallet`이 전역 오탐 395건. 참조는 라인 단위 4분류(path_current 19=차단 / path_stale_old 19=차단 아님 / leaf_only / path_other). BLOCKED_REFERENCE 4건은 **§1에서 방금 고친 참조가 살아있는 것**(`archive/training_data` runtime ref 10 등) → D1 선행으로 registry 키 등록+참조 전환 필요. UNREFERENCED_WEIGHT 4개는 삭제 후보가 아니라 별도 승인 대상, isaac_assets(EULA)·NoAI USD 는 이동 금지, package structural match 를 duplicate 로 부르지 않음(duplicates/ 미사용). 검증: unit 664(646+18 신규 `test_destination_additions.py`)·integration 31·golden 51 skip0 fail0, registry ok=24, 원장 159move/11,622files/failures 0 + SHA256 4종 불변, 5k 938f387d(4,313/687)·3cd365ee(4,439,12/12), Blender no-render absolute0/missing0/Dist_209/active 8cb4109a 불변, 파일시스템 전수대조(archive166·package20·blend17·weight29·dataset120) 불일치 0, hash read 9.48GB/20GB. D1 실행 시 hash-mode all 이면 264.75GiB read 라 예산 결정 필요. Stage 2-D1 미시작, commit/push 0. 상세 [2026-07-30.md](2026-07-30.md).
- 2026-07-30: data/pallet Stage 2-D0(잔여 대용량 자료 비파괴 감사) — **데이터 이동·삭제·rename 0건, 파일시스템 delta dirs+0/files+0/bytes+0** 으로 170.34GB 를 분류하고 Stage 2-D1 계획만 작성. hash read 9.48GB/예산 20GB(refused 0), 압축 해제 0, testzip 0, Blender 렌더 0. 신규 도구 audit_pallet_archives.py(--inventory/--package-signature/--weight-inventory, hash-mode metadata|selective|full, read budget 강제·실제 read bytes 기록). ★archive/ 의 Stage 2-A semantic 하위폴더 7개(legacy_datasets·legacy_scenes·corrupt 등)가 **전부 비어 있고** dataset 156개가 depth1 에 평평하게 있다 — "archive/legacy_datasets 87.7GB" 는 계획된 목적지 이름이었다. ★**중복은 하나도 확정되지 않았다**: ZIP 끼리 central-directory CRC 를 read 0바이트로 대조하니 train_palletobj_v2.zip vs (2).zip(30,010 entries, path·size 동일)이 **CRC 3건 불일치**, pallet.zip/v1 vs (2).zip 이 4건 불일치였고, 불일치 파일만 표적 검증(3.3MB)한 결과 세 사본(추출본·ZIP·bundle)이 서로 조금씩 달랐다(같은 파일명·같은 크기인데 내용이 다른 PNG 3~4개). 파일수+bytes 만 봤다면 "중복이니 ZIP 삭제 가능" 으로 갔을 것 -> 전부 보존, packages/duplicates/ 는 비워 둠. 유일한 CONTENT_VERIFIED_BY_CRC 는 modular zip 2개(이미 2-C2 에서 둘 다 보존). pallet.zip=v1+v2 bundle(60,022 entries) 확정. 손상 1건 train_palletobj_v1.zip(EOCD 미검출 truncated, 꼬리가 PNG IEND)은 유일본 아니며 삭제 대신 corrupt/ 보존 이동 제안. blend 17개: ACTIVE 2·ROLLBACK_CRITICAL 4·COLD_ARCHIVE 11, byte-identical 3쌍이지만 둘 다 보존, BACKUP_OF 는 구조 signature 미확인이라 선언 안 함([추정] 유지). weight 29개 전부 크기 191.8MB 동일이라 SHA256 확인 필수 -> 전부 고유(DUPLICATE 0), ACTIVE 1·REPRO 24·UNREFERENCED 4(삭제 후보라 부르지 않음), broken reference 2건(net_pallet_best.pth·ndds3_pallet.pth). isaac_assets 4.05GB=LICENSE_QUARANTINE(EULA, 참조 0), NoAI USD 3파일은 격리 경로가 provenance 라 **이동 권고 안 함**. ★Stage 2-C2 보고 정정 2건: (a) C2C verify 가 failures 3 — missing 0·SHA256 1,334 mismatch 0 이므로 손상은 아니지만 "failures 0" 은 candidate 생성 **전** 값이었고 부정확했다 -> --verify 기본 동작은 그대로 두고 --allow-destination-additions 추가로 진짜 불변식만 보게 했다. (b) "옛 경로 runtime 참조 0" 은 리터럴 grep 만 본 결과였다 — join-form 재스캔으로 진짜 깨진 입력 참조 8건 발견, 그중 compute_distractor_fill_ratio.py:49(distractors)는 **내 Stage 2-C2 이동으로 깨진 것**이고 debug_pallet_orientation.py:10(models_usd)은 Stage 2-B 잔재다(Stage 2-A 교훈을 다시 놓쳤다). 감사 스크립트가 rg 를 subprocess 로 못 찾아 조용히 빈 결과를 내던 것도 발견·수정(그대로 뒀다면 "차단 0건" 이라는 틀린 안심을 보고할 뻔). blender_config.py:200·floor_and_mask.py:22 는 주석이라 가짜 양성. exclusion 검증 entries 11/problems 0/leaks 0, ★미등록 5건 검토 필요(train_4pallet_mask_v1.zip 은 추출본만 등록돼 NoAI 누출 경로; v4_split 파생 3종은 NoAI 상속을 파일 근거로 확인하지 않아 이름 유사성만으로 단정하지 않음). legacy dataset 120개(COMPLETE 17·NOAI_BAKED 4·PARTIAL 99·UNKNOWN 0). 계획 60행/이동 후보 48건 163.03GB, LEVEL4 CRC 전수는 55.18GB 로 예산 초과라 승인 대기, D1 실행 시 326.07GB hash read(cohort 분할 권장). 검증: registry ok=24·unit 646·integration 31·golden 51 skip 0, 원장 sha256 전부 불변, active scene absolute 0/missing 0/Dist_ 209, 5k FrameSpec 938f387d…·proposals 3cd365ee… 12/12. _raw_pkg_*.json 20개(42MB)는 read 0바이트로 재생성되므로 .gitignore(결론은 요약 CSV 5종). Stage 2-D1 미시작, commit/push 0.
- 2026-07-31: data/pallet Stage 2-D1.2(D11B·D11C 실제 이동, D11 범위 종료) — **D12B_COMPLETE ✅ / D12C_COMPLETE ✅ / D11_SCOPE_COMPLETE ✅ / FULL_DATA_PALLET_LAYOUT_POLICY_COMPLETE ❌ / FULL_PHYSICAL_MINIMAL_TREE ❌**. D1.1 이 예산 때문에 못 옮긴 2 cohort 를 승인 예산(D12B 36 + D12C 34 = 70 GiB)으로 실제 이동: **8 row / 132,049 파일 / 32,922,799,213 B(30.66 GiB)**, hash-mode all·workers 1·same-volume rename, pre 30.66 + post 30.66 = **total hash read 61.32/70 GiB**, SHA256 mismatch 0 · unhashed 0 · failures 0 · 삭제 0 · overwrite 0 · `data/pallet` 파일수 **363,090 불변**(bytes +11,790 = assets/README.md 621 + manifests 10,754 + exclude 415, 자산·데이터셋 0바이트 변경 — 보호영역 blend/weight/package/dataset SHA256 전수 문제 0, read 9.48GiB). D12B 4건(sandbox blend 1 + train_palletobj_v3 · training_data · v3_post_v1)은 D1.1 이 깔아둔 registry 전환 덕에 **실행 표면 무수정, pallet_paths.yaml 값 4줄만** 바꿔 이동(ok=28 missing=0, src_gone 4/4). D12C 4건(v4 파생 GREYBUG·bg1bak·emptywood·pilotA)은 `archive/legacy_datasets/noai_baked/` 로 → **NoAI baked 8종이 한곳에 집결**. ★**live reference 재측정으로 오탐 4건을 걸렀다** — D1.1 CSV 의 current_runtime_test_refs 를 그대로 복사하니 4건 전부 LIVE_REF 로 잡혔는데 그 값은 **registry 전환 이전**이라 무효였다. 지금 실행 표면을 다시 재서(registry 정본 yaml·resolver docstring 은 경로 소유자라 제외) 3건 0, 1건(`generate_all.sh:42` 낡은 주석)은 **진짜라서 갱신**. 그대로 믿었으면 이동을 못 했거나 진짜 stale 을 놓쳤다. ★**chain 은 자기 mapping 만 책임진다**: C2C 원장에서 D1.1 이 10개·D1.2 가 1개를 빼내 재검증 시 11 MISSING — D11A chain 만 주면 failures 2, D12 chain 만 주면 failures 11, **둘 다 줘야 0**(`successor chain: 11 file(s) from 2 chain(s)`). `--successor-ledger-chain` 을 반복 지정 가능(action=append)하게 고치고 중복 prior key 주장은 exit 2 로 거부. D1.1 chain 파일은 무수정. prior ledger 소속은 "chain 이 없으니 없겠지"로 넘기지 않고 8건 전부 직접 조회(D1-003 만 S2C2002 소속). 신규 policy `stage2d12-final-moves`(+241줄) — 기존 stage2d11 policy 가 `KeyError: 'scope'` 를 냈지만 **고치지 않고 새로 추가**(고치면 검증 끝난 원장의 재검증 의미가 바뀐다), scope SHA 결속·cohort 분할 거부·live ref 0·PROVEN_NOAI 는 noai_baked 외 목적지 거부(/redistributable//packages//unidentified//release//partial/)·registry_final_value==destination 강제. **ledger B8 해소**: v4 파생 4종 PROVEN_NOAI 확정(라벨 JSON 13,122개=프레임 13,120 전수, NoAI 9,634=73.4%, emptywood 100%·Pallet_1 없음, mtime 2026-06-16~18 = 재-bake 이전) — "미확정이라 보수적 제외" → **"확정돼서 제외"**. exclusion 은 D12C 직후 검증기가 **STALE_ENTRY 4건을 실제로 잡아** 갱신(entries 16/problems 0/leaks 0/stale 0); 주석의 "13,122 프레임"도 라벨 개수와 프레임 수를 혼동한 것이라 정정. ★**참조 감사 범위를 처음 넓혔다** — canonical(=.gitignore 준수, 이전 단계와 동일 범위)은 fix_required **0 / 530행**으로 D1·D1.1 과 동일하지만, `--no-ignore` 로 `data/pallet` 내부까지 보니 11건이 나왔고 그중 5건이 진짜였다: `data/pallet/assets/README.md` 의 "CURRENT 위치(2026-07-28 기준)" 열이 지금 하나도 없는 구 경로(models_usd/·distractors/·textures_wood/·blender_scene/)를 가리키고 있어 **열 자체를 registry key 로 교체**. 나머지 6건(진단 코드 스냅샷 2·archive 일회성 3·NoAI provenance README 1)은 고치면 안 되는 것이라 근거와 함께 남겼다(현역 v2_pipeline.py 는 scripts/ 쪽이고 스냅샷 import 0건 확인). 구조 감사는 D1.2 목적지가 semantic 컨테이너의 **손자**라 depth 3 까지면 안 보여 **depth 4 추가(233→277 entry)**; 파생 중 낸 실수 2개를 잡았다 — 분류 우선순위를 바꾸다 plan 분기를 죽여 RESIDUAL_DATASET_DIR 가 107 로 부풀었고(원복), 배경 원본 ZIP 3개가 depth 4 에서 `UNEXPECTED_TOP_LEVEL_ZIP` 오탐(원장 목록을 PRIOR_LEDGERS 전체로 넓히고 ZIP 규칙을 depth 1 한정으로) → 최종 UNKNOWN 0. filesystem_before 는 새로 재지 않고 D1.1 after 를 쓰되 **after+이동 역산==D1.1 after** 로 검증(mismatch 0). 검증: unit 745·integration 31·golden 51 skip 0, registry ok=28, 기존 원장 9종 failures 0, 신규 D12B/D12C failures 0, 5k 938f387d(4,313)/3cd365ee(4,439,12-12) 전후 동일, Blender no-render abs 0·missing 0·Dist_ 209, **재검증 후 원장 dirty 0(멱등성 유지)**. 잔여: 권장 구조 밖 200개(depth1 65 + archive depth2 135, 5.47GiB, 전부 Stage 2-A 계획 있으나 executed=no) · KEEP/quarantine 2 · 빈 폴더 7(삭제 금지) · BLOCKED 0 · UNKNOWN 0. 삭제 0 · ZIP 삭제/수정/해제 0 · weight 이동 0 · isaac_assets 이동 0 · NoAI USD 이동 0 · Blender 렌더 0 · 학습 0 · **commit 0 · push 0**. 상세 [2026-07-31.md](2026-07-31.md).
- 2026-07-31: data/pallet Stage 2-D2(레이아웃 정리 종료) — **D2_SCOPE_COMPLETE ✅ / FULL_DATA_PALLET_LAYOUT_POLICY_COMPLETE ✅ / FULL_PHYSICAL_MINIMAL_TREE ✅ / DATA_PALLET_CLEANUP_COMPLETE ✅**. Stage 2-A 계획에 destination 이 있으나 executed=no 로 남아 있던 물리 잔여를 최종 destination 으로 이동: **199 row / 23,284 파일 / 5,876,337,378 B(5.47GiB)**, hash-mode all·same-volume rename, pre 5.47 + post 5.47 = **10.95/16 GiB**, mismatch 0 · unhashed 0 · failures 0 · 삭제 0 · overwrite 0 · `data/pallet` 파일수 **363,090 불변**(bytes +150,544 = manifests 150,286 + exclusion 258 로 전액 특정). cohort 는 destination semantic root 로 유도 — D2_SUPERSEDED_RUNS 135(21,321파일/5,025MiB) · D2_LEGACY_DATASETS 64(1,963파일/579MiB). archive depth-1 **134 감소/0 증가**, top-level unexpected 0 · ZIP 0 · residual_after 0. ★**잔여를 세 자료로 재계산**(파일시스템 201 / stage2d12 final_tree 207 / archive.csv plan 183) 후 차이를 전부 규명: `B−A=8` 은 **D1.2 분류기 결함**(semantic 하위 container noai_baked·redistributable·snapshots·dataset_bundles 와 archive/README.md 를 RESIDUAL 로 세던 허수 — D2 에서 SUBCONTAINERS 규칙으로 수정), `A−C=18` 중 17 은 Stage 2-A 계획 수립 **이후** 생성된 v2 진단 run·로그라 row 가 없었고 계획된 동종 48건과 분류·destination 이 같아 D2 plan row 신설(plan_origin=D2_PLAN_ADDITION). ★**참조 실측에서 내 스캐너 오탐 145건을 잡았다** — join-form 패턴이 leaf 의 첫 세그먼트만 요구해 `os.path.join("data","pallet","archive","training_data")` 한 줄(merge_and_validate.py:18)이 archive/* 후보 120건에 전부 걸렸다. "모든 세그먼트 요구"로 고쳐 **145→14**. 실제 14건은 역할별로 전환(38 치환): **WRITE(--out/out_dir/DEFAULT_OUT) → runs/{diagnostics,eval}/ · READ(--dir/--records/DEFAULT_DIR/config input/docstring) → archive/superseded_runs/**. 출력 기본값을 archive 로 보내면 재실행이 아카이브를 오염시키고 옛 레이아웃이 되살아난다(D0.1 gen_palletobj_v1.py:569 함정) — 도구에 이미 EXPLICIT_EXCLUSIONS 로 v2_dryrun_audit 이 그 이유로 막혀 있었는데 D2 는 **함정 자체를 해소**하고 RESOLVED_EXCLUSIONS 에 기록(가드 삭제 아님). 전환 후 이동 대상 199건 live ref=0. 사용자 결정 3건: eval_results 는 옮기고 출력 기본값을 runs/eval 로 / isaac_assets·_noai_quarantine_usd 는 **둘 다 유지**(§12 "plan 과 라이선스 근거가 모두 있을 때만 이동" — plan row 없음, management allowlist 에 역할·참조·유지근거 명시) / 미계획 17건은 plan row 신설 후 이동. ★**도구 결함 1건 실측 검출**: 첫 verify 가 `failures 0` 을 보고했는데 원장 verified_at 이 199행 전부 비어 있었다 — `is_d1` schema 화이트리스트에 stage2d2.1 이 빠져 post-hash·verified_at 기록 경로를 통째로 건너뛴 것(선언≠실제). D2_SCHEMA_VERSION 추가 후 199/199 기록 + 재검증 SHA 불변(멱등) 확인. 신규 policy `stage2d2-layout-completion`(frozen plan SHA 결속·cohort 분할 거부·live ref 0 강제·제한라이선스 목적지 차단·policy container 이동 금지·중첩 source 거부 등 16조건), 기존 policy 6개 **무수정**. 신규 테스트 33개 → unit **745→778** skip 0, integration 31, golden 51. 빈 디렉토리 420개는 **STALE_EMPTY_SOURCE=0**(policy container 19 + 최종 구조 안 payload 하위폴더 401) 이라 삭제 0·이동 0, `archive/legacy_layout/empty_sources/` 미생성. exclusion 은 검증기가 **stale 4건을 실제로 잡아** 새 경로로 갱신(16/0/0/0), gitignored 이므로 tracked 재구축 명세 `distribution_exclusion_rebuild_spec.md` 신설. archive.csv **source 존재 승인 row 중 executed=no = 0**(잔여 executed=no 는 KEEP_QUARANTINE 1·ALREADY_MOVED_BY_PRIOR_STAGE 12·NO_SOURCE_ROW 3 로 final_status 명시). 검증: 기존 원장 12종 failures 0 · C2C chain x2 로 11 file(s) failures 0 · 5k 938f387d(4,313)/3cd365ee(4,439,12-12) · active scene 8cb4109a·abs 0·missing 0·Dist_ 209 · registry ok=28 · canonical broken ref 0(이동 직후 16 → 현재 문서 40 치환 후 0) · extended actionable 0. 삭제 0 · ZIP 수정 0 · 압축해제 0 · weight 삭제 0 · Blender 렌더 0 · 학습 0 · **commit 0 · push 0**. **폴더 정리 프로젝트 종료 — 추가 Stage 2-D3 없음.** 상세 [2026-07-31.md](2026-07-31.md).
- 2026-08-01: v2 논문용 pilot(seed 7000, public mask) — **PILOT_DIAGNOSTIC_COMPLETE ✅ / PILOT_2K_COMPLETE ❌ / SUITABLE_FOR_FINAL_PAPER_FIGURES ❌ / SUITABLE_AS_GENERATOR_BASELINE ✅ / SUFFICIENT_FIX_GENERATOR ✅**. 2,000장 목표였으나 **1,449장에서 안전 중단**하고 generator 병목을 확정했다(clean-static 400·cargo-only 400·context-rich 600 완료, controlled-occlusion 49/600). 신규: `run_v2_scene_logic.py --session-usable-cap K`(K장 전달 후 정상 저장·exit 0·`session_paused=true`, 기본 None이면 기존 동작 불변 — resume 자체는 `_resume_state()`+`iter_proposals()`로 **이미 구현돼 있었고** quota가 accept 시점에만 advance라 replay가 안전함을 코드로 확인), wrapper `run_v2_pilot_2k_repro.py`(registry로만 scene 해석·순차 세션·progress 미증가 시 중단·병렬 Blender 금지), 테스트 `test_v2_pilot_resume_reproducibility.py` 24개 → **unit 778→802 skip 0**. ★**exact20 재현성 게이트 PASS** — diagnostic-exact CPU로 20프레임 2회 독립 실행, FrameSpec/Plan/record/label/RGB/mask **전부 mismatch 0**. 1차에 mask 18/18 불일치가 났으나 FAIL로 보고하지 않고 파고들어 **PNG IDAT·numpy array_equal은 동일하고 tEXt의 Date/RenderTime/cycles.*_time만 다름**을 확인 — 지시 §6이 정규화 제외를 허용한 wall-clock·runtime과 정확히 일치해 이미지 판정을 파일바이트→**픽셀 내용**으로 정정(완화 아님, 파일바이트 결과도 병기). ★**mask profile을 full-audit으로 잘못 시작해 460장 폐기** — 지시문 고정설정은 full-audit이었으나 **과거 합의(`mask_profiles.py`의 public: mask_amodal/+mask_visible/ 2장·폴더 분리, changelog 2026-07-28 "public은 M1~M3 렌더 자체를 안 함")**와 충돌했고 그걸 알아채고 확인했어야 했다. 사용자 지적으로 중단→public 재시작, 구 출력 434MB는 지시로 삭제. ★**Figure 2(c)를 정본 overlay로 교체** — projection은 `frame_geometry()`/`project_cam_points()`를 재사용했으나 overlay 그리기를 직접 만들어 pose축·Pitch/Yaw/Roll 패널이 빠졌다 → `overlay_archive_trunc_style.draw_archive_style_overlay()`+`archive_metadata()` 호출로 교체(golden reference·51 golden 테스트가 고정하는 정본). 부수 수정: 화면 밖 cuboid가 썸네일 축을 늘리는 문제, ECDF symlog 판독불가(log+게이트 1e-4·1px 기준선), numpy 타입 canonical JSON 누출. **중단은 강제 종료 없이** — wrapper(python)만 종료해 다음 세션을 막고(subprocess.run은 job object 미사용이라 Blender 생존 [확인]), Blender에 AttachConsole+CTRL_BREAK(taskkill /F 미사용), records.jsonl과 progress.json이 **같은 시각 flush**되어 프레임 경계 정지 확인, 부분 산출물 0건. ★**감사에서 내 이전 판정을 정정** — 1차에 "cargo 실제 가림 326장(81.5%)"으로 적었으나 **public mask는 pallet 전용**이라 visible_fraction<1로 cargo 가시성을 추론한 것이 잘못이었다(지시가 명시적으로 금지한 추론). generator 전용 지표(front/left/right_visibility_after_cargo)로 재계산하니 **15장(3.8%)** — cargo는 팔레트 위에 놓이되 카메라 시선에서 가리지 않는다. 전수 감사: cargo-only 400(placed 87.2%·no-cargo 12.8%·실패 attempts median 50 max 114·수율 71.0% Wilson 67.2~74.6), context-rich 600(placed=visible 93.5%·absent 6.5%인데 **attempts=0으로 시도조차 안 함**, cargo가 대신 있는 프레임 13, solver 내부 실패 support 14,840·collision 3,145·occlusion_budget 1,977·수율 69.7%), controlled-occlusion 49(**기능은 정상** — placed 49/49·visible px>0 49/49·side target==actual 49/49·f_target 오차 median −0.003, 그러나 **분모별 수율 proposal 17.6%(13.6~22.5)/render attempt 27.2%(21.2~34.1)·비싼 reject 98/180=54.4%**, `realize_occluder`가 렌더 후 판정이라 reject runtime 4,936초 = accepted 2,445초의 2.0배, 남은 551장에 실측 약 44시간). 무결성 1,449 전수 이상 없음(연속·missing 0·duplicate 0·4종 파일 각 1,449·corrupt 0·visible⊆amodal 위반 0·magenta 0·>10m 0·path mismatch 0·reproj max 1.46e-11 px gate 통과). 재현성 lock: environment/code(16파일+diff patch)/scene(8cb4109a)/asset(1,379파일 3.13GiB 전수 SHA256)/command. sampler·gate·bin·거리상한·f_target·mode 분포 **전부 무변경**, generator 수정 0·재렌더 0·학습 0·40k 0. §10 replay probe·§11~16 Figure/EDA·§17~20 최종보고서는 **미실행**(controlled 49/600으로 분포가 치우쳐 지금 Figure를 확정하면 데이터를 잘못 대표). 데이터 1,449장은 삭제하지 않고 수정 전 baseline으로 보존. 상세 [2026-08-01.md](2026-08-01.md).
- 2026-08-01: v2 generator 수정 Phase G1–G3(mode semantics + controlled 효율) — **G1_MODE_SEMANTICS_COMPLETE ✅ / G1_CONTROLLED_EFFICIENCY_COMPLETE ❌ / G2_MIXED100_PASS ❌ / G3_REPRODUCIBILITY_PASS NOT_RUN / READY_FOR_NEW_PILOT ❌**. 의미가 빈 프레임이 usable 로 통과하던 결함은 **해결**(100장 smoke 에서 mode semantics 100/100), 남은 것은 controlled 생성 **효율**이다. ★**이전 진단 2건 정정**: (a) "`realize_occluder` 는 렌더를 마친 뒤 판정한다"는 **틀렸다** — 비싼 reject 94건은 `rendered=False` 로 RGB 를 한 장도 만들지 않았고 비용은 Blender 안 저해상도 탐색(explicit 3,118초)+그 앞 context 배치(1,424초)다. 그래서 수정 방향을 "렌더 앞으로 당기기"가 아니라 **"Blender 를 열기 전에 거르기"**로 잡았다. (b) "controlled target 오차 median −0.003"은 **explicit solver 정확도가 아니다** — `f_total_from_mask − f_target` 이었고 f_total 에는 cargo/context/static 이 섞인다. `f_explicit_actual` 은 baseline 49건도 신규 30건도 전부 None(public 은 마스크 분해 불가)이라 §15 정확도 게이트는 **BLOCKED** 로 보고(f_total 대체 금지 준수). **G1 수정**: §3 `USABLE_MODE_CYCLE` 로 10장 주기 2/2/3/3 interleave(총량 largest-remainder 그대로, n=1..250 전수 일치, records mode 20/500 무변경) — 어디서 멈춰도 prefix 가 대표성을 갖는다 / §4 cargo 자체 가시성을 context 와 **같은** `_lowres_holdout(only_white=...)` 로 측정(신규 4필드, 임시 PNG 는 finally 삭제, public 스키마 불변, hard gate `>0`) / §5 context `attempts=0` 39장의 원인을 **재현으로 규명** — `image_space_context_poses` 가 이미지 좌우 띠 픽셀→지면 교점으로 푸는데 저앙각에서 광선이 지평선 위로 가거나 교점이 `max_camera_distance=8m` 밖(후보 22,464개 기각 사유 64.2% camera_distance_out_of_band·22.5% ray_up) → 같은 물리 제약 유지한 **ground-ring fallback**(1차 sampler 성공 시 미발동, 기존 561장 무영향) / §6 `CONTROLLED_MODE_MAX_SKIPS` 초과 시 `f_target=0` plan 을 controlled 슬롯에 렌더하던 fallback 제거 / §7 **controlled feasibility prefilter** — `prepare_diagnostic_explicit_occluders` 가 nonce 0..640 후보를 6개로 고르기 **전에** 계획 기하만으로 거른다(side=center·bottom/높이 ∉[-0.60,1.90]·fill<0.45·실루엣/A_target<1.15·실루엣/A_pallet>22.0·band 밖; ML 아님·frame blacklist 아님·seed 무관) / §9 `mode_semantics_verdict()` 를 realize 안(렌더 전 `realize_ok=False`)과 `usable_conditions`(최종 record) **두 곳에서** 강제(None 은 통과 아님, short-circuit 없음). ★**prefilter recall 실측 49/49** — accepted 49건의 승리 후보를 하나도 버리지 않았고 accepted 프레임 탈락 0, 비싼 실패 프레임 12/94(12.8%) 를 **0초에** 조기 배제, 후보 pool 29,725→20,013(32.7%). 회귀 픽스처 `tests/fixtures/controlled_prefilter_winners.json` 로 고정(임계 바꾸면 `BaselineRecall` 이 먼저 깨진다). 신규 도구 5종(`audit_v2_controlled_prefilter.py`·`audit_v2_mode_semantics.py`·`audit_v2_bpyfree_determinism.py`·`audit_v2_dataset_quality_probe.py`·`build_v2_overlay_review.py`, 전부 CLI 인자), 신규 테스트 2파일 54개 + 기존 보강 → **unit 802→865 skip 0 fail 0**. **G2 100장 smoke**(seed 7000·dataset-quality·samples 64·mask public·magenta 0.0·Blender 1개, 세션 2회 exit 0, delivered 100/complete True/render_attempts 162/proposals 226): 통과 — mode 배분 20/20/30/30 정확·10장 블록 위반 0, semantics **100/100**(cargo placed 20/20·visible 20/20, context visible 30/30·**attempts=0 이 39/600→0/30**, controlled placed·visible·side match 각 30/30), 무결성 전 항목 0(reproj max 4.55e-13 px), public mask amodal 100·visible 100·M1~M3 0·임시 마스크 잔존 0. **미달 — §16 효율 게이트 3개 전부**: A(usable/proposal) 17.6%→**20.8%**(<35%), C(비싼 reject/attempt) 54.4%→**50.5%**(>30%), runtime ratio 2.02→**1.78**(>1.0). 셋 다 baseline 보다 개선됐으나 목표 미달 → §18 대로 **Phase G3 미실행**. ★**원인 전수 분해(비싼 실패 47건)**: bounded search 후보 기각 사유 누적 2,114회 중 **score_callback 48.8%**(놓을 수는 있는데 목표 가림률 미달) · candidate_budget_exhausted 23.0% · support 17.9%. 실패 vs 성공의 pre-realize 차이는 **투영 크기**(실패 med 0.300/p95 0.901 vs 성공 med 0.160/p95 0.528)와 카메라 거리(2.58m vs 4.97m)뿐이고, `candidates_after_prefilter` 는 실패 128 / 성공 123 으로 **후보 부족이 아니다**(실패는 탐색 상한 21회를 다 쓴다). 즉 계획 기하로 예측 가능한 실패가 아니라 prefilter 로 더 줄일 수 없고, 투영 크기로 프레임을 거르는 것은 분포 변경이라 금지. ⚠️ interleave 로 controlled 슬롯에 걸리는 proposal 자체가 달라져(baseline proposal 1,900~2,180 vs 신규 0~226) 단계 runtime 차이를 코드 변경 탓으로만 돌릴 수 없다. 다음 TODO: (1) 탐색 초기화를 `explicit_target_mask_stats` 로 시드해 score_callback 실패 공략, (2) explicit 탐색을 context 배치 **앞으로** 옮겨 실패 시 context 비용(med 21초) 회피, (3) 저해상도 `explicit_actual/error` 를 record 에 남겨 public 에서도 §15 게이트 계산 가능하게. 검증: unit 865·integration 31·golden 51 skip 0, registry ok=28, active scene 8cb4109a 불변, 5k FrameSpec 938f387d(4,313/687)·proposal 3cd365ee(4,439, 12/12) **불변**. sampler·gate·elevation/projected-size bin·거리 상한·f_target 분포·scene mode 분포 **전부 무변경**, baseline 1,449장 무변경, 500/2k/40k 0 · 모델 학습·추론 0 · **commit 0 · push 0**. 상세 [2026-08-01.md](2026-08-01.md).
- 2026-08-01: v2 controlled solver 개선 Phase G1.5/G2b — **G1P5_EXPLICIT_METRICS_COMPLETE ✅ / G1P5_ACCEPTED_RECALL_PASS ✅ (30/30, +회복 1) / G1P5_QUALITY_PASS ✅ / G1P5_RUNTIME_TARGET(-50%) ❌(+1.4%) / G2B_SEMANTICS·INTEGRITY·OVERLAY·QUALITY ✅ / G2B_EFFICIENCY PARTIAL / G2B_MIXED100_PASS ❌ / READY_FOR_NEW_PILOT ❌**. ★**직전 단계의 BLOCKED 를 해소했다** — public 프로필은 M1~M3 를 저장하지 않아 마스크 분해로 `f_explicit` 을 못 얻어 controlled 품질 게이트가 계산 불가였는데, 탐색이 **이미 찍는** holdout 두 장(`explicit_before_mask`/`final_mask`)의 **차집합**에서 숫자만 뽑아 해결(새 마스크 렌더 0·파일 저장 0·public 스키마 무변경). 신규 필드 11종(`explicit_metrics_available`·`explicit_target_pixels`·`explicit_actual_pixels_lowres`·`f_explicit_actual_lowres`·`explicit_abs_error_lowres`·target/actual centroid u,v·target/actual bbox)을 순수 함수 `scene_placement_v2.explicit_lowres_metrics()` 로 분리해 bpy 없이 테스트하고, **`f_total` 이 입력·출력 어디에도 없다는 것 자체를 테스트로 고정**. §3 controlled 단계를 `cargo→context→explicit` 에서 `cargo→explicit_prep→explicit→(성공 시)context` 로 재배열하고 `explicit_blocked` 이면 context 를 아예 시도하지 않게 함 — **실패 프레임의 context 낭비 1,151초→32초(-97%)**. ★**재배열이 만든 회귀 1건을 1차 replay(20건)에서 검출·수정**: `explicit_corner_reserve_pass` 는 배치 **전** 예약 계약(`ext_occ<=1`)이라 가리는 것이 본업인 occluder 를 먼저 놓자 모든 context 후보가 탈락(accepted 경로 context median 14.0→224.9초, 저해상도 렌더 52→351) → 배치 **후** 기준 `context_corner_no_regression`(V_inframe·V_vis 감소 없음 + ext_occ 증가 없음)으로 교체, explicit 없는 mode 는 기존 계약 유지, 테스트 6개로 고정. §4 target-mask-conditioned 정렬을 `target-seed` 로 **preprobe 바로 다음**에 배치(같은 offset 을 뒤에서 계산하던 구 `prealign` 은 중복이라 제거) → 승리 stage 가 prealign 16 → target-seed 21 로 이동. ★**locked controlled benchmark 신설**(`build_controlled_case_lock.py`·`replay_controlled_cases.py`·`audit_controlled_replay.py`) — 구 smoke100 의 controlled 77건(accepted 30 + expensive reject 47)을 **같은 seed·FrameSpec·Plan** 으로 재실행해 before/after 를 잰다. **replay 3회로 recall↔runtime 트레이드오프를 확정**: (1)target-seed 추가+구 prealign 유지 28/30·-23.3% (2)구 prealign 제거 28/30·-22.1% (3)**target-seed 를 후보 예산에서 제외 30/30·+1.4% ★채택**. (1)(2)에서 잃은 2건(proposal 113·166)은 둘 다 `candidate_budget_exhausted` — target-seed 를 앞으로 옮기며 proposal 당 예산을 먼저 먹어 gate-overlap/corner-contact 로 살아나던 프레임이 죽은 것이고, 구 파이프라인도 **같은 offset 을 같은 빈도로** 시도했으므로(위치만 뒤) 예산 회계에서 빼는 것은 원상 복구지 게이트 완화가 아니다. §5 가 "미달이어도 recall 우선"이라 (3) 채택. **G2b mixed100b**(신규 디렉토리 `v2_mode_semantics_smoke100b_seed7000_public`, 덮어쓰기 0, 세션 2회 exit 0, delivered 100/complete True/render_attempts 147/proposals 201): mode 20/20/30/30 정확·10장 블록 위반 0, **semantics 100/100**(controlled `metrics_available` 30/30), 무결성 전 항목 0(reproj 4.55e-13·incomplete 0), **overlay 100/100**(크기==해당 RGB·정보패널·축범례 전부), 품질 side match 30/30·abs_err med 0.0375→0.0434(+0.01 이내)·p95 0.1139→0.1139(+0.02 이내). 효율은 A 20.8%→**24.4%**(강한 기준 35% 미달) · C 50.5%→**48.1%**(30% 미달) · ratio 1.78→**1.33**(1.0 미달) · D 총시간 4,949→4,241초(-14.3%) · E context 낭비 1,151→44초(-96.1%) · F score_callback 1,338→1,723(+28.8%, recall 을 지킨 대가) · G usable 1장당 165.0→141.4초(-14.3%). **A·C 는 95% CI 가 크게 겹쳐**(A 0.150~0.282→0.177~0.327, C 0.406~0.605→0.374~0.589) 유의하다고 말할 수 없어 `G2B_EFFICIENCY=PARTIAL` 로 정확히 보고. ★**지시서 전제 1건 정정**: "overlay 640x480 원본 크기"는 성립하지 않는다 — 이 generator 는 프레임마다 해상도가 다르고(실측 640x480 48·720x480 17·960x540 27·560x560 8) 640x480 고정은 52%를 리사이즈하는 것이라 같은 문장의 "원본 크기"와 모순 → **native 해상도**로 만들고 감사 항목을 "overlay 크기 == 그 프레임 RGB 크기"로 변경. overlay 는 데이터셋(`…/overlay/`)과 보고서 두 곳에 100장씩, 정본 `draw_archive_style_overlay()`+`archive_metadata()` 만 사용하고 usable 확정 **후** 후처리 생성이라 generator semantics 무영향. 검증: unit 865→**888 passed**(skip 0 fail 0)·integration 31·golden 51·registry ok=28·5k FrameSpec 938f387d 및 proposal 3cd365ee(4,439,12/12) **불변**·active scene 8cb4109a 불변·**baseline 1,449 및 smoke100 의 records.jsonl SHA256 불변**. 남은 병목은 여전히 `score_callback`(누적 1,723, 투영 크기 큰 프레임에 집중 — 투영 크기로 프레임을 거르는 것은 분포 변경이라 금지). 다음: (1) target-seed 예산 면제를 **상한 있는 허용치**로(후보 수 median 16·max 24 인데 보호가 필요했던 2건은 8개만 씀) (2) `fine_eval_count` 가 대부분 0 인 fine feedback 활성화 (3) mixed100c 재측정 후 강한 기준 재판정 → 통과 시 G3(exact20). exact20 0 · 500/2k/40k 0 · 모델 학습·추론 0 · **commit 0 · push 0**. 상세 [2026-08-01.md](2026-08-01.md).
- 2026-08-01: v2 controlled solver Phase G1.6 (G2c·G3 미실행) — **G1P6_ACCEPTED_RECALL_PASS ✅(30/30) / G1P6_EXPLICIT_QUALITY_PASS ✅ / G1P6_POST_CONTEXT_PASS ✅ / G1P6_LOCKED_EFFICIENCY_PASS ❌ / G1P6_LOCKED_PASS ❌ / READY_FOR_NEW_500_PILOT ❌**. ★**score-gap 감사에서 전제 2건을 정정했다**: (a) **`score` 에는 임계가 없다** — 코드의 실제 수락 조건은 `side_match AND visible_px>=8 AND abs_error<=0.12 AND (G1 AND G2)` 라는 불리언 4개의 논리곱이고 `score` 는 랭킹용 음수 비용이다. 그래서 canonical margin 을 유일하게 임계가 있는 축에 뒀다(`0.12 − abs_error`). (b) **"score_callback runtime 1,723초"는 초가 아니라 개수** — 직전 보고서의 reject 카운트였고 코드에 해당 타이머가 없다(측정 가능한 초는 explicit 단계 시간 locked 3,496s / smoke100b 3,020s). 또 locked 77 모집단의 baseline 은 1,723 이 아니라 **2,026** 이다. ★**fine 이 손댈 수 있는 후보는 6.9%뿐** — 막고 있는 조건 조합에서 `target_error_ok 만` 199/2,892 이고 나머지는 side/코너/가시성이 함께 막아 좌표 미세조정으로 살아나지 않는다. near-miss gap 은 p25 0.0607 · median 0.1114 로 median 은 허용치의 1.9배. **§3 구현**: target-seed 앞 K개 **unique** 후보만 예산 면제(중복은 free 슬롯 재소비 안 함), record 5필드 + `--target-seed-free-cap`. **§4 구현**: 목표오차만 막고 gap ≤ threshold 인 후보 **1개**만 `FINE_MAX_EVALS=8` 안에서 coarse step 의 ±0.5배로 u/v→depth 순차 탐색(전체 grid 아님), record 11필드 + `--near-miss-gap-threshold`. 둘 다 기본값 None = G1.5 동작이라 **CLI 를 빼면 즉시 원복**된다. 테스트 31개 추가 → unit 888→**919**. ★**1차 sweep(6 config)이 실제로 측정하지 못한 것 2가지를 잡았다**: (a) **target-seed 상한은 proposal 단위**이고 per-proposal unique 후보는 실측 8(38 proposal)·7(1) — §2 의 "median 16 · max 24"는 프레임당 합계였다. 따라서 K=8·K=12 는 무제한과 동일(paid 0)이고 물리는 K=4 는 protection 6/7(proposal 113 손실)로 탈락 → **이 축은 소진**. (b) **fine 이 한 번도 평가하지 못했다**(triggered 4~6, eval 0) — fine 단계도 일반 예산 검사를 통과하게 돼 있어 **예산이 소진돼 실패한 프레임(=fine 이 필요한 바로 그 프레임)에서 항상 0개**였다. 자체 상한만 적용하도록 수정하고 테스트로 고정, 1차 sweep 의 p25/p50 비교는 무효 처리. 수정 후 확인 run 1회 추가(총 7 config) — **p50 은 p25 의 상위집합**이라 한 번으로 두 threshold 답이 모두 나오고, p25 가 같은 3건을 회복하면서 trigger 1건(margin −0.1106)과 8 eval 을 아껴 **K=8 · p25 채택**. **§7 locked 77 replay 1회**: 원 accepted recall **30/30**, accepted 31→**34**(expensive reject 회복 1→4), metrics/visible/side **34/34**, abs error median 0.0375→0.0405(≤+0.01)·p95 0.1139→0.1145(≤+0.02), mode semantics 34/34, 실패 프레임 context 낭비 32s, accepted median runtime 45.7→45.6s — **품질 hard gate 전부 통과**. 효율은 total runtime 5,020→**4,642s**(−7.5%, 기준 ≤4,518 **FAIL**) · score_callback **2,026→2,058**(기준 ≤1,465 **FAIL**) → §8 대로 **G2c·exact20 미실행**. 승리 stage 는 target-seed 21 · **fine 4** · gate-overlap 3 · primary 2 · corner-contact 2 · preprobe 2 로 §4 가 노린 near-miss 회복이 실제로 동작한다. ⚠️ accepted 34 중 1건(pi 136)은 context 배치 0(180회 시도·support 165 탈락)인데, 새로 회복된 controlled 프레임이고 controlled 의 semantics 계약은 explicit occluder 에 대한 것이라 gate 는 정당히 통과 — baseline 에 같은 사례 0건이므로 회귀가 아니라 신규 사례. **효율 미달의 원인**: target-seed 축은 소진됐고, fine 은 품질·수율을 올리지만 회복된 프레임이 최종 RGB 까지 렌더하므로 총시간을 늘린다(품질↔총시간 상충, 우선순위상 품질 우선). 검증: unit 919·integration 31·golden 51 skip 0, registry ok=28, 5k FrameSpec 938f387d·proposal 3cd365ee(4,439,12/12) 불변, active scene 8cb4109a 불변, **baseline 3종(1,449·smoke100·smoke100b) records.jsonl SHA256 불변**, public mask schema 무변경. 다음: 후보를 더 잘 고르는 게 아니라 **후보 자체를 덜 만드는** 방향(목표 마스크로 proposal 사전 랭킹, `corner_joint_pass` 를 배치 단계에서 미리 만족) — 범위 밖이라 자동 시도 안 함. mixed100c 0 · exact20 0 · 500/2k/40k 0 · 모델 학습·추론 0 · **commit 0 · push 0**. 상세 [2026-08-01.md](2026-08-01.md).
- 2026-08-01: v2 G1.6 §8 **재판정** (사용자 정정 기준 5가지, 재렌더 0) — replay 완료 후 코드·scene·process 무변경 상태에서 기존 record 만 재집계했다. ★**품질 정정**: explicit 품질을 legacy accepted **30건 paired** 로만 비교하니 abs error median **0.0360 → 0.0360**, p95 **0.1139 → 0.1139** 로 **완전히 동일**하고 값이 바뀐 case 는 1건(pi 18, 개선 방향, delta mean −0.000224)뿐이다. 앞서 "median 0.0375→0.0405" 로 적었던 것은 **회복된 4건이 섞인 비교**였고, 회복분은 원래 실패하던 프레임이라 오차가 커서 baseline 비교에 넣으면 품질이 나빠진 것처럼 보인다 — paired 로 보면 **품질 변화는 사실상 없다**. ★**score_callback 은 reject count** 이므로 gate 를 ≤1,722(count)로 적용: 2,026 → **2,058(+1.6%) FAIL**. gate 1,722 는 이 모집단 baseline(2,026)보다도 낮고, fine 은 near-miss 후보를 **추가로** 평가하므로 이 지표는 구조적으로 증가한다 — 이번 두 메커니즘으로는 도달 불가. 시간은 별도 지표로 보고: total_runtime 5,019.5→**4,641.6s(−7.5%, 기준 ≤4,518 FAIL)** · explicit_stage 3,495.7→3,191.2s(−8.7%) · context_stage 939.4→916.1s · **fine_runtime 61.2s(신규)** · accepted 분 2,127.0→2,149.4s(+1.1%) · **rejected 분 2,892.5→2,492.2s(−13.8%)**. ★**효율 변화는 전부 fine refinement 에 귀속** — `target_seed_paid_used = 0` 으로 K=8 이 한 번도 물리지 않았음을 실측 확인(per-proposal unique 최대가 8). fine triggered/evals/won **8/50/4**, 회복 [29,80,136,183](렌더 비용 구 295s→신 356s), accepted 31→34. 총시간 378초 순감의 출처는 rejected 분이다 — 예전에는 탐색 상한까지 다 쓰고 버려지던 4건이 fine 으로 일찍 성공한다. runtime 은 지시대로 **77건 전체**(회복분 최종 렌더 비용 미제외)로 비교했다. 최종: `G1P6_ACCEPTED_RECALL_PASS ✅(paired 30/30) / G1P6_EXPLICIT_QUALITY_PASS ✅ / G1P6_POST_CONTEXT_PASS ✅ / G1P6_LOCKED_EFFICIENCY_PASS ❌ / G1P6_LOCKED_PASS ❌` → 기준 5 대로 **G2c·G3 미시작**. 산출 `g1p6/{final_gate.json, locked77_report.md, locked77_quality.csv, locked77_quality_paired.csv, locked77_efficiency.csv}` 갱신. baseline 3종 SHA256 불변 · 5k digest 불변 · scene 불변 · unit 919 · **commit 0 · push 0**. 상세 [2026-08-01.md](2026-08-01.md).
- 2026-08-02: v2 controlled solver Phase G1.7 (binding 감사 + constraint-directed rescue) — **G1P7_BINDING_AUDIT_COMPLETE ✅ / RESCUE_READY ✅ / G1P7_MICRO_GATE_PASS ❌ / G1P7_LOCKED_PASS ❌(미실행) / G2D_MIXED100_PASS ❌(미실행) / G3_REPRODUCIBILITY_PASS ❌(미실행) / READY_FOR_NEW_500_PILOT ❌ / OFFLINE_CLOSURE_COMPLETE ✅**. 무인 12h 계약으로 실행(실소요 2.20h, 그중 Blender 1.57h = replay 3회). ★**acceptance 계약을 코드에서 직접 확정**(`v2_realize.py:1838-1843`) — `side_match AND visible_px>=8 AND abs_error<=0.12 AND (G1 AND G2)`, `score` 는 임계가 아니라 ranking 전용, G1/G2 는 boolean 이 아니라 **정수 margin**(V_vis−4, min(ext_occ−1, 4−ext_occ)), None 은 미측정이며 pass 아님. ★**instrumentation replay 불필요 판정** — §6 요구 23항목 중 20개가 기존 G1.6 log 에 있고, 없는 3개는 파생 가능/판정에 불필요/축 자체 부재. `replay_wall_s` 77건 합 4,754.3s 가 baseline 4,754.4s 와 일치해 CASE_WALL_TIME_S 로 쓸 수 있음을 확인(추가 replay 0회 절약). ★**binding signature**(rejected 43건, wall 기준): ONE_MISS_SIDE 10건 734.0s(28.7%) · ONE_MISS_G1 10건 651.2s(25.5%) · MULTI_CONSTRAINT 11건 421.6s · ONE_MISS_G2 3건 227.5s · **HARD_PHYSICAL_ONLY 0건** · 중복후보 128/2,918 은 전부 stage-crossing. case 201·229 는 explicit 이 **성공**했는데 `gate_fail:G3`/`G5` 로 죽은 것이라 rescue 분모에서 제외. §11 게이트는 A 54.2% · B 70.6% 로 **RESCUE_READY=true**. ★**그러나 게이트 통과 전에 절감 상한을 계산해 경고를 기록했다** — rescue 는 proposal loop 안에서 돌고 성공 시 남은 proposal 을 건너뛰는 구조라(case 당 3개), 엄격 near-miss 상한 **263.8s < 필요 475.4s**. 부족분은 side 구제에 달려 있는데 side 는 연속 margin 이 없다. **구현**: `CONSTRAINT_RESCUE_STAGE="constraint-rescue"`(기존 coarse "rescue" 와 이름 분리), constraint vector + Pareto dominance(통과분 margin 은 dominance 를 만들지 않게 해 통과축 밀어붙이기 방지), beam≤3(global best + SIDE/G1 champion), SIDE 는 **discrete reseed 4종**(판정기와 **같은** 삼등분 경계 재구성, left/right 목표점을 bottom 밴드 위로), 방향 부호는 이름이 아니라 **기존 후보쌍의 유한차분으로 실측**, G1 은 d(V_vis)/d(offset) 실측. `fine` 과 마찬가지로 **일반 후보 예산을 통과하지 않게** 했고(G1.6 교훈) evaluated-geometry set 을 공유해 중복 평가 0. **기본 mode=off 라 production 동작 불변**. 테스트 40개 추가 → unit 919→**959**. ★**실행 중 내 구현 결함 1건을 발견·수정하고 그 영향을 분리 측정** — `side_specific_lift` 가 bottom 분기에서만 실측 감도 대신 부호를 추측했다(§16 위반). 모든 side 에서 실측 공식을 쓰고 **감도가 없으면 세로 seed 를 아예 만들지 않도록** 고친 뒤 수정 전/후를 각각 24건 replay: seed 분포는 바뀌었으나(lift 14회→shift 5회) **rescue 성공은 1건으로 동일** — 실패 원인이 그 결함이 아님을 실측으로 분리. **§12 mechanism 24건 subset**(accepted 11 protection + rejected 13, baseline wall 1,606.1s, SIDE/G1 actionable 9건 704.5s = locked77 actionable 의 50.9%): Config A(beam2/eval6) **1,872.1s(−266.0s)** · Config B(beam3/eval8) **1,888.2s(−282.1s)** — 둘 다 절감이 아니라 **증가**이고 예산을 키운 B 가 더 나쁘다. SIDE/G1 actionable 절감 A −129.2s · B −149.1s (필요 **+211.5s**) → **§4 micro-gate FAIL**. 품질 gate 1-3 은 둘 다 **PASS**(protection recall 11/11 · paired median 0.0573 불변 · post-context regression 0) — **품질은 완벽히 보존되는데 시간만 늘어난다**. ★**구조적 이유**: rescue 는 기존 탐색이 실패한 **뒤에** 붙는 단계라 성공하지 못하면 비용이 순증하고, 성공해도 이득은 '남은 proposal 건너뛴 만큼'으로 제한된다(trig 12 / won 1). §12 대로 **locked77·G2d·G3 미실행**, 새 heuristic 추가 0 → **OFFLINE_CLOSURE**: failure_atlas 5종(side 11·g1 10·overlap 1·downstream_g3_g5 2·no_feasible 11·대표 RGB 12장) · rescue_upper_bound.md · next_design_options.md(설계안 3개, 구현 0) · experimental_rescue.patch 2,574줄 보존(git reset/stash 0). ★**남은 원인 3가지**: (1) `_occlusion_side_from_masks`(`v2_realize.py:3516`) 는 **가려진 픽셀 centroid** 위치로 side 를 정하고 **bottom 을 가장 먼저** 검사하는데 occluder 는 support 제약으로 접지해야 해 화면에서 세로로 못 움직인다 — SIDE actionable 11건 중 target side 미달성 **7건** (2) G2 는 고칠 여지가 가장 크지만(실패 1,152 중 1,028 이 ext_occ=0, 딱 1개만 더 가리면 됨) rescue 가 탐색 **끝**이라 예산 소진 후에야 시도 (3) acceptance 도달 전 탈락 **25.1%**(support 463·camera_clearance 157·collision 112)는 rescue 가 아예 안 건드린다. 다음 권장: next_design_options 후보 2(**ext_occ=0 조기 종료**) 의 **offline 판별 실험**(렌더 0회) — 절감이 순증하는 유일한 구조. 검증: unit 959·integration 31·golden 51 skip 0 fail 0 · registry ok=28 · 5k FrameSpec 938f387d(4,313)·proposal 3cd365ee(4,439,12/12) 불변 · active scene 8cb4109a 불변 · **baseline 3종 records.jsonl SHA256 불변** · locked77 G1.6 replay record 불변 · public mask schema(m0/m4 → mask_amodal/mask_visible) 불변 · blender 0 · HEAD 불변. checkpoint10 0 · mixed100d 0 · exact20 0 · 500/2k/40k 렌더 0 · 모델 학습·추론 0 · ADD/ADD-S 0 · **commit 0 · push 0**. 상세 [2026-08-02.md](2026-08-02.md).
- 2026-08-02: 라이선스 원장 갱신 — **★occlusion 선택 재배선 = 해소 판정**(렌더 0회, 문서 수정만). 사용자 질문("현재 저작권 문제 없는 것만 쓰나")에 답하려다 `_docs/dataset_license_ledger.md` 가 **코드보다 뒤처져 있음**을 발견: 원장은 "occlusion 선택 `DISTRACTOR_NAMES`(8)에 라이선스 불명 `Sketchfab_model`×3 이 남아 실 가림 렌더 오염"(07-24 서술)을 잔여 블로커로 유지 중이었으나, **재배선은 이미 완료**돼 있었다. 코드 근거 6종: v2 선택 소스=`distractor_pool_v2`(209 CC0/CC-BY manifest, `v2_realize.py:61`·`v2_pipeline.py:53`) / 선택 호출 `v2_realize.py:2554` / 209 풀에 `Sketchfab_model` 부재 명시(`distractor_pool_v2.py:19-21`) / 레거시 `DISTRACTOR_NAMES`=5종뿐(`config/synthetic/blender.yaml:35-41`) / `git grep "Sketchfab_model" -- scripts/ config/` = **주석 3곳뿐 선택 목록 0건** / 구 생성기도 209 전환(`gen_4pallet_mask.py:119`·`gen_dataset_v4.py:105`). ★**한정을 명시**: 확인 범위는 *선택 경로*(코드)이고 blend 내부 물리적 잔존은 미검증 — 선택 불가라 렌더 영향 없음 `[추정]`. ★**부수 발견 = 새 `?` 항목**: 레거시 5종(`Barrel_01`·`Barrel_1`·`concrete_road_barrier`·`TrafficCone_1`·`TrafficCone_2`)의 라이선스가 원장에 **기록 없음** — 이름상 Poly Haven CC0 같지만 근거 파일이 없어 추정으로 CC0 단정하지 않고 `?`(미검증)로 등재(v2 재생성 시 자연 소멸). 원장 6개 절 수정(BLOCKERS 헤더·블로커 표에 ★해소/?행 추가·그룹7 근거표·baked Sketchfab_model 정정·종합판정에 **"현재 생성/학습 단계 판정"** 절 신설·리스크 요약). **판정 변화**: 잔여 블로커 `B5·B6+★재배선` → **`B5·B6`(배포 시점 절차 2건)**, 생성 파이프라인은 오염 요인 없음. **실질 남은 작업은 불변** — B5(ATTRIBUTION.md 미생성)·B6(isaac_assets 배포 제외)·구 데이터셋 noai_baked 8종 v2 재생성. 근거 약한 잔여 2건(wood texture 9종 LICENSE 부재 / 레거시 5종)은 표로 별도 등재. 코드·scene·blender·commit·push 0.
- 2026-08-02: 검토용 라이선스 요약 문서 신설 — `_docs/asset_license_summary.md`(지도교수 검토·논문 데이터 절 근거용 1페이지). 원장/부록/동봉계획과 역할 분리, `_docs/` 내 배치(새 폴더 0). ★**URL 은 파일에 실재하는 값만 기재**(추측 URL 0): 팔레트 v2 3종=`pallets_v2_add/LICENSE.txt`(poly.pizza·blenderkit 실 URL) · distractor 209=`distractors_manifest.csv`(직접 재집계 gso 128/polyhaven 65/sketchfab 16, **url 결측 0**) · 배경 2=각 `license.txt` · HDRI 30=`SOURCES.txt` · Sketchfab 16=attribution appendix. **미보유·미확정은 그대로 표기**(P0 canonical URL 미보유, P1 은 후보 URL, wood 9종 LICENSE 파일 부재). 미사용·격리 항목(NoAI 팔레트 2·modern_city_block·isaac_assets·구 데이터셋 8종)을 별도 절로 분리해 "안 쓴다"를 명시. 공개 시 조치 4건 표(ATTRIBUTION.md 미생성 등) 포함. 코드·렌더·commit·push 0.
- 2026-08-02: wood 텍스처 9종 라이선스 **전수 검증 → provenance gap 종료**. 요약 문서의 "⚠️ 근거 약함" 을 실제 검증으로 해소. ★검증 전 실태: 폴더에 LICENSE/SOURCES **부재**(floor 는 둘 다 있음), `_tmp_ph/*_files.json` 실물 대조 결과 **6종만** 다운로드 artifact 존재하고 3종(brown_planks_03·weathered_brown_planks·worn_planks)은 history 서술뿐 — 원장은 9종 전부를 `[확인, 검증완료]` 로 태그했으나 **재현 가능한 artifact 기준으로는 3종이 비어 있었다**. 방법은 floor 14종과 **동일 기준 재사용**(`api.polyhaven.com/info/<slug>` → 200 + name 일치 + `type:1` = CONFIRMED CC0; 불일치·404 는 UNVERIFIED 로 남기도록 작성). **결과 9/9 CONFIRMED CC0 / 0 UNVERIFIED** — artifact 없던 3종도 전부 통과(저자: Rob Tuytel 4 · Dimitrios Savva 2 · Jenelle van Heerden+Matterfield · Dario Barresi · Amal Kumar · Rico Cilliers). floor 때와 달리 **교체 불필요, 파일 내용 무수정**. 신규 `textures_wood/{SOURCES.txt, LICENSE.txt}`(floor 포맷 동일 + "검증 전 상태" 절로 이력 보존), 원장 그룹6·종합판정 표 갱신(잔여 = 레거시 DISTRACTOR 5종 + P0 canonical URL 2건), `asset_license_summary.md` 에 **wood 9종 개별 URL·저자 표 추가**. 렌더·코드·데이터 변경 0, commit·push 0.
- 2026-08-02: 팔레트 P0/P1 **원출처 역검색 확정** + ★lambert16 텍스처 귀속 오류 발견. 내부 먼저 조사했으나 `_docs/history/` 는 2026-03-25 시작인데 USD mtime 은 03-04 라 **확보 시점 기록 자체가 없고** 원본 ZIP 도 아카이브에 없음 → 외부 Sketchfab API 역검색으로 전환. ★**판별자는 폴리곤 수**: 시드 4모델 실측(pallet 2606/herisuwardi71 426,540 faces · Plastic Pallet/billy3D 4,392 · Old Wooden Pallet/Luka Feric 3,024 **Free Standard** · Pallet/andree 2,160) vs 우리 USD 크기(P0 20.9MB · P1 0.33 · P2 0.17 · P3 0.57) → **P0 = pallet 2606**(유일 고폴리 ↔ 유일 대용량, 썸네일 형상도 카탈로그 P0 와 일치), **P1 = Plastic Pallet**(기존 후보 URL 이 폴리곤으로 뒷받침). **[강한 정황]으로 표기 — 바이트 동일성은 미확인**(Sketchfab 다운로드 로그인 필요), 확정 태그 남발 안 함. ★**부수 발견**: USD 바이트 스캔에서 `lambert16` 토큰이 **scene_2.usd(격리본)에만** 존재(`blinn1` 은 P1 에만) → `models_usd/textures/lambert16_*.png` 3개는 **P0/P1 상속이 아니라 격리된 P2 의 텍스처**이고, P2 가 andree(CC-BY)/Luka Feric(NoAI) 중 어느 쪽인지 미확정이라 **NoAI 텍스처 가능성 잔존** + `_DISTRIBUTION_EXCLUDE.txt` 에 `models_usd/textures/` 없어 배포 포함 + `randomizers.py:121` 이 lambert16 머티리얼 조회. **다음 단계 = Blender 정점수 대조(1,568 vs 1,120)로 1:1 판명** → CC-BY 쪽 격리 해제 + lambert16 귀속 종결 + B3 실측 확정. 원장·요약문서 갱신, 렌더·코드 0, commit·push 0.
- 2026-08-02: 검토용 요약 문서에 **전체 통합 표(§0)** 추가 — 팔레트만 보이던 것을 카테고리 전체(팔레트 6·distractor 209·배경 2·HDRI 30·floor 14·wood 9·실촬영 1,924·코드·렌더러)를 한 장에 담은 마스터 표로 확장. 종수는 전부 **실측 재검산**(manifest 행수·glob 파일수·디렉토리 열거). 라이선스 집계 = **CC0 120 / CC-BY 149 / 자체 IP 1+1,924장 / 비-CC 0**. 개별 URL 항목(팔레트 5·배경 2)은 직접 링크, 집합 항목은 URL 패턴+목록 파일 병기. 코드(NVIDIA SCL 비상업 한정)·Blender(GPL, 산출물 전파 없음)도 동일 표에 포함. 문서만 수정, 코드·렌더 0, commit·push 0.
- 2026-08-02: 렌더 속도 최적화 — **EEVEE holdout 을 production 기본값으로 전환 + 평가 前 후보 dedup + holdout_engine provenance 기록**. unit 959→**968 passed / 0 failed**. ★**EEVEE 전환**(`v2_realize.py:3235` `HOLDOUT_TUNING={"engine":"eevee"}`, CLI default 도 eevee): 같은 6 controlled 케이스 replay 로 **154.6s → 84.1s (1.84배)**, accepted 판정 6/6 동일. ★**최종 배포 마스크도 같은 `_render_holdout` 을 쓴다**(`:4188`)는 점을 코드로 확인 — 픽셀 14필드 전수 대조 결과 6슬롯 중 4개가 1~3px 변동(`mask_m0_area_px` 19,729→19,728 / 73,793→73,796, **0.005%**). 차이가 작은 이유에 근거 있음: `_render_holdout` 이 렌더 직전 **모든 재질을 순백/순흑 Emission 으로 교체**하고 world=None 이라 알파·투명 발산 경로가 없다(`:3194`,`:3254-3299`). clean/cargo/context verify 18검사는 diff 0. "탐색만 EEVEE·최종 마스크는 Cycles" 분리는 **하지 않기로 결정** — 최종 마스크가 holdout 호출의 6.4%뿐이라 속도 이득이 거의 같은데 코드만 갈라진다. **주의: 엔진이 다르면 마스크가 다르므로 이 커밋 전후 데이터를 한 데이터셋으로 합치면 안 된다.** ★**dedup**(`scene_placement_v2.planned_offset_key/dedup_candidate_offsets`): 기존 `candidate_geometry_key` 는 배치 결과가 있어야 만들 수 있어 constraint-rescue 한 곳에서만 쓰였는데, `(plan, offset)` 쌍은 결과를 결정하므로 **평가 前에** 거를 수 있다. 실측 중복률 999후보 중 52건(5.2%) — `primary` 11.1%·`prealign-primary` 20.6%(격자의 (0,0,0)이 preprobe 와 동일 지점), 나머지 stage 는 0. 예산 trim **앞**에서 걸러 빈 자리를 새 후보가 쓰게 함. 테스트 9종 추가. ★**provenance 빈틈**: `--holdout-engine` 이 어디에도 기록되지 않아 "seed 는 같은데 마스크가 다른" 사고를 추적할 수 없던 상태 → progress.json·**records.jsonl 프레임마다**·usable_manifest.csv 3곳에 추가(샤드 병합 시 progress.json 은 사라지므로 프레임 단위가 필수). ★**병목 특정**: `_render_holdout` 0.1057s 중 재질교체 4.5% + PIL 1.2% + **render operator 94.2%** → PNG/PIL 제거는 전부 없애도 5.7%. 거부 프레임 6,571s 중 81.7%가 `explicit_occluder_not_visible` 104건인데 그 정체는 "놓았는데 안 보임"이 아니라 **애초에 못 놓음**(visible_pixels 전부 0, `bounded_local_search_exhausted` 82). stage 분해상 탐색 70.5% + context 21.2%, **최종 RGB 몫 ~0** — 후보 1개 = 1.33s 이고 대부분이 holdout 렌더라 **후보를 싸게 만드는 EEVEE 가 정답**이었다. ★**기각 목록(전부 실측)**: CPU thread=6 **-2.2% 느려짐**(96스레드 경합 가설 기각) · GPU OIDN +5.6%지만 controlled 효과 0 + 최종 RGB 픽셀 변경 → 미적용 · proposal 3→2 불가(성공의 5.5%가 index 2) · proposal 0 신호 조기포기 불가(성공/실패 분포 겹침, n=20 vs 96) · **GPUOffScreen 은 `-b` headless 에서 생성 자체가 불가**(SystemError: GPU functions for drawing are not available in background mode) · Render Result 메모리 직독도 pixels 길이 0 · long-lived worker 와 context skip 은 **이미 구현돼 있었다**. ★**공정 병렬 sweep**(첫 측정은 조건별 seed 불일치로 폐기 후 재설계 — 같은 12 plan 을 stride 분할, 5조건 모두 동일 8장 확인): 1→**1.00x** / 2→1.58x / 3→**1.79x** / 4→1.75x / 6→2.08x, 프로세스당 효율 100→79→60→44→35%. **3에서 꺾인다**(6프로세스는 프레임당 2.05배 느려짐). GPU 는 병목 아님(util 15%·17W·VRAM 7.5/12.3GiB). ★**1만 장 24h = 불가 판정**: 필요 8.64 s/usable, 최선 조합(4프로세스+EEVEE)도 12.5~15.5 → **34.8~43h**. 밴드 최선값조차 미달이라 EEVEE 배수 불확실성과 무관하게 결론 불변. 대안 = 머신 2대(17~24h) / 이틀(35~48h) / controlled 30%→15%(1.35배, 연구 설계 결정이라 미실행). 디스크는 병목 아님(장당 647KiB → 10k=6.2GiB, 여유 1.3TiB). ★**샤드 병합·시각화 end-to-end 검증**: 20장 4샤드 중복 0건(mask_m0 해시·frame seed 전수), 병합 후 Figure 2장 생성 성공. **병합 함정 발견** — `idx` 만 재번호하고 `usable_id` 를 두면 figure 도구가 20장을 **5장으로 뭉갠다**. ★**내 오판 6건 정정**: 기준선 30.9→**55.1 s/usable**(pilot500 은 resume run 이라 세션 elapsed 를 누적 delivered 로 나눴음) · 거부 시간은 이미 `record` 안에 중첩 기록됨 · context skip 이미 구현 · 최종 렌더는 낭비 아님 · CPU 경합 가설 오류 · cargo/context 배치 낭비 아님. 렌더는 측정용 소량만(dist100 20장·A/B 64장·sweep 40장), **500/2k/10k/40k 데이터셋 생성 0 · 모델 학습·추론 0**. 상세 [2026-08-02.md](2026-08-02.md).
- 2026-08-03: **1만 장 production 생성 완료** + 팔레트 치수 랜덤화 + Figure 3 신설. unit 976 passed. ★**1만 장 = 16.0시간 · 5.78 s/usable · 5프로세스**(전수 감사 전항목 통과: 파일 4종 x 10,000 일치 · 0바이트 0 · 샤드간 해시/seed 중복 0 · 게이트 실패 0 · mode 20/20/30/30% 설계값 정확 일치 · 재투영 최대 2.29e-13px · 거부율 56.2%). 100장마다 Blender 재시작(20라운드)해 메모리 누수 대비 — **속도 5.50~5.85 등락, 단조 증가 없음 = 누수 징후 없음**(단 재시작 조건 하 관측이라 [추정]). nohup 분리 실행으로 16시간 무인 완주, 실패 샤드 0. ★**"24시간 불가" 판정 뒤집힘** — 3프로세스(9.31 s/usable) vs 5프로세스(6.92, **1.35배**, GPU util 16→33%, 전력 17→58W, VRAM 7.5→10.8GiB=88%) 실측. 6프로세스는 VRAM 88%라 비권장. ★**치수 랜덤화**(`sample_pallet_scale_ratio` sigma 0.10/절단 0.20, **균등 스케일만** 형상 유지, `_STAGES` 에 "pallet" 추가 — stage 이름 독립 해시라 기존 seed 불변): 조사 중 발견한 것이 더 컸다 — 에셋 4종이 **전부 KS T-11(정사각형) 아님**(P0/P1 1.20 · P2 1.18 · **P3 1.50**), 높이도 136~184mm 제각각. 내가 "비균등 스케일 버그"로 오판했다가 W/D 고유값이 각 2개뿐(자리만 교환)임을 확인해 **스케일 랜덤화가 아예 없었음**으로 정정. `get_normalized_scale` 에 `target_dims` 인자가 **이미 있어** 신규 구현 최소. ★**Figure 3 신설**(밝기 · 종횡비 · 납작함 · 씬 커버리지) + Fig1/2 레이아웃 결함 5건 수정 — 근본 원인은 `fig.text` 좌표 하드코딩(0.305)이라 figsize 바뀌면 몽타주와 겹침 → `gs[2,:].get_position(fig).y1` 로 계산. (b)(c)는 "종류별 막대"→"프레임 분포"로 교체하되 **KDE 미사용**(고유값 3~4개 이산이라 매끄럽게 그리면 없는 연속성을 지어냄). ★**재생성 전 고칠 문제 5건**: (1) 카메라가 팔레트 안 — projected_size 1.0 초과 983장(9.83%)·5 초과 17장·**최대 279.69**(거리 0.82m, 실물 확인 결과 판자만 가득) (2) bbox 최소변 10px 미만 94장 + **PnP 부적격 플래그가 계산만 되고 거부에 안 쓰임**(4cell 2,008장) (3) visible_kp **4개 265장**(PnP 최소치, 여유 0, 그중 bbox 1.7~2.9px) (4) **G5 여유 0**(luma_pallet 최소 12.04 vs 하한 12.0) — 단 밝기 자체는 문제 아님, 가장 어두운 프레임은 **역광 실루엣**이고 팔레트-프레임 차이가 모든 preset 에서 **−13.5 로 일정**해 재질 설계대로임(노출 상향은 금지 항목) → G5 12→18~20 상향이 해법 (5) **저앙각·근거리 편중**(앙각 중앙 17.6°/20° 미만 55% · 거리 1~2m 24.7% 최다 + **9~10m 상한에 10.4% 쌓임**). ★"앙각 양극단" 인상은 **Figure 1(c) 몽타주가 maximin(가장 특이한 12장)** 이라 생긴 착시 — 원시 히스토그램은 저앙각 단조 편중. **코드 미커밋** — 1만 장은 이 상태로 생성됐으므로 `reports/v2_prod10k/preflight/provenance_lock.json`(diff sha 92b9a077 + 파일 4종 해시 + config)에 고정. 상세 [2026-08-03.md](2026-08-03.md).
- 2026-08-04: **keypoint 가시성 정정(자체가림) + G1 근본 수정**. unit 991 passed. ★사용자 지적("9개가 다 보일 수 없다 · 중앙점 빼라")이 옳았다 — `visible_kp_count` 가 centroid 를 세고 자체가림을 아예 측정하지 않아 "9개 전부 보임"이 21.7% 나왔다(육면체 상한은 7). `SP2.self_visible_corner_mask` 신설(인접 3면 중 하나라도 전면이면 보임 -> 4/6/7개, 8은 불가능). ★**코너 순서 비의존**으로 구현 — canonical 순서와 perm_v4 가 함께 도는 파이프라인이라 면을 인덱스로 박으면 한쪽에서 반드시 틀린다. "최근접 3점 = 세 모서리"로 축을 기하에서 유도하고 순열 불변성을 테스트로 고정. ★**같은 버그가 탐색기 G1 에도 있었다** — `external_corner_gate_metrics` 의 v_vis 가 자체가림을 빼지 않아, 탐색기는 "G1 통과"라 렌더까지 했는데 라벨에선 4점 미만이었다(거부율 74%의 주원인). `self_visible=` 인자 추가로 정정하니 **거부율 74->64.5%, pnp 거부 30.2->16.5%, accepted 의 4점 미만 소멸**. ★이 방식이 최선인 이유: 제약(G1 >=4)이 **이미 있었고 계산만 틀렸다** — 고치면 탐색기가 "코너를 살리는 배치"를 스스로 찾는다. 사용자가 든 두 상황이 자동 처리된다: **0° 정면 뷰**(앞면 4코너 -> 평면 PnP 로 풀림)와 **중앙만 가림**(코너 생존)은 통과, 옆에서 코너를 먹는 배치는 렌더 전에 탈락. 별도 "center" 목표 불필요. 검토 후 폐기한 대안: 사후 게이트(렌더 낭비) · 앙각 40°+ 상향(★사용자가 원하는 저앙각을 없앰) · center side 신설. 추가: `PNP_MIN_VISIBLE_KEYPOINTS=4`(하한만 — 4점 근처 어려운 케이스는 남긴다) · `MIN_CAMERA_DISTANCE_M=1.5`+`proj_size_feasible_upper()`(하한이 없어 카메라가 팔레트 안까지 들어갔다: 화면크기 최대 **279.69 -> 1.19**) · ELEV_BIN_FRAC/V_FRAC 잠정 재조정 · Figure 2 에 **(c) 가시비율 x visible_kp** 교차표 신설(면적 가림과 keypoint 손실은 다른 축 — 가운데 가림은 코너를 남기고 모서리 가림은 없앤다). 300장 검증: 거부율 64.5% · kp 최소 4 · **kp 4개가 42.7%**(원하던 구간) · 앙각 중앙 17.6->24.6° · ★**우려한 부작용 없음** — 20° 미만이 39.3%로 저앙각 정면 뷰 유지. ★**내 폴백이 불가능한 값을 만들었다** — 3D 정보 결측 시 `[True]*8`로 채워 8이 1건 새어나왔고, 지어내지 않도록 `None`(측정불가)로 정정. Figure 도 상한 초과를 조용히 버리지 않고 축라벨에 노출. ★정정 4건: 앙각 상향 제안 철회 · "4점은 위험"(평면 PnP 로 풀린다) · 폴백 지어내기 · 속도 15.69 s/usable 오산(로그 ctime 기준, 실제 5.33). 상세 [2026-08-04.md](2026-08-04.md).
- 2026-08-04 (2부): 거리 분포 2~6m 집중(PROJ_SIZE_FRAC 규약 변경 + fx 조건부 선호, 43.7%->70.0%) · 팔레트 축별 형상 지터(종횡비 고유값 3->38) · ★obj.scale 누적 버그 수정(authored_scale) · 거부율 53.8%->40.0% · 1012 tests
- 2026-08-04 (3부): ★육면체 축 유도 버그 — "최근접 3점 = 세 모서리" 가정이 납작하고 긴 상자(=팔레트)에서 깨진다(면대각 0.807 < 모서리 1.125). 600장 중 7코너가 한 장도 없었다(정상 77%). visible_kp 과소계상 + G1 과다거부. `cuboid_axes_from_corners`(직교 유일조합)로 교체 · 회귀 6개(반례 도형으로) · Figure 3(b)(c) 이산/연속 자동분기 + KDE(대역폭은 원본에서 고정) · 1018 tests
