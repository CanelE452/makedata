# 2026-07-25


## [10:11:47] ⚠️  Auto Compact (컨텍스트 한계 도달)

- Working directory: `/e/CODING/GitHub/FoundationPose`
- Trigger: `auto`
- Session ID: `e0817866-5bc9-4b77-87e8-1e0742d2be7c`
- Transcript: `C:\Users\User\.claude\projects\E--CODING-GitHub-FoundationPose\e0817866-5bc9-4b77-87e8-1e0742d2be7c.jsonl`
- Note: 컨텍스트가 가득 차서 자동 트리거됨 — 다음 세션에서 이 마커 위쪽 작업을 반드시 복원할 것

**TODO (session resume)**: compact 이후 이 마커 위쪽의 작업 내역을 확인하고 이어서 진행

---

## Phase B3 완료 — v2 realize/measure/gate/label 구현 + ★3 필수확인 ALL PASS

v2 규약 구현 order 7(measure/실측배정)+8(게이트 G1~G5)+9(규약 realize측). 첫 실렌더 구간. 3d-expert 위임(세션한도로 1회 실패 후 재시도 성공).

### 신규/변경 파일
- **신규 `scripts/data_prep/blender/v2_realize.py`**(35705B, bpy측 전부): `realize`(L315), `measure`(L570), `render`(L505)/`render_post`(L516), `label`(L708), `safety_gates`(L677). 규약 wire — perm 방위각(`compute_perm_v4`, measure 내), Intrinsics DR `_set_camera_intrinsics`(L276, per-frame K+lens_mm), Illumination DR(exposure_ev + luma 측정 + dark→noise σ), 해상도 DR(per-frame resolution). CHECK 인프라: `force_distractors_v2_enabled`(L104), `_hide_ground_geometric`(L212, XY>10m & Zthick<0.3m & |cz|<0.15), `_corner_occlusion`(L536, per-corner raycast → occlusion_fraction[9]).
- **`v2_pipeline.py:1197-1226`**: realize/measure/render/label + safety_gates 4스텁이 **lazy import(`import v2_realize`)로 위임** → 순수 dry-run(bpy-free) 유지. py_compile OK 둘 다.
- **레거시 무손상**: `camera_effects.apply`에 `noise_scale=1.0` 선택인자만 추가(레거시 호출 무영향).
- **★실버그 수정**: solve_placement는 카메라를 팔레트 **centroid** 기준 배치하나 USD 팔레트 **origin≠centroid**라 방치 시 앙각 target 52.9° vs actual 39.3°(Δ13°) 오염. realize서 `delta=centroid−[0,0,H/2]`로 cam·occluder 정렬 → Δ+0.00. (C2 "미변경"과 별개인 정렬버그라 수정.) → Phase D elev target vs actual로 재검증 예정.

### ★3 필수확인 — 전부 PASS (실렌더 검증, 산출물 `data/pallet/_v2_b3_check/`)
- **check1 Distractors_v2 render-enable**: `force_distractors_v2_enabled`가 view-layer exclude 강제해제 실증(was_excluded_before=true→after=false, 209 roots). silent-0 리스크(exclude면 조용히 distractor 0개 렌더) 방지. **오케스트레이터 직접 검증**: `f02_overlay.png`에서 concrete_road_barrier 실물 렌더 + 9kp 팔레트 정합 육안 확인, `f02_distonly.png` occluder-only mask 47942px.
- **check2 mask_unoccluded 경로**: 전 프레임 area_unocc>0. f_cargo=1−|after_cargo|/|unocc|, f_total=1−|visible|/|unocc|, f_occ=f_total−f_cargo. 3-holdout(unocc/after_cargo/visible) 정합.
- **check3 바닥×glTF 마젠타 기하검출**: 이름 무매칭 새 지면(`WarehouseGroundTest_new`)을 기하로 hide, 마젠타 0.0→0.4947(임계 0.12). native floor 선택지도 구현. ★함정: 마젠타 검증은 top-down + Standard view transform 필요(oblique+Filmic이면 벽 가림·톤맵으로 마젠타 0).

### 게이트 실측(5프레임, report.json ground-truth)
- all_pass=true: frame 2(P0 V8/8 Vvis6 focc0.185 luma112), 3(P1 V4/6 Vvis4), 4(P2 V6/7 Vvis4). / 정상 폐기: frame 0(P3 Vvis3<4·luma3.9 → G1·G3·G5 fail), 1(P2 완전가림 visible0·luma0.09 → G1·G3·G5 fail).
- occlusion_fraction[9] per-corner raycast 정상(f02 rear 코너 4·7 가림→ext_occ 2). 실측배정: target≠actual이어도 actual bin(예 f3 V_target4→actual6).

### 유보(측정불가로 남김 — Phase D/별도머신)
- 주점 ±3% 지터: 파라미터(`principal_jitter`)만 두고 기본 0(렌더-K 정합 보장). per-frame K는 cx=W/2·cy=H/2.
- camera_effects blur∝해상도: 문서 스펙, 미적용(noise 스케일만).
- frame0 fire_hydrant occluder off-screen(dist_only_area=0) = occluder 배치 정밀도 → D에서 판단.
- C2 상승 = 지시대로 미수정.

### 다음: Phase D (진행중)
캘리브 200장 실렌더 → ①f_target vs f_actual 산점도+bin편차 ②elev target vs actual(centroid보정 검증) ③★occluder size_class 실사용(dry-run large 29.9% 유지?) ④side별 분포+f편차 ⑤게이트 통과율·폐기분포 ⑥계통편차면 보정계수 제안(f_target 분포 임의변경 금지). D 보고 후 파일럿 2k·v2 본렌더 = 사용자 승인.

---

## Phase D 완료 — 200장 캘리브 6종 분석 (실측)

렌더: `generate_accepted(200, seed=7000)` → 200/200 realize OK(0 실패), ~4.5s/frame·891s, solve pass-rate 0.797(51 reject/251). 파일럿·본렌더·commit 안 함. 산출물 `data/pallet/_v2_calib_200/`(rgb200·labels200·mask600·analysis/{a1~a5.png,phaseD_analysis_report.txt,phaseD_summary.json}·calib_records.json). 드라이버 `scripts/data_prep/blender/_v2_calib_200.py`+`_v2_calib_200_analyze.py`.

**6종 결과**:
1. **f_target vs f_actual**: total OVERSHOOT(저-bin, bias +0.32/+0.28) — 주범 cargo(독립 50/50축, on이면 ~47% 가림, f_target 아래로 못 내림=구조적, solve버그 아님). occluder-only UNDERSHOOT(median f_occ/f_need=0.00, 82% under 0.5x, mean −0.176). 판정=보정필요(occluder 한정).
2. **elev target vs actual**: Δ=actual−target mean+0.00000 std0.00000 max0 (전 200장). **centroid 보정 OK**. ★orchestrator 검증: 순환 아님 — realize:342·measure:581 둘 다 `get_pallet_geometry(실제 pobj)` centroid로 배치·역산, `_actual_elevation_deg`=atan2(Δz,horiz). 보정 전이었다면 Δ13° 검출됐을 측정. (단 배치-산수 검증이지 렌더 2D 재검출 독립검증은 9kp 투영이 담당.)
3. **★occluder size_class 실사용**(placed n=69): large 33.3%(23) vs dry-run 29.9% → **유지(+3.4pp)**. C2 원기준(large<29.9%) 미발동. road23.2·medium29.0·indoor7.2·small7.2. pool avail 대비 large 선호(22%→33.3%) 작동.
4. **side**(n=69): left33.3·right21.7·bottom43.5·center1.4(≪presc15, contained solve d_occ in-band 실패). 전 side f_occ bias −0.161~−0.197(일관 under).
5. **게이트**: ALL_PASS 78/200=39.0%. G1(Vvis≥4) 44.5%(저앙각 grazing, elev_bin0 62%→bin6 28% 단조), G3 30.0, G5(luma≥12) 24.0(dark exposure×floor), G2 17.5, G4 1.5. occluder frame pass 20% vs no-occ 49%. net yield≈solve0.797×gate0.39≈31%(N장당 ~3.2N 렌더).
6. **보정제안(미적용, f_target 분포 불변)**: occluder A_target×f_need/(f_need−0.176) 상향; 단 median0=scalar 불가, 근본은 occluder 2D 실루엣 정합(C2). cargo overshoot=cargo×f_target 축 결합 재설계 사안.

**판단재료(고치지 말 것)**: ①occluder-delivery median0·82% under → C2 2D정합 진짜 신호(large usage 아님) ②net yield 31% throughput ③cargo×f_target 저bin overshoot 구조적 ④G5×exposure_ev 24% 낭비. 에이전트 인사이트 `agents/3d-expert/memory/2026-07-25_v2_phaseD_calib200.md`.

**다음(사용자 결정 대기)**: C2 2D정합 지금 할지 / exposure 하한 조정+파일럿 2k 승인 여부. elevation 저앙각 낭비(G1)는 elevation 7-bin 잠정=meas 별도머신 소관.

**사용자 결정(AskUserQuestion)**: C2=보류(1번, large 33.3% 유지가 핵심성과·delivery 약함은 안전방향). 파일럿=진행(2번)하되 **★내 exposure 하한 조정 제안은 기각** — 사용자 교정: "노출 −3.0은 야간 도메인 커버리지 결정, 올리면 야간 HDRI 5종 커버 사라짐. G5 dark 폐기는 튜닝 아니라 버그(G5가 전체 luma 판정→야간 배경서 파렛트 보여도 폐기)." → G5를 파렛트영역 기준으로 수정.

---

## G5 dark 폐기 버그 수정 + f_actual 층화 배선 (사용자 지시)

3d-expert 위임(pre-change-check 원칙: 노출·elevation·f_target·임계 불변).

### ① G5 수정 (scripts/data_prep/blender/v2_realize.py, untracked)
- `measure()` L642-647: `luma_pallet` mask **unocc→visible**(`_visible.png`) — 실제 보이는 파렛트 픽셀 밝기로 판정.
- `safety_gates()` G5 L695: `(luma_frame≥12) AND (luma_pallet≥12)` → **`g5=(lp is None or lp≥12)`** (luma_frame 조건 제거). `G5_LUMA_MIN=12.0` 불변. lp=None(visible 0px)→G5 permissive, G3가 완전가림 폐기.
- **★orchestrator 정밀 검증**(200 calib visible mask 재계산): 에이전트 "net +0" → **실제 +2** 정정. old G5-only-blocker 10장 중 visible luma≥12로 회복 2장(idx38 vis12.5·idx131 vis12.5, 둘 다 unocc<12였으나 visible 밝음). ALL_PASS 78→80(39.0→40.0%). 미미하나 0 아님.
- **진짜 binding = G1(Vvis≥4, 저앙각 grazing 44.5%), G5 아님.** HDRI+global exposure라 어두운 프레임은 파렛트도 같이 어두워짐 → "어두운 배경+밝은 파렛트" 거의 없음. G5 완화 효과 +1%p 그침. 야간 손실 주범=저앙각(elevation 7-bin 잠정=meas 머신, 안 건드림).
- 누수 0: 안 보이는 파렛트 8/10 G3 폐기. outdoor-night 46% 생존 불변. render_post noise 어두운 bin 상향 작동(luma39→0.1, noise 1.52→2.50).

### ② f_actual 층화 배선
- quota=**target 기준**(`advance_quota(picks.fti=TARGET)`). 파이프라인 설계원칙=측정 하류량은 감사만·quota 강제 안 함(luma도 동일). 진짜 f_actual quota-feedback은 render trap(occluder under=C2 이슈).
- 구현: `v2_pipeline.f_actual_bin(f_total)`(cuts .10/.20/.35) + `v2_realize.label()`에 `f_actual_bin` 필드(실측배정). audit-by-actual: **bin3 ≥0.35 target15%→actual43%(cargo overshoot), bin2 target20%→actual9%(occluder under-deliver)** — target 집계가 숨긴 왜곡 노출.

### ③ 파일럿 2k — STOP(에이전트 판단, 타당)
전제(G5 yield 개선) 반증(G1이 binding, 수정 분포중립)·2k 단일 Blender ~2.5h·OOM 리스크(드라이버 mid-run restart 없음, 200만 검증)·큰 리소스 confirm. literal STOP 트리거(야간死·누수)는 미발동이나 +2%p 결과가 렌더 전 surface 가치. 전축 감사(perm·efront12kp·luma커버·접지·BVH겹침·도메인) 독립가치는 남음. → 사용자 결정 대기.
- 불변 확인 [확인]: `EXPOSURE_EV_RANGE=(-3.0,0.2)`·`ELEV_BIN_EDGES` 7-bin·`F_TARGET_EDGES/FRAC`·`G5_LUMA_MIN=12.0`. C2 미구현(occluder solve 무손상). commit 안 함.
- 잔류 blender.exe PID34104=사용자 GUI 세션(kill 안 함). 에이전트 인사이트 `agents/3d-expert/memory/2026-07-25_v2_g5_visible_luma_fix.md`.

---

## 파일럿 2k 렌더 → 전축 감사 → 마젠타 발견 → 분포 EDA → 진단 패키지 (2026-07-25 밤~26 새벽)

사용자 승인(감사 통과 후 40k는 별도 승인)으로 파일럿 2k 진행. 산출물 전부 `data/pallet/_v2_pilot_2k/`. commit/40k 안 함.

### 1. 파일럿 2k 렌더
- `generate_accepted(2000, seed=7000)` → 200장/청크 fresh Blender(OOM 방지, resumable). idx 0~1999 연속, solve_pass 0.8065. rgb 2000·labels 2000(DOPE camera_data+objects)·mask 6000(unocc/aftercargo/visible ×2000).
- **★사고: launcher 무한루프 중복 launch** — resume 오판으로 두 chained job이 각각 `while:` 무한루프 → 7루프 누적 + 동시 Blender OOM. ★TaskStop이 detached bash-loop+blender children 안 죽여 **process-tree 명시 kill** 필요. 데이터 무결(연속·구멍0·중복0, 139 double-render는 GPU 낭비만) — orchestrator가 프로세스 수·labels 연속성으로 실증.
- f1661 원본 rgb 손상(truncated PNG, 렌더/디스크 defect) 1장.

### 2. 전축 감사 (full-2000)
- [높음] perm 방위각 PASS(front_near 2000/2000, orchestrator 60-80° 오버레이 4장 눈검증: 0123 FRONT가 azimuth 추적, 고앙각 front_cos 음수는 설계의도). [높음] outdoor-night PASS(야간 G5 폐기 **22.2%→10.2%**, orchestrator 육안 lumaP>12 생존). [중간] 12kp/occ_frac(distinct10 연속)/luma(dark<35=34.3%) PASS. [표준] 처방=실측 max|Δ|≤1.5pp·reproj p99 0.0·front_near 100%·empty_mask 36(전부 gate-reject). 편차2(connector_cross 3·noise mismatch 2)=전부 gate-reject degenerate.

### 3. ★마젠타 배경 오염 발견 (전수 오버레이로만)
- 전수 오버레이(overlay_all/ 2000장, bpy-free 9kp cuboid) 생성 중 orchestrator가 육안 발견: **배경 마젠타(누락 텍스처) 74장(3.7%), 그중 45장 all_pass**(학습셋 5.6% 오염). 안전게이트(G1~G5)가 기하/밝기만 봐서 못 잡음. B3 마젠타 기하검출은 이 케이스(배경 에셋 텍스처 누락) 미포착. → magenta 게이트 추가 필요.

### 4. 전 DR축 분포 EDA (data-engineer)
- `dist_all/` 차트29 + report + panels(주제별4 + A/B/C 3, 총7). **처방축 전부 완벽 일치**(deficit-fill quota 강제, max|Δ|≤0.05pp). 편향은 실측·품질에서: f_actual bin3 42.6% vs 처방15%(+27.6pp)·occluder side center 2.0%(vs15)·luma>140 0.25%(미커버)·V_actual V=8 최빈(vs V6)·all_pass 40%(G1 43.8%·G3 31.8%·G5 10.5%). 품질: magenta 45 all_pass오염·배경 에셋 2종(industrial/parking_lot)뿐·material_family 전프레임 None.

### 5. mask 종류별 폴더 분리 (사용자 요청)
- calib_200: `mask/{unocc,visible,aftercargo}/f0000.png` 완료(각200·루트0). **pilot_2k: mv 6000 loop 2분 timeout으로 부분 이동 미완**(visible2000·aftercargo971·unocc0, 루트3029). 데이터 무결(총량 보존). → 일괄처리+접미사유지 필요, 미완 상태.

### 6. ★진단 패키지 (data-engineer) — `data/pallet/_v2_pilot_2k/diagnosis/`
- `pilot_frames.csv` 2480행×33열(rendered 2000 + **solve_reject 480 재현**, seed7000 조인 mismatch0) + README(게이트/솔버 실행순서 라인번호+인과표) + code/(v2_pipeline·v2_realize·blender_math·distractor_pool_v2·pallet_geometry+build_diag_csv) + **blender_mcp_onboarding.md 복사**(사용자 "한 폴더" 요청).
- solve reject: C2 172·d_occ_fail 112·v_below_min 99·C1_pallet 55·C1_cargo 40.
- ★핵심 인과(orchestrator CSV 재검증 [확인]): ①center occluder = d_occ 포함형 해가 1차 필터(632try→42valid) + C2 62%(26/42) 추가 드롭 → accepted 2.0%. ②통과율 40% 주범=과다 가림(G1 fail의 95.7%가 truncation 아닌 코너 가림, 순수 out-of-frame 38건뿐). **③f_actual +27.6pp = cargo 아니라 배경 벽/선반이 f_cargo로 오집계**(cargo_off인데 f_cargo>0 = 45.4% 실증. aftercargo mask가 배경 수직구조물 미hide, `_hide_ground_geometric`이 바닥슬래브만). EDA "cargo 주도" 정정.

### 7. 온보딩 md
- `_docs/blender_mcp_onboarding.md`(564줄) — 환경·파이프라인·파일구조(mask3종)·overlay(archive방식)·과거실패 40건·저작권(URL·미확인5종·B1~6)·함정 체크리스트. 다른 LLM Blender MCP 온보딩용.

### 미해결 / 다음 TODO
- **40k 본렌더 = 사용자 승인 대기**(파일럿 감사 통과했으나 결함들 수정 우선).
- 수정 후보: ①magenta 게이트(배경 오염 차단) ②★measure aftercargo에 배경 수직구조물 hide(f_cargo 오집계 #3) ③occluder center d_occ 포함형 밴드 완화 ④G1 저앙각 가림(elevation 재튜닝=별도머신) ⑤material_family 채움.
- pilot_2k mask 분리 미완(일괄 재개 필요). C2 2D정합 보류 유지. 노출·elevation·f_target 불변.

