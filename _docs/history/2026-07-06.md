# 2026-07-06

## 4개 USD 팔레트 마스크 데이터셋 (~1만 장) — 신규 태스크 시작

### 목적 / 판단 지표 (사용자 합의)
- **목적**: 추론용 단일 팔레트(`pallet_full.obj`)가 아닌 **4개 팔레트 전부**로, "가장 최근 방식"(마스크 png+COCO RLE / 오버레이 / 바닥 랜덤 / visibility gate / camera_effects) 데이터셋 **~10,000장** 생성.
- **분포(사용자 지정)**: `scene.usd`(Pallet_0) **50%**, `scene_1/2/3` 나머지 50% 균등 → `PALLET_WEIGHTS ≈ [0.5, 0.167, 0.167, 0.167]`.
- **전제**: `scene.usd` 머티리얼 `emissive_strength=10000` 먼저 수정 (안 하면 Pallet_0이 빛나서 사용 불가). 사용자: "포함 — 머티리얼 수정 후 4개 다".
- **방식**: 파일럿 테스트 → PASS면 질문/멈춤 없이 자동으로 대량 생성까지 진행.
- **판단 지표(프레임별)**: 팔레트 접지 / 겹침 없음 / 마스크 정확 / 오버레이 keypoint 정확 / visibility gate 통과 / camera-facing 0123 == max-facing.

### 조사 결과 (팔레트 실체 — "obj 4개" 오해 교정) [확인]
- 사용자는 "obj 4개(이름 1/2/3)"로 기억했으나, 실제로 팔레트 4개는 **OBJ가 아니라 USD**.
  - `data/pallet/models_usd/scene.usd`(20.8MB, Pallet_0) / `scene_1.usd`(0.33MB) / `scene_2.usd`(0.17MB) / `scene_3.usd`(0.57MB) — 4개 전부 존재.
  - 저장소 전체 `*.obj` 검색 → 팔레트 OBJ는 `data/palletobj/pallet_full.obj` **1개뿐**(단일 추론 팔레트).
  - 원본 glTF(`scene*.gltf`) + `data/pallet/pallet_scene/` 폴더는 현재 저장소에 **없음**(USD 캐시만 잔존). config(`config/synthetic/isaac_sim.yaml`)의 `gltf_files:[scene,scene_1,scene_2,scene_3].gltf` 경로도 실재 파일 없음.
- **"저번에 3개만 썼다" = 사실 확인** [확인]: v4 Blender 파이프라인이 `gen_dataset_v4.py:47-48` 에서 `cfg.PALLET_WEIGHTS = [0.0, 0.3333, 0.3333, 0.3333]` 로 **scene.usd(Pallet_0)를 가중치 0으로 제외**. `gen_preview10.py`, `gen_topview_test.py` 도 동일. 제외 사유(2026-06-16 기록): scene.usd 머티리얼 emissive 문제 + 파일 큼.

### 아키텍처 (두 파이프라인 병합 필요) [확인]
- **palletobj 파이프라인 = 원하는 "최신 형식"**: `gen_palletobj_scenarios.py`(`PALLET_NAME="pallet_full"` 단일, `_sandbox_palletobj_production.blend`에 baked) + `floor_and_mask.py`(holdout 마스크 png+COCO RLE, 바닥 랜덤) + `camera_effects.py`(RGB post) + occluder pool(19, blend 내장) + visibility gate. Blender 5.1 EEVEE 16spp 640×480. 드라이버 `run_mass_10k.py`/`run_trunc_addon.py`.
- **v4 파이프라인 = 4개 USD 팔레트 처리**: `gen_dataset_v4.py` + `randomizers.py:574`(`bpy.ops.wm.usd_import`) + `compute_annotation_v4`(4팔레트별 큐보이드/키포인트) + camera-facing 0123 + `FACING_MARGIN_MIN=0.15`.
- **병합 방향**: 최신 형식 모듈(floor_and_mask, camera_effects, gate)은 단일 `pal` 오브젝트 기반이라 어느 파이프라인에도 결합 가능. 어노테이션은 v4의 `compute_annotation_v4`(4팔레트 geometry 대응) 필수. → v4 백본 + palletobj 형식 모듈 결합이 자연스러움 (최종 설계는 위임 agent 판단).
- **공유 convention**: `compute_perm_v4`(blender_math.py) 2026-07-03 버그 수정 완료(4옆면 열거, camera max-facing==FRONT). 재생성 시 자동 반영.

### 진행
- 위임: `blender-pallet-synthetic-data-pipeline` agent (설계+구현+파일럿+self-verify). 상위(나)가 파일럿 이미지 직접 검증 후 스케일업 greenlight.
- TODO: (1) 병합 설계 (2) scene.usd emissive 수정 (3) weights [0.5,.167,.167,.167] (4) 파일럿 ~30-50장 (5) 마스크/오버레이/접지/겹침/facing 검증 (6) PASS → 10k 스케일업.

### 파일럿 PASS (agent + 내 독립검증 일치) [확인]
- 백본=v4(`gen_dataset_v4.py`→신규 `gen_4pallet_mask.py`) + palletobj 형식모듈(mask COCO+camera_effects). 드라이버 `run_4pallet_mask.sh`(flat dir, chunk별 fresh Blender, resume by png count, 절대경로). 원본 gen 스크립트/공유모듈 무수정.
- scene.usd(P0): 현재 synth_data_scene.blend에 이미 non-emissive(emis=0) baked → glow 없음. 방어적 `_kill_pallet_emission()` 추가. 렌더 확인=dark plastic 정상.
- agent 버그수정 2건: (1) render_holdout_mask가 `ob is pal`만 white→USD 팔레트는 pal=EMPTY wrapper라 마스크 전체 black→`render_holdout_mask_hier()`(wrapper+children) (2) bag 샘플러가 P0 weight0.5여도 ~33%로 눌림→`_weighted_pallet_index()`→P0 55%.
- 출력 `data/pallet/train_4pallet_mask_v1_pilot/` 40장. **내 독립검증**: 분포 P0=55/P1=15/P2=20/P3=10%, physical_audit 전수 grounded 40/40·penetration 0/40, 콘택트시트 육안 마스크실루엣·9kp정렬·P0 dark plastic 정상. 마스크 빈것 아님 확인.

### 10k 스케일업 착수 [확인]
- 명령: `bash scripts/data_prep/blender/run_4pallet_mask.sh --num_frames 10000 --out data/pallet/train_4pallet_mask_v1 --seed 4444 --batch_size 250` (RGB Cycles 24spp, ~2.5s/frame 실측 → 약 7h).
- **함정/교훈 [확인]**: 처음 `nohup ... &`+run_in_background 이중백그라운드로 실행→하퍼스가 런처만 추적(즉시 exit0)하고 실제 렌더는 orphan(추적X, 완료알림X). orphan 드라이버 체인(nohup bash 1551→드라이버 1552→blender 1630) `kill -9`+`taskkill blender`로 정리. 교훈: **run_in_background 안에서는 nohup/& 쓰지 말고 명령을 직접** 줘야 추적·완료알림 정상. `rm -rf <dir>`는 권한거부(파괴적)→`rm -f *.png` 등 파일 glob 삭제로 clean slate.
- 재실행: clean slate(파일만 삭제, dir유지) 후 `run_in_background`로 직접 실행(job b63b3dxxc). chunk0 진행 확인(40s에 9장). 완료 시 자동알림→최종 전수검증 예정.

### 10k 완료 + 전수 감사 PASS [확인]
- 완료: 01:53~14:53(~13h), 40 chunk 전부 exit0, gen.log DONE. png/json/mask/overlay 각 **10000**. 인덱스 0~9999 연속(누락 0), 4종 짝 100% 일치. 용량 **9.3GB**(overlay 4.2GB=QA용, 학습용 ~5.1GB).
- **전 10000장 감사(내가 직접, 표본=전수)**: 분포 P0 49.8% / P1 16.6 / P2 16.9 / P3 16.7% (목표 [50,16.7×3] 정확 수렴). 품질 전부 **0**: not_grounded 0 / distractor_penetration 0 / empty_mask 0 / front_is_camera_near=False 0 / connector_cross>0 0. vis<1.0 = 0 (전 프레임 9kp in-frame, 이 셋은 full-visible 게이트 — truncation 아님). ratio 적용 40.4%.
- 육안: 콘택트시트(파일럿) + 007000(P0 scene.usd) + 009999(P1 scene_1) 오버레이 = 큐보이드/키포인트/접지/마스크 정상. 처음~끝 품질 유지.
- 정리: `_smoke_4pallet` 파일 비움. 산출물 `data/pallet/train_4pallet_mask_v1/` (+ `_pilot` 40장, gen.log).
- **태스크 완료.** 남은 선택(사용자): (a) 데이터셋 zip 패키징(이전 패턴, 학습용 overlay 제외 가능) (b) 신규 스크립트 git 커밋(gen_4pallet_mask.py/run_4pallet_mask.sh — 현재 미추적) (c) Discord 완료알림 원하면 webhook URL 필요(미제공).
