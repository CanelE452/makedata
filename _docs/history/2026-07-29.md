# 2026-07-29 — data/pallet Stage 2-B: 현역 자산·golden reference 이동

기준 커밋 `0264ae4` (Stage 2-A.1) 에서 branch `chore/data-pallet-stage2b-active-assets` 분기.
commit/push 없음. 500/40k 렌더 없음. Stage 2-C 미시작.

## 목적

Stage 2-A/2-A.1 에서 만든 경로 registry + transaction 기반으로, 생성기가 실제로 읽는
현역 자산과 golden reference 를 `data/pallet/assets/` · `reference/` 아래로 옮긴다.
SHA256 전수 검증 + rollback 가능.

## 결과 한 줄

**10개 source 중 7개 이동(3,288 파일 / 1.711 GB, SHA256 mismatch 0), 3개 BLOCKED.**

---

## 1. 이동 전 감사에서 나온 결정적 사실

### ★ production `.blend` 가 `distractors/` 를 절대경로로 356건 참조 [확인]

Blender 5.1 headless 읽기 전용 감사(`blend_dependency_audit.json`, 603 datablock):

```
BLOCKED_ABSOLUTE   356   전부 E:\...\data\pallet\distractors\... (파일은 실재)
SAFE_RELATIVE      246   //textures/ 158 + packed 86 + generated 2
MISSING_CURRENT      1   factory_yard_2k.hdr -> C:\Users\User\Documents\GitHub\... (기존 파손)
libraries            0
```

→ `distractors/` 를 옮기면 씬 텍스처 356개가 끊긴다. `.blend` rewrite 는 금지되어 있으므로
**distractors 이동 BLOCKED**. 같은 이유(+ MISSING_CURRENT 1)로 **blender_scene 도 BLOCKED**.

**부수 발견**: 이 절대경로들 때문에 `synth_data_scene.blend` 는 **이미 이 머신에 못박혀 있다.**
다른 워크스테이션에서 열면 distractor 텍스처가 전부 깨진다. MISSING_CURRENT 1건이 그 증거로,
과거 `C:\Users\User\Documents\GitHub\...` 에서 작업한 흔적이다. Stage 2-C 최우선 과제.

**감사 스크립트 자체 버그도 잡았다**: `is_within()` 이 `os.path.commonpath()` 결과(백슬래시)를
forward-slash 정규화 문자열과 비교해 늘 False 였다. 처음엔 514건으로 보였고, `normcase` 만
쓰도록 고쳐 재실행하니 356건이 진짜였다(158건은 `//textures` 상대참조 오탐).

### `scene_noemit.usd` 의 절대경로는 실참조가 아니었다 [확인, pxr USD API]

텍스트 스캔에서 `scene_noemit.usd` 안에 `E:\...\models_usd\scene.usd` 문자열이 잡혀
models_usd cohort 를 막을 뻔했다. `Sdf.Layer` 로 확인하니
`GetExternalReferences()` / `GetCompositionAssetDependencies()` / `subLayerPaths` 가 **전부 빈 리스트**.
그 문자열은 `documentation` 필드의 주석("Generated from Composed Stage of root layer …")이고
레이어는 flatten 된 자기 완결본이다. → models_usd 이동 안전.

### `background/` 는 원본 ZIP 3개 때문에 막혔다 [확인]

`background/` 안에 `parking_lot.zip`(101MB) + `modular_buildings_industrial_area(.).zip` ×2(28MB)
가 있어 "ZIP 이동 금지" 규칙에 걸린다. glTF 자체는 내부 상대참조만 있어 무해했지만
규칙을 완화하지 않고 BLOCKED 로 뒀다. ZIP 을 `archive/packages/` 로 먼저 분리해야 한다.

---

## 2. 이동 정책 추가 (`manage_pallet_data_layout.py`)

```
--policy stage2a-runs           (기본, 하위호환) 기존 Stage 2-A 동작 그대로
--policy stage2b-active-assets  exact source->dest allowlist 10쌍, --hash-mode all 강제,
                                10GB 상한, 3D/HDR 허용(ZIP/weight 는 계속 금지),
                                LICENSE/SOURCES 는 blocker 가 아니라 **보존 검증 대상**
                                (source 에 있던 라이선스 파일이 destination 에 하나라도
                                 없으면 verify 실패)
--cohort / --only-source        cohort 별 manifest 분리
```

Stage 2-A 회귀 없음: 기존 원장 verify 146/6,921/failures 0, 트랜잭션 unit 39 passed.

## 3. 실제 이동

```
cohort  source                        -> destination                                   files    MB
──────────────────────────────────────────────────────────────────────────────────────────────────
B1      archive/textures_wood         -> assets/materials/pallet/textures_wood            27   314.9
B1      archive/textures_floor        -> assets/materials/floor/textures_floor            59   674.9
B1      archive/trunc_addon_v1_pilot  -> reference/golden_overlay/trunc_addon_v1_pilot  1210   283.0
B1      real_data                     -> reference/real_images/real_data                1924   154.5
B2      hdri                          -> assets/lighting/hdri/library                     33   199.0
B2      models_usd                    -> assets/pallets/models/models_usd                 21    79.6
B2      pallets_v2_add                -> assets/pallets/source/pallets_v2_add             14     4.9
──────────────────────────────────────────────────────────────────────────────────────────────────
                                                                                       3,288  1,711
BLOCKED: distractors(1,161) · background(77) · blender_scene(171)
```

verify: SHA256 3,288건 전수 일치 · license 6개 보존 확인 · failures 0 · overwrite 0 · 삭제 0.

**Stage 1 이 찾아낸 "이름은 archive 인데 현역" 3종이 이로써 전부 정상 위치로 갔다.**

## 4. ★ Stage 2-A 잠복 회귀 발견·수정 [확인]

Blender 내장 Python(3.13.9)에는 **PyYAML 이 없다.** `pallet_data_paths._load_raw` 가
`json.loads` 로 떨어지는데, Stage 2-A 에서 registry 파일 머리에 넣은 `#` 주석을 json 이
파싱하지 못해 **Blender 안에서 `blender_config` import 가 실패**하고 있었다.
Stage 2-A 이후 Blender 를 한 번도 돌리지 않아 드러나지 않은 것이다.
`_strip_hash_comments()`(줄 전체 주석만 제거, 줄 번호 보존, 값 안의 `#` 는 유지)로
json fallback 을 방어하고 회귀 테스트 2개를 추가했다. §15 감사를 하지 않았으면
다음 렌더에서 터졌을 문제다.

## 5. registry / 코드 / 문서 갱신

registry 8키 갱신(audit ok=21 missing=0). BLOCKED 3종의 6키는 옛 경로 유지.
config 3파일, 현행 v2 py 9파일, 테스트 3파일, 실행가능 legacy input 5파일,
문서 6파일(`data_pallet_layout`·`blender_mcp_onboarding`·`dataset_license_ledger`·
`CLAUDE.md`·`AGENTS.md` + data/pallet 로컬 README 4종).
history/report snapshot/path_map original_path 는 **수정하지 않았다**.

golden overlay 테스트는 리터럴 대신 **registry key 조회**로 바꿨고, integration 에
"로컬에서 skip 되면 FAIL" 단언을 추가했다.

## 6. `_DISTRIBUTION_EXCLUDE.txt` 복구

stale 5건을 `archive/` 실제 위치로 정정, 존재하지 않는 1건은 주석 강등,
ledger:25/70 의 **NoAI baked legacy 4종을 신규 추가**(이전엔 목록에 아예 없어 릴리스 시
그대로 들어갈 수 있었다). 신규 검증기 `scripts/data_prep/verify_distribution_exclusions.py`
— entries 10 / problems 0 / release leaks 0 / exit 0.

## 7. 검증 결과

```
registry audit          ok=21 missing=0 absent_optional=0
default unit            568 passed (566 -> +2), skip 0, fail 0
local integration        23 passed (20 -> +3), skip 0, fail 0
golden overlay           51 passed, skip 0
Stage 2-A 원장           146/6,921/failures 0, sha256 fe1adc26… **불변**
Stage 2-B manifest       B1 4/3,220 · B2 3/68, hash all, unhashed 0, failures 0
5k dry-run              FrameSpec 938f387d… 동일, accepted 4,313 / rejected 687 /
                        distractors 209 / NaN·inf 0
asset counts            A 27 · B 59 · H 1210 · I 1924 · C 33 · D+E 35 — 전부 이동 전과 동일
Blender no-render 감사   missing_images 1(이동 전과 동일) · Dist_ 209 · Pallet_0~3 ·
                        //textures 158 resolve · HDRI 30 load OK · floor 42 / wood 27 decode OK
dependency 재감사        broken 0 · EXTERNAL 0 (이동한 cohort 기준)
data/pallet             dirs +9 / files +20 / bytes +2.89MB = **전부 smoke run 산출물**
                        (기존 파일 소실 0)
```

## 8. 2-frame smoke

`data/pallet/runs/smoke/_stage2b_asset_smoke2_seed7200/` — seed 7200, usable 2/2, 133.9s,
public mask, dataset-quality, noise clean.
**필수 66항목 전수 검증 실패 0.** overlay 2장 + RGB 를 직접 열어 확인
(f0000 Pallet_1/industrial, f0001 Pallet_3/parking_lot). 마젠타·누락 텍스처 없음.
경로 검증용이며 수율·분포 판단에 쓰지 않는다.

## 9. 남은 Stage 2-C 항목

1. **`.blend` 절대경로 356건 해소**(`make_paths_relative` + 재저장) — distractors/blender_scene
   이동의 전제이자, .blend 를 머신 독립으로 만드는 작업
2. `background/` 원본 ZIP 3개를 `archive/packages/` 로 분리 후 폴더 이동
3. `blender_scene` 안 factory_yard_2k.hdr 깨진 datablock 정리
4. archive 대량 이동(legacy_datasets 87.7GB + packages 80.8GB)
5. `inventory.csv` → `grouped_inventory.csv` 개명
6. distractors GSO MTL 2건의 `map_object_normal` 결측(기존 상태, 이동 무관)

보고서 전문: `reports/data_pallet_cleanup/stage2b/`
(baseline · blend_dependency_audit · model_dependency_audit · active_asset_* ·
distribution_exclusion_audit · postmove_blender_asset_audit · smoke2_verification · final_report)

---

# 2026-07-29 (Section 2) — data/pallet Stage 2-C1: production `.blend` portable 화

기준 커밋 `e72a719`(Stage 2-B) 에서 branch `chore/data-pallet-stage2c1-portable-blend` 분기.
commit/push 없음. 500/40k 렌더 없음. Stage 2-C2 미시작. 데이터 폴더 이동 0.

> Section 1(Stage 2-B)의 경로·수치·설명은 소급 수정하지 않았다.
> 아래 §1 에서 Stage 2-B 의 수치 하나를 **정정**하지만, 그건 이 Section 의 새 관찰이다.

## 목적

production Blender scene 이 이 Windows 머신의 절대경로에 묶여 있는 문제를,
**원본 `.blend` 를 한 바이트도 건드리지 않고** 해결한다.

## 결과 한 줄

**원본 보존(sha256 46f436dc… 불변) + portable 사본 승격. 절대경로 229→0, missing 1→0, 구조 diff 0.**

---

## 1. ★ Stage 2-B 의 "절대경로 356건" 은 과다계상이었다 [확인]

원본을 Blender 5.1.1 headless 읽기 전용으로 재감사한 결과(603 datablock, 실행 전후 SHA256 동일):

```
verdict                      이번 감사   Stage 2-B 보고
──────────────────────────────────────────────────────────
BLOCKED_ABSOLUTE               228          356
SAFE_RELATIVE                  286          246 (= //textures 158 + packed 86 + gen 2)
SAFE_PACKED_OR_GENERATED        88            -
MISSING_CURRENT                  1            1
──────────────────────────────────────────────────────────
합계                           603          603
```

파일이 바뀐 게 아니다 — SHA256 은 `_docs/history/2026-07-26.md:10` 기록값과 지금까지 동일하다.
**Stage 2-B 가 커밋해 둔 `stage2b/blend_external_paths.csv` 를 열어 직접 세어 확인했다**:

```
Stage 2-B 의 BLOCKED_ABSOLUTE 356행 중
  filepath_raw 가 드라이브 절대경로   228
  filepath_raw 가 // 로 시작         128   <- 이미 blend-상대경로인데 ABSOLUTE 로 분류
```

예: `//..\distractors\small\gso__Clue_Board_Game_Classic_Edition\materials\textures\texture.png`.
Stage 2-B 는 감사 중 `//textures` 158건 오탐을 한 번 고쳤지만 `//..\distractors` 128건은
남아 있었다(두 보고서에 나오는 중간값 514 가 그 흔적: 158+356 = 286+228 = 514).

원시 문자열 기준 사실:

```
raw 가 // 로 시작          370   (packed 84 + non-packed 286)
raw 가 드라이브 절대경로    229   (data/pallet 안 228 + factory_yard 1)
raw 가 빈 문자열             4   (packed 2 + VIEWER 2)
```

**Stage 2-C2 에 주는 의미**: `distractors/` 이동 차단 사유가 "절대참조 356건" 이 아니라
**"상대참조 356건(기존 128 + 이번 228) rebase 필요"** 로 바뀐다. 폴더 이동이 여전히
`.blend` 수정을 요구한다는 결론 자체는 같다.

## 2. 신규 재사용 도구 3종

Stage 2-B 는 이 감사를 **커밋되지 않은 임시 스크립트**로 돌렸다. 다음 단계(2-C2 rebase)에서
같은 일을 또 해야 하므로 정식 스크립트로 만들었다.

```
scripts/data_prep/blender/blend_path_utils.py            bpy-free 순수 helper
  is_within / to_blend_relative / resolve_blend_relative / is_absolute_filepath /
  has_user_specific_prefix / escapes_root / sha256_file / build_mapping /
  assert_distinct_files / assert_candidate_not_present / assert_source_unchanged /
  assert_only_planned_changes / decide_missing_datablock

scripts/data_prep/blender/manage_blend_external_paths.py Blender 내장 python
  --audit / --plan / --apply-candidate / --verify
  안전 규약(코드로 강제): source==candidate 거부 · candidate overwrite 거부 ·
  apply 는 bpy.data.filepath==candidate 일 때만 · source 를 연 상태에서 저장 API 미호출 ·
  pack/unpack/make_paths_relative/make_paths_absolute 전체 호출 0 ·
  실행 전후 source SHA256 대조 · 계획 외 변경 시 저장 전 실패 · strict 잔존 게이트

scripts/data_prep/blender/audit_blend_assets.py          no-render 자산 감사 (Stage 2-B 임시본 정식화)
```

### ★ 단위 테스트가 실제 버그를 잡았다 [확인]

`tests/test_blend_external_paths.py` 43개를 먼저 쓰고 돌렸더니
`test_posix_root_is_absolute` 가 FAIL 했다. **Python 3.13 에서 `ntpath.isabs` 가 바뀌어
선행 슬래시 하나짜리(`/home/u/a.png`)를 더 이상 절대경로로 보지 않는다**(드라이브 상대경로 취급).
Linux 에서 저장된 `.blend` 를 Windows 에서 감사하면 POSIX 절대경로를 상대경로로 오분류할
뻔했다. `is_absolute_filepath` 에서 드라이브 표기·선행 슬래시를 직접 판정하도록 수정.

## 3. factory_yard_2k.hdr 판정: REPOINT_EXACT

```
users=1, referenced_by = world:World/환경 텍스처   (world 1개·scene 1개 -> 활성 world)
  -> "미사용 datablock 제거" 경로는 성립하지 않는다

후보 검색 (data/pallet 전체 / repo / registry hdri_root / Stage 2-B 이동 전 기록)
  -> 정확히 1건: data/pallet/assets/lighting/hdri/library/factory_yard_2k.hdr

결정적 증거: 그 파일의 sha256 71d5b32c0e179f5b… 가
  reports/data_pallet_cleanup/_inventory_raw.csv:2614 의 이동 전
  data/pallet/hdri/factory_yard_2k.hdr sha256 과 **동일**  (basename 우연 일치가 아니다)
라이선스/출처: LICENSE.txt CC0 + SOURCES.txt (polyhaven.com/a/factory_yard, Sergej Majboroda)
decode: Blender 5.1.1 에서 hdri_root 30/30 성공
```

**렌더 동작 변화 없음**: `v2_realize.py:73 CONSTRAINED_HDRI_EXCLUDE` 와 v4 계열 `_DROP` 이
factory_yard 를 이름으로 계속 제외한다(constrained pool = 28, 전후 동일).
즉 이 작업은 un-drop 이 아니며 `blender_mcp_onboarding.md:598` 의 미검증 항목은 그대로다.

부수 관찰: `randomizers._collect_hdri_images()` 는 disk 로드 시 decode 실패를 거르지만
(L1252-1259) 바로 뒤 L1262-1266 "Also include any already-loaded HDRIs" 에서
`filepath` 에 `.hdr` 이 든 datablock 을 **다시 pool 에 넣는다**. 현행 v2 는
`_prepare_constrained_hdri_pool()` 이 `_hdri_cache` 를 선점해 이 경로를 타지 않아 실害는 없다.
(`2026-07-26-v2-attempt-log.md:107-108` 의 "no pixels skip" + "C:\Users\… 못 찾음" 근원이 이 datablock.)

## 4. candidate 생성 → 승격

```
1. stable/candidate 부재 확인
2. shutil.copy2 로 복사 후 SHA256 == source 확인 (byte-identical)
3. candidate 만 Blender 로 열기 (bpy.data.filepath == candidate 검증 후 진행)
4. 계획 228건 filepath 재작성 + repoint 1건, 제거 0건
5. 저장 전 게이트 전부 통과 후 save_as_mainfile(compress=True)
   (원본 헤더가 zstd 28b52ffd 임을 확인하고 압축 방식을 맞췄다)
6. Blender 종료 -> 새 프로세스에서 재개방 검증
7. same-volume rename 으로 stable 승격, rename 전후 SHA256 동일
```

```
파일                                          크기          sha256      mtime
────────────────────────────────────────────────────────────────────────────────────────
synth_data_scene.blend          (rollback) 358,917,479  46f436dc…  2026-07-24 19:39:00.380291100
synth_data_scene_portable.blend (active)   358,898,907  5cad94e5…  2026-07-29 16:56:38.267575100
```

## 5. 재개방 검증

```
항목                     원본     portable   기대
─────────────────────────────────────────────────────
image datablock           603       603     동일
절대 외부경로              228         0        0
사용자별 절대경로            1         0        0
missing path               1         0        0
//textures resolve       158       158      158
//../distractors resolve 128       356   128+228
packed                    86        86     동일
mapping SHA256 대조        -    228/228  mismatch 0
계획 외 변경                -         0        0
구조 diff                  -         0        0
```

구조 diff 0 — scenes 1 / view_layers 1 / collections 2 / objects 1,122 / meshes 597 /
materials 350 / node_groups 1 / worlds 1 / cameras 1 / lights 1 / images 603,
이름 집합, active scene, BLENDER_EEVEE 640x480, Filmic·Medium Contrast·gamma 1.05·sRGB,
카메라 matrix_world(9자리)까지 전부 동일.

## 6. no-render 감사 + 2-frame smoke

```
no-render (원본/candidate/승격 stable 3회)
  registry missing 0 · Pallet_0~3 · Distractors_v2 · Dist_ root 209 · manifest 209
  image missing 0 · 절대경로 0 · node image 누락 0 (마젠타 원인 후보 0)
  HDRI 30/30 (v2 pool 28) · floor 42/42 · wood 27/27

smoke  data/pallet/runs/smoke/_stage2c1_portable_blend_smoke2_seed7210/
  seed 7210 · usable 2/2 · public · dataset-quality · noise clean · OPTIX · 181.45s
  필수 87항목 실패 0
  f0000 Pallet_2 / parking_lot / dirt_ground / brown_dry / context-rich / 4.38m / m0 3,308px
  f0001 Pallet_1 / parking_lot / gravel_concrete_02 / ind_blue / controlled-occlusion
        (Dist_utility_box_01) / 2.32m / m0 17,628 m4 13,255
  magenta 0(record+픽셀 실측) · visible ⊆ amodal 위반 0px · overlay canvas == RGB
  RGB 2 + overlay 2 직접 열어 확인
```

### 검증 과정에서 내가 틀렸던 것 (기록)

```
증상                          원인                                    조치
──────────────────────────────────────────────────────────────────────────────────────
mask 면적 402,393px(비현실적)   전경을 >0 으로 셈. 실제 배경값이 1~2       파이프라인 정본
                                                                     strict_decode_mask(>127)
                                                                     을 import 해서 사용
G1~G5 를 못 읽음               필드명이 G1_pass..G5_pass               실제 스키마로 교체
hdri/material 이 None          records 가 아니라 label 의 v2_labels ·   실제 위치로 교체
                              camera_data 에 있음
mask_paths 비교 실패           dict 인데 list 로 가정                   키 집합 비교
```

**HDRI 메타데이터**: v2 라벨 스키마에 HDRI 파일명 필드가 **없다**(label/records/driver_summary
전수 검색 결과 `hdri` 문자열 0). 조명 메타는 `scene_preset` + `exposure_ev` 이며 둘 다
non-null 확인. 없는 필드를 만들어 넣지 않았다.

## 7. registry / 코드 / 문서

```
registry   production_scene -> …/synth_data_scene_portable.blend
           production_scene_rollback_source -> …/synth_data_scene.blend  (신규)
           experimental_scene 등 나머지 변경 없음.  audit ok=22 missing=0

runtime    run_dataset_v4.sh · run_4pallet_mask.sh · run_pilot_2k.sh
           -> SCENE 을 리터럴 대신 `pallet_data_paths.py --key production_scene` 로 조회
              (run_pilot_2k.sh 는 머신 절대경로 3줄도 ROOT 변수로 정리)
docstring  run_v2_scene_logic.py(2) · gen_dataset_v4.py · gen_4pallet_mask.py
docs       _docs/data_pallet_layout.md · _docs/blender_mcp_onboarding.md(4곳) ·
           scripts/data_prep/efront_calibration/README.md
test       unit 3 신규(portable 이 active / rollback 키 / "_candidate_" 금지)
           integration 3 신규(basename 검증 / candidate 아님 / **원본 sha256 46f436dc… 고정**)
미수정      _docs/history/* · reports/ snapshot · Stage 2-A/2-B manifest ·
           path_map original_path · `_` 진단 스크립트 docstring 예시
```

새 portable 경로 리터럴을 여러 곳에 다시 박지 않았다 — 전부 registry 조회다.

## 8. 승격 후 회귀 검증

```
registry audit          ok=22 missing=0 absent_optional=0
default unit            614 passed (568 -> +43 helper +3 registry), skip 0, fail 0
local integration        26 passed (23 -> +3), skip 0, fail 0
golden overlay           51 passed, skip 0
Stage 2-A 원장           146 / 6,921 / failures 0, sha256 fe1adc26… **불변**
Stage 2-B manifest       B1 4/3,220 · B2 3/68, hash all, unhashed 0, failures 0
5k dry-run (FrameSpec)   accepted 4,313 / rejected 687 / 209, sha256 938f387d…
                         덤프 파일이 before/after **byte 동일**
5k dry-run (proposals)   digest 3cd365ee… 동일, accepted 4,439, 12/12 PASS, NaN·inf 0
final no-render(stable)  전 항목 통과
원본 보존                 sha256·size·mtime 전부 작업 전과 동일
```

> 5k dry-run 은 두 하네스가 서로 다른 값을 낸다(`v2_pipeline.py --dump` = SAMPLE-ONLY,
> accepted 4,313 / 938f387d… · `dryrun_v2_proposals.py` = ACCEPT-TIME quota, 4,439 / 3cd365ee…).
> 지시서가 기대한 값은 앞쪽이고, 뒤쪽 4,439 는 저장소에 이미 커밋된
> `reports/v2_revision/dryrun_5k_checks.json` 과 동일한 원래 값이다. 둘 다 재현·대조했다.

## 9. 잔존 / 다음 단계

```
남음    distractors 이동 시 //..\distractors\ 356건 rebase
남음    blender_scene 이동 시 //textures\ 158 + //..\distractors\ 356 rebase
남음    background/ 원본 ZIP 3개(157MB) 를 archive/packages/ 로 먼저 분리
남음    packed 17건이 옛 AppData\Local\Temp 경로 문자열 보유 — 전부 packed(픽셀 내장)라
        inert 하고 원본과 완전 동일. §6 "packed image 수정 금지" 에 따라 손대지 않았다.
정리    Blender 가 만든 백업 synth_data_scene_portable_candidate_20260729.blend1
        (원본과 동일 바이트). 삭제하지 않았다. Stage 2-C2 정리 대상.
[추정]  상대경로 구분자가 Windows 백슬래시(`//..\distractors\`)다. 기존 158/128건과 같은
        표기라 파일 내부는 일관적이지만, 다른 OS 에서 여는 경우의 구분자 처리는
        Blender 쪽 동작이라 이번에 검증하지 않았다.
```

보고서 전문: `reports/data_pallet_cleanup/stage2c1/`
(baseline · pre_audit_summary · distractor_path_plan · factory_yard_decision ·
candidate_apply_log · post_audit_summary · structure_diff · candidate_no_render_audit ·
smoke2_verification · promotion_report · rollback_plan · final_report)

---

# 2026-07-29 (Section 3) — data/pallet Stage 2-C2: 최종 레이아웃 이동 + blend rebase

기준 커밋 `26f2194`(Stage 2-C1) 에서 branch `chore/data-pallet-stage2c2-final-layout` 분기.
commit/push 없음. 500/40k 렌더 없음. Stage 2-D 미시작. 데이터 삭제 0.

> Section 1(Stage 2-B) · Section 2(Stage 2-C1) 의 경로·수치·설명은 소급 수정하지 않았다.

## 목적

Stage 2-C1 의 portable 씬을 기반으로, 루트에 남아 있던 세 자산군
(`distractors/` · `blender_scene/` · `background/`)을 최종 `assets/` 구조로 옮기고,
옮긴 뒤 blend 의 상대경로를 새 위치 기준으로 rebase 한다.

## 결과 한 줄

**1,411 파일 / 6.09GB 이동(SHA256 mismatch 0), 상대경로 357건 rebase, 구조 diff 0.
`data/pallet` 루트에 남은 자산군 없음. 앞선 blend 2개는 수정 없이 rollback 으로 보존.**

---

## 1. 이동 결과

```
cohort  entry  source                     -> destination                              files   bytes
──────────────────────────────────────────────────────────────────────────────────────────────────────
C2A     file   background/*.zip 3         -> archive/packages/background_sources/         3   157,408,367
C2B     dir    background/                -> assets/scenes/backgrounds/background/       74   133,646,354
C2C     dir    distractors/               -> assets/distractors/library/               1,161 1,958,754,064  ┐ 같은
C2C     dir    blender_scene/             -> assets/scenes/production/blender_scene/     173 3,836,556,170  ┘ group
──────────────────────────────────────────────────────────────────────────────────────────────────────
                                                                                       1,411 6,086,364,955
hash-mode all · unhashed 0 · SHA256 mismatch 0 · license 12개 보존 검증 ·
overwrite 0 · 삭제 0 · 같은 볼륨 rename만
```

background archive 는 **실측으로** 정확히 3개였다. `modular_buildings_industrial_area(.)zip`
2개는 SHA256 `3f233a6b…` 동일(이름에 점이 하나 더 붙은 중복 다운로드)인데, **둘 다 보존
이동**했다. 상대경로를 보존하므로 basename 평탄화는 하지 않았다.
runtime/code/config/test 참조 0, gltf buffers/images URI 는 전부 상대경로라 의존 0.

## 2. 트랜잭션 도구 확장 (`manage_pallet_data_layout.py`)

Stage 2-A/2-B 는 **directory 만** 옮길 수 있었다. Stage 2-C2 는 두 가지가 더 필요했다.

```
신규 정책   --policy stage2c2-final-layout
file entry  entry_kind=file. background 안의 원본 ZIP 을 파일 단위로 분리해야 했다.
            snapshot_file / precheck_file / apply / verify / rollback 전부 대응.
group       transaction_group + 그룹 원자성. distractors 와 blender_scene 은 함께 가야
            의미가 있다(blend 상대참조가 distractors 를 가리킨다). 그룹 안에서 실패하면
            이미 옮긴 것을 역순으로 되돌린다.
archive 제한 ZIP 은 C2A cohort 에서만. directory cohort 계획 시 source 에 archive 가
            하나라도 있으면 ARCHIVE_IN_NON_PACKAGE_COHORT 로 거부.
```

### ★ 기존 테스트가 내 회귀를 잡았다 [확인]

group rollback 을 처음 넣었을 때, `transaction_group` 이 없는 Stage 2-A row 를 `cohort` 로
묶어버려 `test_apply_stops_at_the_first_failure_and_keeps_manifest_state` 가 FAIL 했다.
Stage 2-A 계약은 "실패하면 그 자리에서 멈추고 **이미 옮긴 것은 둔다**" 인데, 내 변경이
그걸 통째 rollback 으로 바꾼 것이다.
→ 그룹 원자성을 **명시적 `transaction_group` 이 있는 row 에만** 적용하도록 수정.
기존 39개 전부 통과 회복.

신규 테스트 `tests/test_stage2c2_layout_policy.py` **30개** (tmpdir fixture 전용,
실제 data/pallet 미사용): file entry / 상대경로 보존 / ZIP cohort 제한 / 그룹 원자성
(한쪽만이면 계획 거부, 두 번째 실패 시 첫 번째까지 롤백, 역순 rollback) / hash all 강제 /
legacy 계약 불변.

## 3. blend 상대경로 rebase

### 이동 **전에** 파일 정체를 고정했다

문자열만 기록하면 폴더가 옮겨진 뒤 무엇을 가리켰는지 알 수 없다. 그래서
`--emit-target-manifest` 를 새로 만들어 각 datablock 의 **현재 resolved 절대경로 + SHA256**
을 `blend_rebase_target_manifest.csv` 에 박아 두고, 이동 후 그 정체로 대조했다.

```
target manifest 603행
  SKIP_PACKED_OR_GENERATED   88
  KEEP_RELATIVE             158   //textures/... — blend 와 함께 이동해 문자열 그대로 유효
  REBASE                    357   distractor 356 + HDRI 1
  BLOCKED                     0   unknown root 없음
```

### action 판정을 한 번 틀렸다 [확인]

처음엔 `action` 을 "root 가 움직였는가"로 정했더니 `REBASE 514 / KEEP_RELATIVE 1` 이 나왔다
(기대와 정반대). textures 는 root 가 바뀌지만 문자열은 그대로 맞고, HDRI 는 root 가 그대로여도
blend 디렉토리가 옮겨져 문자열이 달라지기 때문이다.
→ **이동 후 디렉토리 기준으로 상대경로를 실제 계산해 기존 문자열과 비교**하도록 고쳤고,
그제야 `KEEP_RELATIVE 158 / REBASE 357` 이 나왔다.

### 계산된 새 경로 (하드코딩 아님)

```
textures    //textures\Image_0.jpg                        -> //textures/Image_0.jpg   (동일)
distractor  //..\distractors\medium\ph__SchoolChair_01\…  -> //../../../distractors/library/medium/…
HDRI        //..\assets\lighting\hdri\library\factory_…   -> //../../../lighting/hdri/library/factory_yard_2k.hdr
```

## 4. candidate → stable

```
1. Stage 2-C1 portable(5cad94e5…)을 byte 복사해 candidate 생성
2. candidate 만 열어(bpy.data.filepath 확인) manifest 에 있는 path 만 수정
3. 저장 전 게이트: absolute 0 / user-specific 0 / missing 0 / unresolved 0 / mismatch 0
4. save (compress=True) -> Blender 종료 -> 새 프로세스로 재개방 검증
5. same-volume rename 으로 stable 승격 (sha256 불변 확인)
```

```
파일                                          bytes         sha256      역할
─────────────────────────────────────────────────────────────────────────────────────
synth_data_scene.blend                    358,917,479  46f436dc…  rollback 3 (원본, 불변)
synth_data_scene_portable.blend           358,898,907  5cad94e5…  rollback 2 (C1, 불변)
synth_data_scene_portable_stage2c2.blend  358,898,838  8cb4109a…  ★ active
```

## 5. 재개방 검증

```
항목                     C1 portable   C2 stable   기대
──────────────────────────────────────────────────────
image datablock              603          603     동일
절대 외부경로                   0            0        0
사용자별 절대경로                0            0        0
missing path                   0            0        0
packed                        86           86     동일
textures resolve             158          158      158
distractor resolve           356          356      356
HDRI 외부 상대참조              1            1        1
target path mismatch           -            0        0
target SHA256 mismatch         -            0  (515건 대조)
변경된 datablock                -          357  (= REBASE 수)
계획 외 변경                    -            0        0
구조 diff                      -            0        0
```

## 6. registry / 코드 / 문서

```
registry (ok=22 -> ok=24, missing 0)
  production_scene                    -> assets/scenes/production/blender_scene/…_stage2c2.blend
  production_scene_stage2c1_rollback  -> …/synth_data_scene_portable.blend        (신규 키)
  production_scene_rollback_source    -> …/synth_data_scene.blend
  production_scene_textures           -> …/blender_scene/textures
  experimental_scene                  -> …/blender_scene/_sandbox_palletobj_production.blend
  background_root                     -> assets/scenes/backgrounds/background
  background_package_archive          -> archive/packages/background_sources      (신규 키)
  distractor_root / _manifest         -> assets/distractors/library
  나머지 13키 변경 없음
```

### registry 를 우회하지 않기 위해 한 일 [확인]

`blender.yaml` · `blender_train_4000.yaml` 의 배경 자산 경로가
`"filepath": "data/pallet/background/parking_lot/scene.gltf"` 리터럴이었다.
새 절대경로를 다시 박으면 다음 이동 때 또 같은 일을 해야 한다.
→ **background_root 기준 상대키**로 바꿨다: `"relpath": "parking_lot/scene.gltf"`.
`blender_config.py` 가 `PALLET_PATHS.get("background_root")` 와 조인한다(옛 `filepath` 도
계속 읽어 하위호환 유지).

옛 경로 스캔: CURRENT_RUNTIME / CURRENT_TEST / CURRENT_DOC 에서
`data/pallet/{distractors,blender_scene,background}` 참조 **0건**.
남은 것은 전부 `reports/` snapshot · `_docs/history` · `path_map.csv` 의 original_path 이며
전부 **수정하지 않았다**.

`distractor_pool_v2.py` 와 `distractors_manifest.csv` 는 손댈 필요가 없었다 —
전자는 이미 registry 조회, 후자의 `mesh` 컬럼은 distractor_root 기준 상대경로다(결측 0 확인).

## 7. distribution exclusion

`archive/packages/background_sources/` 신규 추가(원본 ZIP 이 해제본과 중복 배포되는 것 방지).
기존 10 entry 전부 유지. 검증 **entries 11 / problems 0 / release leaks 0 / exit 0**.

## 8. 검증

```
registry audit          ok=24 missing=0 absent_optional=0
default unit            646 passed (614 -> +30 신규 정책 +2 registry 규칙), skip 0, fail 0
local integration        31 passed (26 -> +5), skip 0, fail 0
golden overlay           51 passed, skip 0
Stage 2-A 원장           146 / 6,921 / failures 0, sha256 fe1adc26… **불변**
Stage 2-B 원장           B1 4/3,220 · B2 3/68, hash all, unhashed 0, failures 0
                        B1 sha256 43461e47… / B2 0d0c06a8… **불변**
Stage 2-C2 트랜잭션       C2A 3/3 · C2B 1/74 · C2C 2/1,334, hash all, unhashed 0, failures 0
5k FrameSpec            accepted 4,313 / rejected 687 / sha256 938f387d… 덤프 byte 동일
5k proposals            digest 3cd365ee… 동일, accepted 4,439, 12/12 PASS
distractor              manifest 209 · mesh 결측 0 · files/bytes 이동 전과 동일
background              gltf URI 결측 0 (42+27) · archive 0
Blender no-render       absolute 0 · missing 0 · textures 158 · distractor 356 · HDRI 1 ·
                        Dist_ 209 · HDRI 30/30 · floor 42/42 · wood 27/27 · node 누락 0
```

### ★ 감사기 카운터가 옛 문자열을 하드코딩하고 있었다 [확인]

최종 no-render 감사에서 `//../distractors=0` 이 나왔다. 파일이 깨진 게 아니라
카운터가 `//../distractors/` 문자열로 매칭하고 있었기 때문이다(새 형태는
`//../../../distractors/library/`). → `audit_blend_assets.py` 를 **resolve 된 절대경로가
registry root 안인지**로 세도록 바꿨다(위치 비의존). 안 고쳤으면
"distractor 참조 0" 이라는 **틀린 안심**을 보고할 뻔했다.

## 9. 2-frame smoke

```
data/pallet/runs/smoke/_stage2c2_final_layout_smoke2_seed7220/
seed 7220 · usable 2/2 · public · dataset-quality · noise clean · OPTIX · 61.07s
필수 93항목 실패 0
f0000  Pallet_3 / eur_pallet_bk_cc0.glb / parking_lot / gravel_concrete_02 / context-rich / 4.58m
f0001  Pallet_2 / woodpallet_block_jtoastie_ccby.glb / parking_lot / tile_brown /
       controlled-occlusion(Dist_utility_box_01) / cargo 2 / 2.10m
magenta 0 · visible ⊆ amodal 위반 0px · overlay canvas == RGB
라벨에 옛 경로(distractors/blender_scene/background) 문자열 0건
```

RGB 2장 + overlay 2장 **직접 열어 확인**. 중요한 것은
**두 프레임 모두에서 외부 distractor 가 실제로 렌더됐다는 점**이다
(f0000 = CAUTION WET FLOOR 표지판 + 캔/쓰레기, f0001 = utility box + 금속 선반 + 탄약통).
decode 통과만이 아니라 **렌더 시점에도** `//../../../distractors/library/` 가 해석된다는
직접 증거다. 전체 렌더 2 frames (상한 3).

## 10. 남은 것 (Stage 2-D)

```
남음    archive 대량 이동 (legacy_datasets 87.7GB + packages 80.8GB)
남음    최상위 ZIP 약 80GB · isaac_assets · NoAI quarantine
정리    blender_scene 안의 .blend1 자동 백업 4개 + legacy scene snapshot 6개
        (SHA256 로 역할 확정해 둠. synth_data_scene_portable_candidate_20260729.blend1 은
         원본과 동일 바이트, POSTBAKE_CLEAN == REBAKE_WIP 도 동일 바이트)
남음    packed 17건이 옛 AppData\Local\Temp 경로 문자열 보유 (inert, packed 라 수정 금지)
남음    inventory.csv -> grouped_inventory.csv 개명
남음    distractors GSO MTL 2건 map_object_normal 결측 (기존 상태, 이번 이동과 무관)
```

보고서 전문: `reports/data_pallet_cleanup/stage2c2/`
(baseline · source_inventory · background_packages · blend_rebase_target_manifest ·
transaction_policy · transactions/*.jsonl · candidate_apply_log · candidate_structure_diff ·
registry_before_after · old_path_scan · distribution_exclusion_audit ·
final_no_render_audit · smoke2_verification · rollback_plan · final_report)
